# P2-1 后端分层 · 阶段 A/B 实施记录

> 对应《FVCC_混乱度评价报告_细化版》§4.1（P2-1 后端分层细化方案）。
> 目标架构：`server/internal/` 按 `api / store / scheduler / remote / security / edl / media / node / version / protocol` 分层。
> 原则：每搬一个文件即 `go build` / `go test` 验证全绿，禁止行为重构。

## 1. 阶段 A（已完成：protocol / security / store / version）

| 目标包 | 迁入内容 |
|---|---|
| internal/protocol | apierr.go(.test)、applyjson.go(.test)（git mv）；新增 errcode.go（错误码常量独立文件） |
| internal/security | audit.go、crypto.go(.test)、gateway.go、smb_validate.go(.test)（git mv）；新增 ratelimit_core.go(.test)（限流核心拆分，导出 NewCooldownLimiter / NewSlidingWindowLimiter / SetNow） |
| internal/store | store.go、store_edl.go、store_proxy.go、trash.go(.test)（git mv）；新增 model/models.go（模型域独立包） |
| internal/version | version.go(.test)、VERSION（git mv，go:embed 单一来源） |

## 2. 阶段 B（已完成：edl / remote / media / ws）

| 目标包 | 迁入内容 |
|---|---|
| internal/edl | edl_validate.go（git mv，类型改 `model.*` 前缀）；新增 edl_support.go（共享常量/辅助）、export.go（大写导出包装）、edl_validate_test.go（纯校验单测） |
| internal/remote | remote.go + 4 个测试（git mv）；新增 export.go（RenderDispatcher 接口 / ParseHelloCaps / NormalizeRootForCompare 导出） |
| internal/media | ffprobe.go、localtranscode.go、localtranscode_syscall_{generic,windows}.go（git mv） |
| internal/ws | ws.go、ws_limit.go（git mv）；Hub 增加 SetClock / SetEmitHook 注入，globalStore 依赖改 TaskSnapshotProvider 注入；WSConnRegistry 方法导出（Acquire/Release/Count/Len） |
| 公共下沉 | security/rate.go（RateKey / ClientIP 从根包收口）、protocol/errcode.go 新增 ErrCodeRateLimited、main.go 注入 ws.TaskSnapshotProvider |

## 3. 根包兼容层（shim，阶段 C 随各域内联删除）

| 文件 | 转发内容 |
|---|---|
| models_shim.go | 模型符号（40+ 类型别名/常量/DefaultSettings/boolPtr 等）→ internal/store/model |
| store_shim.go | Store / NewStore / ErrProjectNotFound / MoveToTrash / TrashDirName → internal/store |
| edl_shim.go | edl 域全部小写符号 → internal/edl |
| remote_shim.go | RemoteClient / RemoteProgress / RemoteTaskStatus / RenderDispatcher 等 → internal/remote |
| media_shim.go | FFprobe / LocalTranscode* 等 → internal/media |
| ws_shim.go | Hub / SegInfo / TaskUpdateFullMsg / ProxyReadyMsg 等 → internal/ws |

引用适配：handlers*.go、router.go、main.go、ratelimit.go、scheduler.go 及关联测试同步改用 internal 导出符号；错误码统一收敛（如 errCodeEDLInvalid → edl.ErrCodeEDLInvalid）。

## 4. 验证状态（2026-09-18）

- 阶段 A：`go build ./...` + `go test ./...` 全量通过（根包 104.6s，含 e2e / 并发 / 迁移回归）。
- 阶段 B：`go build ./...` + `go vet ./...` + `go test ./...` 全量通过（根包 145.1s，internal/remote 4.6s；media、ws 无测试文件）。
- 阶段 B 收口复核（2026-09-19）：`go build ./...` + `go vet ./...` + `go test ./...` 全绿（exit 0），根包与 11 个 internal 包全部 ok；工作区干净（git status 无残留）。

## 5. 剩余待办（阶段 C）

| 文件 | 行数 | 目标域（建议） | 状态 |
|---|---|---|---|
| node_select.go / node_select_test.go | 417 / 356 | internal/node | ✅ 已完成（2026-09-19）→ `internal/node`，接口解耦后迁移，测试迁入 `internal/node` |
| proxy_flow.go / proxy_flow_test.go | 275 / 249 | internal/media | ✅ 已完成（2026-09-19）→ `internal/media`（proxy.go/proxy_test.go），三根解析收口 roots.go |
| scheduler.go / scheduler_edl_test.go | 1072 / 319 | internal/scheduler | ✅ 已完成（2026-09-19）→ `internal/scheduler`（scheduler.go + 3 测试），依赖抽象解耦 |
| stream.go / stream_test.go | 716 / 260 | internal/media | ✅ 已完成（2026-09-19）→ `internal/media`（stream_media.go），根包 stream.go 薄壳化、新增 hashutil.go；根包测试自建 stubProber 兼容桩 |
| handlers*.go、router.go、trash_handlers.go | — | internal/api | ⏳ 待办（阶段 C） |

阶段 B 提交记录（上层仓库 `D:\Fnos.VideoConversion`，2026-09-19）：
- `6c89e26` node layering into internal/node
- `e7f28f6` scheduler layering into internal/scheduler
- `ad057dd` proxy_flow layering into internal/media + roots 收口
- `3ca8e9e` stream media capability layering into internal/media
- `6151f3b` cross-domain adaption + test migration 收口

后续（阶段 C）：internal/api 收口（handlers/router/trash_handlers）；ratelimit.go 根包剩余组装逻辑随 api 层一并整理；各域 shim（edl_shim/media_shim/store_shim/node_shim/remote_shim/ws_shim/models_shim）随 api 迁入后内联删除；完成后跑全量 `go test ./...` 与 e2e 回归，并同步更新 README 任务状态与行数统计。

## 6. 清理项

- 删除 file-agent 遗留的 0 字节重定向残留：`server/build_out.txt`、`server/build_out2.txt`、`server/err.txt`（已移入回收站）。
- 清理 `P2-2_细化细则.md` 顶部 AIGC frontmatter 残留。

## 7. 提交建议

- FVCC 内禁止 `git init`，统一由上层仓库 `D:\Fnos.VideoConversion` 管理（当前 `git status` 已正确识别 21 组 RM 重命名）。
- 本次变更建议一次性提交，示例：`refactor(fvcc): P2-1 stage A backend layering (protocol/security/store/version)`。
- 提交前复查 `git diff --check` 与 AIGC frontmatter 清理情况。

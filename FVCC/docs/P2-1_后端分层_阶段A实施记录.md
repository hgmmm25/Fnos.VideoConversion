# P2-1 后端分层 · 阶段 A/B 实施记录

> 对应《FVCC_混乱度评价报告_细化版》§4.1（P2-1 后端分层细化方案）阶段 A：
> 目标架构为 `server/internal/` 下按 `api / store / scheduler / remote / security / edl / media / node / version / protocol` 分层；
> 阶段 A 先搬移叶子文件再搬移中层文件，每搬一个文件即 `go build` / `go test` 验证全绿，禁止行为重构。
> 本文档由 main agent 代 file-agent 执行完成后整理（file-agent 首次派发仅返回欢迎语未实质执行）。

## 1. 完成清单（迁入映射）

### 1.1 internal/protocol（协议与错误码）

| 源文件（server/） | 目标文件（internal/protocol/） | 说明 |
|---|---|---|
| apierr.go | apierr.go | git mv，保留历史 |
| apierr_test.go | apierr_test.go | git mv |
| applyjson.go | applyjson.go | git mv |
| applyjson_test.go | applyjson_test.go | git mv |
| —（新增） | errcode.go | 错误码常量独立文件，随包新建 |

### 1.2 internal/security（安全：审计/加密/网关/限流/校验）

| 源文件（server/） | 目标文件（internal/security/） | 说明 |
|---|---|---|
| security_audit.go | audit.go | git mv，审计常量/计数器随包导出 |
| crypto.go | crypto.go | git mv |
| —（新增） | crypto_test.go | 随包新建测试 |
| gateway.go | gateway.go | git mv |
| smb_validate.go | smb_validate.go | git mv |
| smb_validate_test.go | smb_validate_test.go | git mv |
| —（新增） | ratelimit_core.go | 由根包 ratelimit.go 限流核心拆分而来，导出 NewCooldownLimiter / NewSlidingWindowLimiter，并新增 SetNow 时钟注入 |
| —（新增） | ratelimit_core_test.go | 承接原根包 §4 三个限流器单元测试（窗口滚动/空闲回收/冷却生命周期），同包测试 |

### 1.3 internal/store（存储）与 internal/store/model（模型）

| 源文件（server/） | 目标文件 | 说明 |
|---|---|---|
| models.go | internal/store/model/models.go | git mv，模型域独立包 |
| store.go | internal/store/store.go | git mv |
| store_edl.go | internal/store/store_edl.go | git mv |
| store_proxy.go | internal/store/store_proxy.go | git mv |
| trash.go | internal/store/trash.go | git mv |
| trash_test.go | internal/store/trash_test.go | git mv |

### 1.4 internal/version（版本单一来源）

| 源文件（server/） | 目标文件（internal/version/） | 说明 |
|---|---|---|
| version.go | version.go | git mv，go:embed VERSION 单一来源 |
| version_test.go | version_test.go | git mv |
| VERSION | VERSION | git mv |

## 2. 兼容层与引用适配

- **models_shim.go（新增）**：main 包对模型符号（40+ 类型别名 / 常量 / DefaultSettings / boolPtr 等）以 type alias / const / func 转发到 `internal/store/model`，避免一次性改写 30+ 文件的引用点；阶段 C 模型按域拆分时随各文件内联清理。
- **store_shim.go（新增）**：main 包对 `Store / NewStore / ErrProjectNotFound / MoveToTrash / TrashDirName` 等以 type alias / var 转发到 `internal/store`。
- **handlers.go / handlers_edl.go / handlers_proxy.go / handlers_render.go / main.go / ratelimit.go / router.go / scheduler.go / ws.go**：引用路径与符号名适配（引用 internal 包、改用 security 导出名）。
- **测试引用迁移**：
  - `ratelimit_test.go`：私有构造器 / 时钟字段（`newCooldownLimiter`、`newSlidingWindowLimiter`、`l.now`、`l.events` 等）改为导出 API（`security.New*` / `SetNow`）；删除随核心迁走的三个限流器单元测试函数；清理未使用 import。
  - `security_audit_test.go`：`resetAlertCounters` 等旧符号改为 `security.ResetAlertCounters`。
  - `security_e2e_test.go`：`auditActionValidateReject / auditActionSecurityAlert / alertAssetScanThreshold / resetAlertCounters` 全部改为 `security.*` 导出符号并引入 `fvcc/internal/security`。

## 3. 验证结果

`go build ./...` 与 `go test ./... -count=1` 全量通过：

| 包 | 结果 |
|---|---|
| fvcc（根包） | ok（104.6s，含全部 e2e / 并发 / 迁移回归用例） |
| fvcc/internal/protocol | ok |
| fvcc/internal/security | ok |
| fvcc/internal/store | ok |
| fvcc/internal/store/model | 无测试文件 |
| fvcc/internal/version | ok |
| fvcc/logger / fvcc/smbshare | 无测试文件 |

搬移过程每完成一个文件即增量 `go build` / `go test`，未发生整体性回退。

## 4. 清理项

- 删除 file-agent 遗留的 0 字节重定向残留：`server/build_out.txt`、`server/build_out2.txt`、`server/err.txt`（已移入回收站）。
- 清理 `FVCC/P2-2_细化细则.md` 顶部 AIGC frontmatter 残留（AIGC 标记块已移除）。
- 根包 `internal/` 下新增文件无 AIGC frontmatter 残留（`.go` 不受注入影响，文档类仅 P2-2 一处已清理）。

## 5. 阶段 B 实施记录（EDL 域，2026-09-18）

### 5.1 EDL 域迁入 internal/edl

| 源文件（server/） | 目标文件（internal/edl/） | 说明 |
|---|---|---|
| edl_validate.go | edl_validate.go | git mv，保留历史；类型改 `model.*` 前缀（RenderTaskPayload/RenderProfile/Timeline） |
| —（新增） | edl_support.go | 集中共享常量与辅助：errCodeEDL* / errCodeNodeOffline / EDL 约束常量（edlMaxClips 等）/ 渲染常量（renderOutputExt 等）/ fvcsSampleRates / edlPresetSpec / edlRenderPresetTable / parseTimecode / edlErr |
| —（新增） | export.go | 大写导出包装：ErrCode* 常量、EDLAllowedSourceExt / FVCSampleRates / EDLRenderPresetTable、EDLValidationError / EDLPresetSpec 类型别名、全部校验函数、ParseTimecode / EDLErr |
| —（新增） | edl_validate_test.go | 纯校验单测迁入（向量用例 / 载荷校验 / 路径解析 / 状态映射 / Field-Index），同包测试 |
| —（新增） | server/edl_shim.go | 根包兼容层：全部小写符号经 internal/edl 转发，根包引用点零改动 |

### 5.2 引用适配

- handlers_edl.go：删除已迁符号（errCodeEDLInvalid / errCodeEDLTooLarge / edlMaxClips / edlMinClipMs / edlMaxTotalMs / edlMaxBodyBytes / edlErr），保留 handler 级错误码（errCodeRevConflict / errCodeProjectMissing / errCodeProjectNameUse / edlMaxNameLen）。
- handlers_render.go：删除已迁符号（errCodeProfileInvalid / errCodeNodeOffline / errCodeAssetMissing / errCodeAssetNotInRoot / renderPayloadType / renderOutputExt / renderContainerMP4 / renderAudioCodecAAC / fvcsSampleRates / edlPresetSpec / edlRenderPresetTable / parseTimecode），保留 reOutputBase / renderOutputBaseMax / renderOutputMaxTries / renderVideoPixFmt / renderAudioKbps / renderAudioChannels。
- edl_validate_test.go（根包保留集成部分）：PathValidator 用例 / newD02RenderEnv / 提交资产门 / 输出名门 / 共享根映射 / 载荷落库映射 6 个集成用例，经 shim 走 edl 包校验。

### 5.3 验证结果

`go build ./...`、`go vet ./...`、`go test ./...` 全量通过：

| 包 | 结果 |
|---|---|
| fvcc（根包） | ok（124.3s，含全部 e2e / 集成回归） |
| fvcc/internal/edl | ok（向量 / 载荷 / 路径 / 状态映射单测） |
| 其余 internal 包 | ok / 无测试文件 |

注意：`internal/edl` 测试工作目录为包目录，共用向量路径为 `..\..\..\..\FVCS\pkg\protocol\testdata\edl_vectors.json`（比根包深一级）。

## 6. 遗留问题与阶段 B 剩余工作

### 6.1 阶段 B 待搬移中层文件（仍位于 server/ 根包）

| 文件 | 行数 | 目标域（建议） | 状态 |
|---|---|---|---|
| edl_validate.go / edl_validate_test.go | 448 / 476 | internal/edl | ✅ 已完成（见 §5） |
| ffprobe.go | 275 | internal/media（或 edl） | 待办：与 handlers 强耦合，需接口重构 |
| localtranscode.go + syscall_generic/windows | 244 + 11 + 15 | internal/media | 待办 |
| node_select.go / node_select_test.go | 417 / 356 | internal/node | 待办：依赖 Handlers/Store，需接口重构 |
| proxy_flow.go / proxy_flow_test.go | 275 / 249 | internal/media | 待办 |
| remote.go + remote_*_test.go | 1111 + 677 | internal/remote | ✅ 已完成（见 §8） |
| scheduler.go / scheduler_edl_test.go | 1072 / 319 | internal/scheduler | 待办：依赖 Handlers/Store/节点池，需接口重构 |
| stream.go / stream_test.go | 716 / 260 | internal/media | 待办 |
| ws.go / ws_limit.go | 415 / 49 | internal/api 或 internal/ws | 待办 |

### 6.2 阶段 B/C 后续

- handler 层（handlers*.go、router.go、trash_handlers.go）迁入 internal/api；
- 随各域迁入逐步内联删除 models_shim.go / store_shim.go；
- ratelimit.go 根包剩余组装逻辑随 api 层一并整理（限流核心已在 internal/security）；
- 完成后再跑一次全量 `go test ./...` 与 e2e 回归，并同步更新 README 任务状态与行数统计。

## 7. 提交建议

- FVCC 内禁止 `git init`，统一由上层仓库 `D:\Fnos.VideoConversion` 管理（当前 `git status` 已正确识别 21 组 RM 重命名，历史可追溯）；
- 本次变更建议一次性提交，commit message 示例：`refactor(fvcc): P2-1 stage A backend layering (protocol/security/store/version)`;
- 提交前复查 `git diff --check` 与 AIGC frontmatter 清理情况。

## 8. 阶段 B 实施记录（Remote 域，2026-09-18）

### 8.1 Remote 域迁入 internal/remote

| 源文件（server/） | 目标文件（internal/remote/） | 说明 |
|---|---|---|
| remote.go | remote.go | git mv，保留历史；package 改 remote，类型加 `model.*` 前缀（Server/Task/Settings/NodeCaps/TaskType/GPUInfo/TaskTypeRenderEDL/TaskTypeGenProxy） |
| remote_edl_test.go | remote_edl_test.go | git mv，package 改 remote |
| remote_reconnect_test.go | remote_reconnect_test.go | git mv |
| remote_wss_test.go | remote_wss_test.go | git mv |
| genproxy_share_cred_test.go | genproxy_share_cred_test.go | git mv |
| —（新增） | export.go | 大写导出包装：ErrCodeSMBMountFailed / ErrCodeRenderFailed / ErrCodePayloadMissing 常量、RenderDispatcher 接口、ParseHelloCaps / NormalizeRootForCompare |
| —（新增） | server/remote_shim.go | 根包兼容层：错误码常量、RemoteClient / RemoteProgress / RemoteTaskStatus / RenderDispatcher 类型别名、NewRemoteClient 变量及 normalizeRootForCompare / parseHelloCaps 转发，根包引用点零改动 |

### 8.2 引用适配

- scheduler.go：删除已迁符号（RenderDispatcher 接口、errCodeSMBMountFailed / errCodeRenderFailed / errCodePayloadMissing），保留调度域专属错误码。
- handlers_render.go：删除 normalizeRootForCompare 原实现（经 shim 转发 internal/remote）。
- 错误码归一：`errCodeEDLInvalid` → `edl.ErrCodeEDLInvalid`（渲染校验错误码统一收敛至 internal/edl）；其余三错误码包内引用改导出名 `ErrCode*`。
- 测试文件：误替换修正（`json:"model.TaskType"` / 字段名 `model.TaskType` / `model.NodeCaps` 字段 / 注释恢复为裸类型）；包内错误码引用改导出名；remote_edl_test.go 补 `fvcc/internal/edl` import。

### 8.3 验证结果

`go build ./...`、`go vet ./...`、`go test ./... -count=1` 全量通过：

| 包 | 结果 |
|---|---|
| fvcc（根包） | ok（145.1s，含全部 e2e / 集成回归，经 remote_shim 走 internal/remote） |
| fvcc/internal/remote | ok（4.6s，含 EDL 下发 / 重连自愈 / WSS / 共享凭据解析单测） |
| fvcc/internal/edl 等其余 internal 包 | ok / 无测试文件 |

### 8.4 阶段 B 剩余待搬移文件

| 文件 | 行数 | 目标域（建议） | 状态 |
|---|---|---|---|
| ffprobe.go | 275 | internal/media（或 edl） | 待办：与 handlers 强耦合，需接口重构 |
| localtranscode.go + syscall_generic/windows | 244 + 11 + 15 | internal/media | 待办 |
| node_select.go / node_select_test.go | 417 / 356 | internal/node | 待办：依赖 Handlers/Store，需接口重构 |
| proxy_flow.go / proxy_flow_test.go | 275 / 249 | internal/media | 待办 |
| scheduler.go / scheduler_edl_test.go | 1072 / 319 | internal/scheduler | 待办：依赖 Handlers/Store/节点池，需接口重构 |
| stream.go / stream_test.go | 716 / 260 | internal/media | 待办 |
| ws.go / ws_limit.go | 415 / 49 | internal/api 或 internal/ws | 待办 |
*（内容由AI生成，仅供参考）*

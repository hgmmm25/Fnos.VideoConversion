




# 10 - 实施进度与下一步（维护台账）

> 本文件是 WebVideoEditor（Fnos.VideoConversion）P0 落地的**进度台账**，与 `08-P0实施计划与验收清单.md` 配套使用。
> 08 是计划与验收口径（不变），本文件记录**实际做到哪一步、证据在哪、下次从哪里继续**。
> 约定：每完成一项即更新本文件第 2、3、4 节；状态只允许取「未开始 / 部分完成 / 完成 / 待真机验证」。

- 台账更新日期：2026-09-14（本次修订：**D-04 落地并收口 D 组 4/4**——安全响应头 + 三档通用限流 + 双侧审计/阈值告警，FVCC 13 个 + FVCS 5 个 `TestD04*` 全绿；同步刷新 §2E 逐条状态、§3 第十四轮记录、§5 下一步为「真机联调 + E 组」；历史轮次记录与证据链原样保留）。上一版 2026-09-13（勘误 C-01 状态、修正 A 组计数与 D 组计数、补齐 C 组/M4/D 组逐条状态、重构 §5 为当前真实待办）。**第十轮追加**：A-06 断点续跑与 workDir 复用/孤儿清理落地、`pkg/task` 首建单测、FVCS `./pkg/...` 三项全绿，见 §3 第十轮；**第十一轮追加（2026-09-14）**：D-01 落地（`FVCS/pkg/protocol/edl_validate.go` + 共享向量 `pkg/protocol/testdata/edl_vectors.json` + 单测全绿），D 组由 0/4 转 **1/4**，见 §3 第十一轮；**第十二轮（2026-09-14）**：D-02 落地（FVCC 侧同源校验 + 渲染提交三闸门 + 共享根映射层，14 个 `TestD02*` 全绿），D 组转 **2/4**，见 §3 第十二轮；**第十三轮（2026-09-14）**：D-03 落地（`smb/credentials.go` DPAPI 密文档案 + `server/local_admin.go` 环回本机接口 + 三处任务创建前置拒单 + 启动期接入，`pkg/smb` 13 用例 + `pkg/server` 7 用例全绿），D 组转 **3/4**，见 §3 第十三轮；**第十四轮（2026-09-14）**：D-04 落地（安全响应头 + 三档限流 + 双侧审计/阈值告警），D 组转 **4/4**（代码层收口），见 §3 第十四轮）；**2026-09-16 第十六轮（缺陷修复 + 方案决策）**：① 修复 FVCC 剪辑项目列表不显示历史项目 BUG（前后端契约字段对齐，新增 2 个回归用例 + 进程级 E2E 复核）；② 修复 FVCS `AssignProcessToJob` 句柄误用导致的 ffmpeg 未纳入 Job 告警（改 `OpenProcess` 真实句柄 + `IsProcessInJob` 复核 + `SetInformationJobObject` 返回值校验，3 用例全绿）；③ 重打包 `FVCC_v1.2.1_fnos_x86.fpk`；④ **EasyTier 方案 A/B/C 全部作废，FVCS 保持独立应用手动启动**，详见 §5.0）；**2026-09-16 第十七轮（缺陷修复二）**：① 修复 FVCS 上报节点版本号硬编码（新增 `pkg/version`（`go:embed VERSION`），`hello.go` 的 `const agentVersion="1.2.0"` 改为 `version.Version`，二进制 `1.2.0` 清零）；② 修复 FVCC↔FVCS WS 约 15s 断链重连（服务端证伪 15s 主动断链后，FVCC `remote.go` 补「Connect 单飞 + 指数退避自动重连 + 优雅 Close 帧 + 关闭先停重连」，`TestB09*` 4 组全绿）；③ FVCS 重编译（25,734,551 B）并替换重启（新 PID 28980，日志 `version=1.2.1`）；④ FVCC 重打包 **`FVCC_v1.2.1_fnos_x86.fpk` 17,708,825 B（SHA256 `74B77404…E4638C`）**，详见 §3 第十七轮与 §5.0B）
- 代码根目录：`D:\Fnos.VideoConversion`（git 根）
- 子项目：`FVCS`（Windows 渲染端 Go 服务）、`FVCC`（NAS 调度端 + 自研 Web UI）、`WebVideoEditor_Design`（设计文档）、`freecut`（前端交互参考工程）

---

## 0. 环境与工具链（开工前必读）

| 项 | 结论 | 处置 |
|---|---|---|
| Go 工具链 | `C:\Program Files\Go`（1.26.7）**std 源码不完整**，编译报 std 缺包（如 `internal/stringslite`），`go test` 只回显包名无详细错误 | 用 `GOTOOLCHAIN=local` + 模块缓存内工具链 `C:\Users\xx318\go\pkg\mod\golang.org\toolchain@v0.0.1-go1.24.6.windows-amd64\bin\go.exe` |
| `-race` | 需要 CGO/gcc，本机未配置 | P0 阶段不使用，改用 `-count=1 -v` |
| `go vet` 历史告警 | 全仓 `logger.*` 拼接式参数触发 printf 检查，导致 `go test ./pkg/...` 整包 build failed | 已清零（40 处改为 `%v` 占位符）；**新增代码禁止拼接式日志** |
| 错误输出被截断 | PowerShell 下直接回显 `go build` 报错只出首行；**第十一轮实测补充**：管道 `2>&1 \| Set-Content`/`Out-File` 到 `$env:TEMP` 后 `read_text` 读不到（路径解析异常，误以为已落盘） | 已实证方案：`cmd /c "cd /d <repo> && <go> test ./pkg/... -count=1 1>\"<工作区temp>\t.log\" 2>&1"`，再用 `read_text` 读**工作区内**文件；禁用 `$env:TEMP`。切忌只凭首行推测根因 |
| Go 工具链（FVCC，**已更正**） | `D:\fntv-tools\go\bin\go.exe` 在本机**不存在**（路径已失效）；`C:\Program Files\Go`（1.26.7）std 源码不完整（报 `internal/stringslite is not in std`），**勿用** | FVCC 侧改用 `C:\Users\xx318\sdk\go1.25.3\go\bin\go.exe` + `GOTOOLCHAIN=local`，`build/vet/test ./...` 三项全绿（2026-09-11 实测） |
| FVCC 打包 | 直接 `fnpack build` 源码目录会因缺 `app/` 与 `app/ui/config` 失败 | 需标准骨架：`app/`、`manifest` 声明的 `desktop_uidir` 对应目录含 `config` 与 `images/icon_*.png` |

FVCS 编译与测试（PowerShell）：

```powershell
cd D:\Fnos.VideoConversion\FVCS
$env:GOTOOLCHAIN='local'
$go='C:\Users\xx318\go\pkg\mod\golang.org\toolchain@v0.0.1-go1.24.6.windows-amd64\bin\go.exe'
& $go vet ./pkg/...
& $go build ./pkg/...
& $go test ./pkg/... -count=1 -v
```

> 注：`go build ./...` 会因 `cmd/settings` 依赖 fyne + go-gl 在本机（无 CGO/GL 环境）失败，属既有环境限制，非本次改动引入；内核验证请用 `./pkg/...`。

---

## 1. 进度看板

| 组 | 范围 | 进度 | 状态 |
|---|---|---|---|
| A 组 | FVCS 渲染内核（08 §4.1，12 人日） | **7/8 项代码完成**（A-01~A-06、A-08）；**A-07 部分完成**——`pkg/server` 分支与 `Hello` 上报已就绪，仅缺 WS 端到端真机联调；`pkg/task` 已建单测（8 个 `TestA06*` 全绿）、`pkg/ffmpeg` 用例全绿；**真机（ffmpeg 在环）验证未做** | 部分完成 |
| B 组 | FVCC 调度与持久化（08 §4.2，13 人日） | 9/9 项代码就绪；单测全绿（`go build/vet/test ./...` 三项全绿，`ok fvcc 3.17s`，2026-09-13 复验）；**真机（FNOS 网关在环）权限验证未做** | 完成（代码层） |
| C 组 | 前端剪辑页（08 §4.3，15 人日） | 10/10 项代码就绪并已装配进 `renderEditor`（C-01~C-10，逐条见 §2C）；`npm run build`（`tsc -b && vite build`，30 modules）全绿（2026-09-13 实测）；依赖的 M4 后端四条路由已于 2026-09-13 注册 → **联调阻塞解除**；**2026-09-16 修复「剪辑列表不显示历史项目」前后端契约回归缺陷（见 §5.0）** | 代码完成（待真机联调） |
| M4 | 预览网关 + 代理工作流（04 号文档，M4-A/M4-B） | 2/2 项代码就绪（逐条见 §2D）；10 个 `TestM4*` 用例全绿；**真机（SMB/UNC 素材 + ffmpeg 在环）联调未做** | 完成（代码层） |
| D 组 | 安全与凭据（08 §4.4，6 人日） | **4/4**（逐条见 §2E）：D-01 完成——FVCS 侧 `pkg/protocol/edl_validate.go` + 共享向量 `pkg/protocol/testdata/edl_vectors.json`（relpath 14 / output 11 / payload 14 / symlink 1）+ `edl_validate_test.go`（**6 个顶层用例 / 46 个子例全绿，0 FAIL，`ok … 1.367s`，2026-09-14 第十一轮**），并在 `edl.go::ValidateRenderEDLPayload` 补上 `sourceRoot`/`destRoot` 白名单校验；**D-02 完成（2026-09-14 第十二轮）**——`FVCC/server/edl_validate.go` 同源复刻并加载**同一份向量**、`PathValidator.ValidateRel` L1~L4、渲染提交三闸门接入（严格素材预检 / 输出名白名单 / 载荷合规）+ 新增「共享根相对根」映射层（`toShareRelRoot`），`go build/vet/test ./...` 三项全绿（`ok fvcc 4.680s`），**14 个 `TestD02*` 全 PASS（0 FAIL/0 SKIP，含软链越权）**；**D-03 完成（2026-09-14 第十三轮）**——`FVCS/pkg/smb/credentials.go`（DPAPI/CryptProtectData 密文落库 + Scope/目标校验 + 占用删拒 + 180 天审计）+ `pkg/server/local_admin.go`（`/local/credentials` GET/POST/PUT/DELETE，非环回 403，永不回显密码）+ 三处任务创建前置拒单（`precheckCredentialTarget`）+ 启动期 `InitCredentialAdmin()` 接入，**`pkg/smb` 13 用例 + `pkg/server` 7 用例全绿，`go build/vet/test ./pkg/... ./cmd/service` 三项全绿**；**D-04 完成（2026-09-14 第十四轮）**——FVCC 侧安全响应头（`nosniff`/`no-referrer`/`SAMEORIGIN` 全链路）+ 鉴权失败/渲染提交/代理提交三档限流 + `security_audit.go`（`edlErr` 集中记账 + 5min 阈值告警），FVCS 侧 `pkg/server/audit.go`（七类动作落 `audit_log` + 同阈值告警，库不可用降级 WARN），**FVCC 13 个 `TestD04*` + FVCS 5 个用例全绿**；「`remote.go` 停止下发凭据」随 B-06 先行落地 | 完成（代码层） |

**已核实的关键事实（用于避免重复排查）**

- `FVCC/server/` 已新增：`store_edl.go`（B-01/B-02 持久化 + B-07 `ApplyRenderProgress`）、`handlers_edl.go`+`handlers_edl_test.go`（B-03）、`handlers_render.go`+`handlers_render_test.go`（B-04）、`scheduler_edl_test.go`（B-05 用例）、`remote_edl_test.go`（B-06 用例）、`ws_edl_test.go`+`ws_progress_edl_test.go`（B-07 用例）、`node_select.go`+`node_select_test.go`（B-08 选机/健康分/熔断/能力落库）；`scheduler.go` 已加 B-05 分流/冷却调度与 B-07 `HandleRemoteProgress`/`BroadcastNodeStatus` 接通、B-08 `resolveRenderNode`→`pickNode` 自动选机与派发侧 `NoteNodeAttempt`/`NoteNodeFailure`/`NoteNodeSuccess` 指标回填；`remote.go` 已实现 B-06 渲染下发（`RenderDispatcher`）并解析共享根 UNC、B-07 结构化进度（`RemoteProgress`）回传、B-08 `Hello` 能力上报解析（`parseHelloCaps` + `OnPushHello`）；`main.go` 已完成装配（`remote.SetSettingsProvider(store.GetSettings)` + `scheduler.SetRenderDispatcher(remote)` + `OnPushProgress`→`HandleRemoteProgress` + `OnPushHello`→`ApplyNodeHello` + `OnDisconnect`→按真实健康分广播 `node_status offline`）；`ws.go` 已实现 B-07 事件聚合（`BroadcastTaskUpdateFull` / `BroadcastProxyReady` / `BroadcastNodeStatus`）。
- B-05 遗留开关已收口：`taskPriorityRank` 只把 `GEN_PROXY` 判为低优档；`GEN_PROXY` 的"再减 1 空闲槽"（04 §3.5 / 06 §5.3）已由 B-08 `NodeFreeSlots`（`proxySlotCost`）实现。
- **勘误（2026-09-13 核对）**：本行旧版记为「`FVCC/ui-src/src/types.ts`、`api.ts` 中**没有** `edl`/`timeline`/`clips` 符号 → C-01 未开始」，**与代码现状不符**。实际 C-01 **已完成**：`types.ts` 已含 `Timeline`/`EDLClip`/`Project`/`ProjectSummary`/`ProjectUpdateResult`/`RenderRequest`/`RenderSubmitResult`/`RenderTaskView`/`StreamTicket`/`ProxyRequest`/`ProxyRequestResult` 及 WS 事件类型（03 §2 契约），`api.ts` 已含 `listProjects`/`createProject`/`getProject`/`updateProject`/`deleteProject`/`renderProject`/`createStreamTicket`/`streamUrl`/`thumbUrl`/`createProxy`。
- `FVCS/pkg/ffmpeg/` 现有文件：`caps.go`、`edl.go`、`edl_exec.go`、`edl_test.go`、`ffmpeg.go`。
- `FVCS/pkg/task/` 现有文件：`task.go`、`render_edl.go`、`proxy.go`、**`task_resume_test.go`（第十轮新建，8 个 `TestA06*` 用例）**；此前该目录无 `_test.go`，A-06 单测缺口已在第十轮补齐。
- M4（2026-09-13 第九轮）已新增：`FVCC/server/` 的 `stream.go`(795)/`stream_test.go`(293)、`proxy_flow.go`(262)/`proxy_flow_test.go`(275)、`handlers_proxy.go`(171)/`handlers_proxy_test.go`(143)、`store_proxy.go`(144)，以及 `FVCS/pkg/task/proxy.go`(217)；并改 `router.go`/`models.go`/`security.go`/`scheduler.go`/`store.go`/`handlers.go`。`router.go` 现可检索到 `/stream/ticket`、`/stream`（含 `HEAD`）、`/thumb`、`/proxy` 四条路由。

---

## 2. A 组逐条状态与证据

| ID | 交付 | 状态 | 证据（文件 / 符号 / 用例） |
|---|---|---|---|
| A-01 | `ffmpeg/edl.go` 骨架：`RenderPlan`/`SegmentPlan`/`BuildRenderEDLCommands` | 完成 | `pkg/ffmpeg/edl.go`：`BuildRenderEDLCommands`、`RenderPlan`/`RenderStage`、模式常量 `copy_all`/`segment_copy_final_encode`/`segment_copy_concat_filter`；用例 `TestT1_CopyAll_SameSource_NoReencode`（3 段 + 1 finalize，阶段序列 prepare→segment×3→concat→finalize） |
| A-02 | 分段命令构造（无音轨补静音、非 AAC 音频转码） | 完成 | `buildSegmentArgs` + `silentFill` 分支（**本轮修复缺 `-c:a aac`**）；`audioEncodeArgs`；用例 `TestT4_MissingAudio_SilentFill` |
| A-03 | 拼接两条路径（concat demuxer / filter_complex）+ `'` 转义 + >32 段分批 | 完成（代码） | `escapeConcatPath`（`\`→`/`，`'`→`'\''`）、`buildFilterGraph` 分批 `maxClipsPerBatch=32` + 批间二次 concat；用例 `TestT3_MixedSource_FilterConcat`、`TestA03_ConcatPathEscape`、`TestA03_FortySegment_BatchedFilterGraph`（40 段 → 32+8+合并）；**手工跑 40 段真机未做** |
| A-04 | 最终导出构造 + 归一化裁切（05 §4.6） | 完成（代码） | `buildFinalArgs` + `headOffsets` 的 `trim/atrim` 精度归一化；`-t` 以 `TotalMs` 为准（`TestT3` 断 `-t 00:00:09.000`）；**输出时长误差 ≤0.5s 待真机** |
| A-05 | 硬编协商与降级 + `Settings.progressWeights` | 完成（代码） | `edl.go`：`profileTable` + `resolveProfile`（硬编不可用→软编降级并写 `Warnings`）、`progressWeights`；`caps.go`：`ListEncoders`/`DetectGPUs`；用例 `TestT2_SameSource_Transcode_ProfileNegotiation` 覆盖降级；**无 NVENC 环境实测待做** |
| A-06 | `task.go`：`CreateRenderTask`/`executeRenderEDL`/阶段进度/续跑/workDir 复用与清理 | **完成（代码）**（2026-09-13 第十轮补齐续跑与单测） | 已实现：`pkg/task/render_edl.go` 的 `CreateRenderEDLTask`、`executeRenderEDL`、`setRenderStage`（签名改为收 `ffmpeg.StageRunInfo`）、`setRenderProfile`、`markRenderPlan`、`taskWorkingDir`、`resolveOutputUNC`、`previewFastCopy`；workDir 终态清理已接（`cleanup`）。**本轮新增**：`Task.SegDone`/`SegTotal`/`ResumePending`（task.go:79/90）+ `task_list` 的 `seg_done`/`seg_total` 列迁移（task.go:186/217）；`recoverRuntimeTask`（task.go:365，重启时渲染类置 `Waiting+ResumePending`、转码类仅置 `Waiting`）、`isRenderTaskType`(379)、`effectiveResumeSegDone`(393)、`mergeRenderRunInfo`(411)、`planOrphanWorkDirCleanup`(459)、`cleanupOrphanWorkDirs`(482)；`pkg/ffmpeg/edl_exec.go` 新增 `RunRenderPlanResume`(53，`resumeSegDone>0` 跳过已完成段)、`ResumableSegmentPrefix`(174，校验 workDir 前 N 段完整性并清理断点后残留)、`segmentArtifactComplete`(219)；`StageRunInfo` 增 `SegDone`(35)。单测 `pkg/task/task_resume_test.go`（新建，8 用例全绿）。**剩余**：真机杀进程重启验证（08 E2E-05，见 §4「断点续跑」）。**注：workDir 孤儿清理阈值为「保留最近 3 个 + 删除 7 天前」，与 06 §3.4/05 §4.5 口径一致** |
| A-07 | `server.go`：`CreateRenderEDL`/`CreateGenProxy` 分支 + `Hello`/`Progress` 载荷扩展 | **部分完成** | 已实现：`pkg/server/server.go` 已接 `CreateRenderEDL`/`GenProxy` 分支；`pkg/server/hello.go` `handleHello` + `buildHelloData`（`agentVersion=1.2.0`、`supportsRenderEDL`/`supportsGenProxy`/`encoders`/`gpu`/`running`）；`pkg/protocol/hello.go` 契约；`pkg/task/proxy.go` GenProxy 全链路。**缺口：WS 端到端手工联调未做** |
| A-08 | `pkg/ffmpeg/edl_test.go` 覆盖 05 §9 T1~T4、T7 | 完成 | 8 个用例全绿（`ok Fnos.VC_Service/pkg/ffmpeg 1.28s`） |

补充：`pkg/protocol/edl.go` 已含 `ValidateRenderEDLPayload`（type/clips/out>in/TotalMs 一致性）、`ErrCodeEDLInvalid`/`ErrCodeAssetMissing`/`ErrCodeProfileInvalid`/`ErrCodeTimeoutStall`；`pkg/ffmpeg/edl_exec.go` 已含 `RunRenderPlan`（阶段执行）、`prepareWorkspace`、`runStage`（`parseProgressLine` 进度）、`finalizeOutput`、`copyFileWithProgress`。

---

## 2B. B 组逐条状态与证据

| ID | 交付 | 状态 | 证据（文件 / 符号 / 用例） |
|---|---|---|---|
| B-01 | `store.go`：`tasks` 列迁移 + 四张新集合建表 | 完成 | `store.go`：`loadTasksLocked` 列迁移；`projects`/`node_caps`/`node_health_samples`/`audit_log` 加载（缺失按空集合初始化，不阻断启动） |
| B-02 | 项目 CRUD + `rev` 乐观锁 | 完成 | `store_edl.go`：`CreateProject`/`UpdateProject`（期望 rev 不符 → `ErrRevConflict`）/`GetProject`/`GetProjects`/`DeleteProject`/`normalizeProject`/`newProjectID`；用例 `TestProjectCRUDAndOptimisticLock` |
| B-03 | `/api/edl/projects*`（列表/详情/新建/保存/删除） | 完成 | `handlers_edl.go`：`listEDLProjects`/`getEDLProject`/`createEDLProject`/`updateEDLProject`/`deleteEDLProject` + `edlErr`/`edlViolation`；`router.go` 已注册 5 条路由；用例 `TestEDLProjectCRUDContract`/`TestEDLProjectValidationGate`/`TestEDLProjectLimits` |
| B-04 | `renderEDLProject`（幂等键、重名检测、`totalMs` 计算） | 完成 | `handlers_render.go`：`renderEDLProject`（POST `/edl/projects/:id/render`）→ 项目 404 / presetKey 枚举闸门 / 复用 B-03 `validateProjectInput` / `normalizeOutputBase`+`uniqueOutputName`（补 `.mp4`、重名 `_1`/`_2`、保留名与非法字符拒绝）/ `precheckAssets`（本地可见才判 `E_ASSET_MISSING`404、越界 `E_ASSET_NOT_IN_ROOT`403，否则降级交 FVCS）/ `buildRenderPayload`+`computeFastCopyAllowed`+`computeRenderChecksum`（sha256 前 16 位，**与 FVCS `ComputeRenderChecksum` 同形**）/ `totalMs=Σ(outMs−inMs)` / 幂等复用 `FindActiveTaskByChecksum`+`FindSuccessTaskByChecksum`（`force:true` 跳过）/ 指定节点离线 409 `E_NODE_OFFLINE` / `UpsertTask` 入队 + `SetProjectLastRenderTask` + `AppendAudit`；`router.go:79` 已注册该路由（`TODO(B-09)` 为权限中间件）。用例 `handlers_render_test.go`：`TestB04RenderSubmitContract`、`TestB04RenderTotalMsAndFastCopyVariants`、`TestB04RenderIdempotentReuse`、`TestB04RenderReuseSuccessTaskWithArtifact`、`TestB04ChecksumStableAndScope`、`TestB04NormalizeOutputBase`（17 子例）、`TestB04UniqueOutputNameAppends`、`TestB04RenderErrorContract`（10 子例）、`TestB04RenderServerIDPinned` 全绿 |
| B-05 | `scheduler.go` 分流 + 冷却到期扫描 + 代理低优 | 完成 | `scheduler.go`：`RenderDispatcher` 接口（`CreateRenderEDL`/`CreateGenProxy`，B-06 实现并注入）+ `Scheduler.dispatcher`/`SetRenderDispatcher`；`processTask` 的 `StatusQueue` 分支按 `TaskType` 分流（`RENDER_EDL`→`handleRenderEDL`、`GEN_PROXY`→`handleGenProxy`、`""`/`TRANSCODE`→既有 `handleQueue` 零改动），`StatusCooldown` 显式空转、渲染任务的 `RUNNING`(`StatusTranscoding`) 不再走 `checkTranscodeProgress`；`handleRenderEDL`/`handleGenProxy`→共用 `dispatchRenderLike`（载荷预检 → `resolveRenderNode` 校验 → `AcquireTransLock`+`AcquireCodeLock` → 下发 → 成功后 `RUNNING` 且释放双锁）；`failRenderTaskWithCooldown`（`attempt+=1`、`cooldownSec=CooldownBackoffSec(attempt)`、`next_retry_at=now+coolSec`）与 `failRenderTaskPermanent`（FAILED，归档交既有 `handleError`）；`promoteCooldownTasks` 冷却到期扫描（→`QUEUE`，保留原 `OrderID`）；`sortQueueTasks`/`lessQueueTask`/`taskPriorityRank`（键 = 优先级档位 + `OrderID`，`GEN_PROXY` 强制低优）+ `nextQueueTask`（串行取队首 + `processing` 去重）；`classifyRenderError` + 可重试/不可重试错误码白名单。用例 `scheduler_edl_test.go`：`TestB05RenderDispatchSuccess`、`TestB05GenProxyDispatchAndLowPriority`、`TestB05CooldownBackoffSequence`（30/60/120/240/480 全序列 + 到期提升 + 超限 FAILED + 归档）、`TestB05ErrorCodeWhitelist`、`TestB05NodeGuards`、`TestB05SideGuards` 全绿 |
| B-06 | `remote.go` `CreateRenderEDL`/`CreateGenProxy`（仅 `credentialId`） | 完成 | `remote.go`：`var _ RenderDispatcher = (*RemoteClient)(nil)` + `CreateRenderEDL`/`CreateGenProxy`→共用 `dispatchRender`（载荷非空且合法 JSON 才下发，否则 `E_PAYLOAD_MISSING`/`E_EDL_INVALID` 本地拦截 → `resolveRenderSharePaths` 解析共享根 UNC → `wsCmd{Cmd, TaskId=本端 TaskID, TaskType, PriorityLevel, Payload, SMBPath, SMBOutputPath, CredentialId}` 经 `roundTrip` 60s 下发 → 返回 FVCS `TaskId`，为空则 `E_RENDER_FAILED` 不外标 RUNNING）；**安全红线：渲染下发不设置 `SMBUser`/`SMBPassword`**（`wsCmd` 新字段仅 `TaskType`/`PriorityLevel`/`Payload`/`SMBOutputPath`/`CredentialId`，凭据由 FVCS 侧按 `credentialId` 解析，07 §5.2/§5.5）；FVCS 业务拒绝时透出 `Msg` 中的 `E_*` 码供 `classifyRenderError` 分类（06 §4.3）；`SetSettingsProvider` 注入 `store.GetSettings` 用于共享根解析；`toSMBUNC` 四段解析（UNC 直传/归一化 → `smbUser` 走 `smbshare.BuildSMBURL` → `smbSharePath` 前缀拼接共享相对路径 → 均不可用报 `E_SMB_MOUNT_FAILED`，绝不猜测映射）；`main.go` 装配 `remote.SetSettingsProvider(store.GetSettings)` + `scheduler.SetRenderDispatcher(remote)`。用例 `remote_edl_test.go`：`TestB06RenderDispatchWireContract`（含 RENDER_EDL/GEN_PROXY 两子例：Cmd/TaskType/PriorityLevel/TaskId/CredentialId/SMBPath/SMBOutputPath/payload 逐字段断言 + 报文口令字段禁传 + 不得携带 `FFmpegArgs`）、`TestB06SharePathResolution`（5 子例 + 无配置报错）、`TestB06RenderDispatchPayloadGuards`、`TestB06RenderDispatchSurfacesFVCSErrors` 全绿 |
| B-07 | `ws.go` `BroadcastTaskUpdateFull` + 聚合 + `node_status`/`proxy_ready` | 完成 | `ws.go`（440 行）：新增 `SegInfo`/`TaskUpdateFullMsg`/`ProxyReadyMsg`/`NodeStatusMsg` 三个线上消息体 + 常量 `taskUpdateAggInterval=500ms`/`proxyReadyDedupeWindow=10s`/`nodeHealthBandSize=20`/`healthScoreUnknown=-1`；`Hub` 增加 `agg`/`proxySeen`/`nodeState` 三张聚合表与 `clock`/`emitHook` 测试注入口；`BroadcastTaskUpdateFull`（窗口内暂存最新值 + `time.AfterFunc` 到期补发、内容指纹未变不广播、线协议终态立即直发并清出聚合表、progress 钳位 [0,100]）、`BroadcastProxyReady`（同 assetId 10s 去重 + 惰性清理 + 空 assetId 拒发）、`BroadcastNodeStatus`（状态变化或 20 分档变化才广播，健康分 `-1` 不分档）。落库与接通：`store_edl.go` 新增 `RenderProgress` 结构 + `ApplyRenderProgress`（progress 单调不回退 + 钳位、stage/seg/时长/速度空值保持、已收口任务不覆盖）；`scheduler.go` 新增 `HandleRemoteProgress`/`findProgressTask`/`renderSegInfo`/`renderProgressLabel`/`BroadcastNodeStatus`（按 `server_id`+`remote_task_id` 反查、兜底本端 ID，渲染任务走结构化进度、转码任务保持原语义）；`remote.go` 的 `OnPushProgress` 改为回传 `RemoteProgress{TaskID,Stage,SegIndex,SegTotal,Progress,OutTimeMs,TotalMs,Speed}`；`main.go` 接线 `OnPushProgress`→`HandleRemoteProgress`、`OnDisconnect`→`node_status offline`；`handlers.go` 连通性测试成功/失败广播 `node_status online/offline`。用例 `ws_edl_test.go`（4 用例）+ `ws_progress_edl_test.go`（3 用例）：`TestB07TaskUpdateFullAggregation`、`TestB07TaskUpdateFullGuards`、`TestB07ProxyReadyDedupe`、`TestB07NodeStatusBroadcast`、`TestB07ApplyRenderProgressStore`、`TestB07HandleRemoteProgressRender`、`TestB07HandleRemoteProgressTranscode` 全绿 |
| B-08 | 选机 `pickNode` + 健康分 + 熔断 + `Hello` 能力落库 | 完成 | `node_select.go`（455 行，新文件）：常量 `minAgentVersionForRender="0.2.0"`/`defaultNodeMaxConcurrent=1`/`proxySlotCost=1`/熔断 `nodeCircuitWindow=5m`+`nodeCircuitFailThreshold=3`+`nodeCircuitDuration=10m`/健康分罚分 `healthFailPenalty=40`+`healthStallPenalty=15`+`healthMountFailPenalty=10`+`healthOfflinePenalty=20`/打分权重 `0.5/0.3/0.2`（命中 100、未命中 20）；`nodeMetrics{attempts,failures,stalls,mountFails,circuitUntil,probing}` 内存窗口（`withMetrics` 加锁、`trimBefore`/`countWithin`）+ `NoteNodeAttempt`/`NoteNodeFailure`（按 `E_TIMEOUT_STALL`/`E_SMB_MOUNT_FAILED` 归集并触发熔断）/`NoteNodeSuccess`（探测成功清窗解熔断）+ `nodeCircuitOpen`（熔断内跳过、到期置 `probing` 放行 1 个探测任务）；`healthScoreAt`/`HealthScoreOf`（clamp[0,100]，离线 20 分罚在节点状态判定处）；`NodeFreeSlots`（`nodeMaxConcurrent − runningCount`，`GEN_PROXY` 再 `−proxySlotCost`）+ `runningCount`（非终态占用槽任务计数）+ `nodeMaxConcurrent`；`requiredEncoder`（`GEN_PROXY`→`libx264`，`copy`/空不惩罚）+ `capabilityMatch`（能力未上报按命中）；`agentVersionAtLeast`/`leadingInt`/`nodeVersionOK`（能力未上报不排除）；`nodeSelectable`（在线 + 非 `IsLocal` + 版本达标 + 非熔断）/`onlineNodes`/`earliestFreeNode`（按已占用槽升序）；`pickNode`（显式 `serverId`：不存在→`E_NODE_NOT_FOUND`、离线/版本不符→`errNodeUnavailable`＝`E_NODE_OFFLINE`、满槽仍返回该节点排队不换机；自动：`0.5×free/maxC×100 + 0.3×健康分 + 0.2×能力分`，全忙→`earliestFreeNode`，无候选→`errNoSelectableNode`）；`ApplyNodeHello`（`UpsertNodeCaps` + 按真实健康分 `BroadcastNodeStatus`，低版本附 reason 文案）。接线：`scheduler.go` 的 `resolveRenderNode`（line 958）改为调 `pickNode`（无候选保持 QUEUE 不耗重试、可重试错误走冷却、不可重试走 FAILED），`dispatchRenderLike` 派发前 `NoteNodeAttempt`（line 817）、失败 `NoteNodeFailure`、成功 `NoteNodeSuccess`；`remote.go` 新增 `OnPushHello func(serverID string, caps NodeCaps)`（line 30）+ `helloPush`（line 124）+ `parseHelloCaps`（line 140，camelCase/snake_case 逐字段合并、支持嵌套 `caps`/`nodeCaps`，载荷缺失以连接身份兜底）+ `readPump` 的 `Cmd=="Hello"` 分支触发（line 325）；`main.go` 装配 `OnPushHello`→`ApplyNodeHello`，`OnDisconnect` 离线广播改用 `HealthScoreOf`（原 `healthScoreUnknown=-1`）。用例 `node_select_test.go`（403 行，新文件）10 个全绿：`TestB08PickNodeExplicitPreference`、`TestB08AutoSelectPrefersFreeCapacity`、`TestB08AutoSelectPrefersHealthyNode`、`TestB08AutoSelectPrefersCapabilityMatch`、`TestB08AllBusyFallsBackToEarliestFree`、`TestB08HealthScoreFormula`、`TestB08CircuitBreaker`、`TestB08ApplyNodeHello`、`TestB08ParseHelloCaps`、`TestB08AutoPickThenDispatch` |
| B-09 | 路由注册 + 权限中间件挂载 | 完成 | 路由注册已随 B-03 完成；B-09 补齐**权限挂载**：`router.go` 7 条 `DELETE` 路由（`/log`、`/video-cache`、`/servers/:id`、`/profiles/:id`、`/tasks/:id`、`/history/:id`、`/edl/projects/:id`）+ `POST /edl/projects/:id/render` 挂 `requireAdmin()`；`gateway.go` 新增 `errCodeForbidden`（`E_FORBIDDEN`）并把拒绝响应改为 03 §4.1 统一契约（403，独立模式仍放行）；用例 `router_auth_test.go`：`TestB09RenderAndDeleteRequireAdmin`（非 admin `/render` → 403 `E_FORBIDDEN`、admin/root 放行、7 条 DELETE 路由逐一断言）、`TestB09ProjectReadWriteOpenToReadonlyUser`、`TestB09StandaloneModeBypassesAdmin` 全绿；`TODO(B-09)` 已清零 |

### B 组实现中的决策（文档缺口对齐）

| 项 | 决策 | 依据 |
|---|---|---|
| 错误码 `E_PROJECT_NOT_FOUND` / `E_PROJECT_NAME_USED` | 03 §5.3 未定义，实现补充（404 / 409） | 项目域需区分「不存在」与「重名」；建议回写 03 §5.3 |
| `Project.clipCount`/`totalMs` | 契约可见但**不落盘**：写盘前清零，`Load` 后由 `normalizeProject` 重算 | 03 §2.2「派生字段不持久化」；修复前二者为 `json:"-"`，重启即归零 |
| `schemaVer`/`lastRenderTaskId` | 随项目 JSON 落盘（`omitempty`） | 03 §7；修复前 `json:"-"` 导致 `schema_ver` 重启丢失 |
| 缺省回填 | `fps=30`/`sampleRate=48000`/`width=1920`/`height=1080`；`audio` 必须为 `true` | 03 §2.2 默认值 + §2.5「audio P0 恒 true」 |
| 体积上限 | 项目 JSON 256 KB，超限 413 `E_EDL_TOO_LARGE` | 07 §3.6 |
| 扩展名白名单 / 落地 `EvalSymlinks` | B-03 **不实现**，留给 D-02 `FVCC/server/edl_validate.go` | 07 §3.2 L3/L4，避免双份实现 |
| 时间线零值判定 | `UpdateProject` 以 `p.Timeline != (Timeline{})` 判「是否提供时间线」；B-03 的 `validateTimeline` 已强制回填四个数值字段并要求 `audio=true`，故经 API 的更新不会误命中 | 记录以备 C 组或后续直接调 store 时注意 |
| **幂等键范围（与 06 §4.4 偏差）** | checksum 仅由 `clips + profile` 规范化后 sha256 前 16 位构成，**`output` 不参与**（`FastCopyAllowed` 置 false 后计算），与 FVCS `pkg/protocol.ComputeRenderChecksum` 逐位一致；因此「仅改输出名再提交」会被幂等复用（需 `force:true` 强制重渲染） | 06 §4.4 称含 `output`，但 A 组已落地的 FVCS 权威实现不含；两侧不一致会导致 FVCS 校验告警，**建议回写 06 §4.4** |
| **路径语义：本地绝对路径 → 共享相对根（D-02 已落地映射层）** | 本地物理视图仍以绝对路径工作（`TRIM_DATA_ACCESSIBLE_PATHS` + `PathValidator.AccessPaths`）；协议内根由 D-02 新增的 `toShareRelRoot` 以 `settings.smbSharePath` 为基准映射为**共享相对根**（如 `videos`/`exports`），下发给 FVCS 的 `sourceRoot`/`destRoot` 不再出现盘符；**未配置共享根时保留本地写法并对仅根写法错误降级告警**（不阻断提交，交由 FVCS `MountSMB` 后 stat 兜底） | 03 §2.3 / 09 §2 要求协议内不出现磁盘绝对路径；`toShareRelRoot` 严格要求本地根位于共享根之下（大小写不敏感、UNC 与重复斜杠归一），不匹配则拒绝映射而非猜测 |
| **素材预检降级** | 仅在 `os.Stat` 确认根为本地可访问目录时才做逐 clip 存在性/越界预检（`E_ASSET_MISSING` 404 / `E_ASSET_NOT_IN_ROOT` 403）；根本地不可见则跳过本地预检，语义上退化为「交由 FVCS `MountSMB` 后 stat 兜底」，避免 NAS 上所有提交误报 404 | 03 §5.2 要求 FVCC 侧 `os.Stat` 预检；但 FVCC 运行于 NAS、素材在 Windows 节点 SMB 侧，强行 Stat 会造成误判 |
| **素材预检严格分支（D-02）** | 渲染提交闸门 6 在「素材根已落在授权目录内」时走 `PathValidator.ValidateRel` 的 L1~L4 严格校验（软链越权判 `E_ASSET_NOT_IN_ROOT` 403），**并在其后自行 `fileExists` 判定存在性** | `edlResolveRelPath` 对不存在的文件走「父目录解析 + 文件名拼接」的宽松路径（输出文件场景需要），故素材存在性不能在 L4 内解决；漏判会让缺失素材以 200 通过（第十二轮首跑即由此暴露） |
| **仅根写法错误降级告警（D-02）** | 闸门 8 对 `field ∈ {sourceRoot, destRoot}` 的校验错误仅记告警不阻断（`isRootPathStyleErr`），其余载荷违规仍按契约拒绝 | 共享根映射层依赖 `settings.smbSharePath`；未配置时沿用本地根写法属预期状态，不应把整个提交判死（03 §2.3 的绝对路径禁令由 E-01 部署侧配置共享根后完全达成） |
| **输出名白名单收紧** | `reOutputBase = ^[A-Za-z0-9_\x{4e00}-\x{9fa5}][A-Za-z0-9_.\-\x{4e00}-\x{9fa5}]{0,79}$`：允许中文/英文/数字/`_`/`-`/`.`，**不允许空格、括号与方括号**（严于 07 §3.5 示例正则） | 07 §3.5 示例含 `（）()[]` 与空格；收紧为更小字符集，仍符合「结构化白名单」，且本轮修复了首字符仅允许 ASCII 导致纯中文输出名被拒的缺陷 |
| **渲染任务重试上限 = 5（与既有转码路径不同）** | B-05 渲染路径固定 `renderMaxRetryDefault = 5`，不读 `settings.maxRetry`（`DefaultSettings` 默认 3）；转码路径继续用既有 `settings.maxRetry` | 06 §4.3 明确「MaxRetry 默认 5」且退避序列 30/60/120/240/480 共 5 档，与 3 次上限不兼容；两者语义域不同，故不合并 |
| **冷却状态在 tick 中显式空转** | `processTask` 新增 `case StatusCooldown: return`（不再落入既有的 `default` 分支） | 06 §4.3 规定 COOLDOWN 只等 tick 的到期扫描转回 QUEUE；避免被既有 default 分支（重新入队/推进）干扰 |
| **渲染任务 RUNNING 不走 HTTP 探活** | `TaskType ∈ {RENDER_EDL, GEN_PROXY}` 且 `Status ∈ {RUNNING}` 时 `processTask` 直接返回，进度由 B-07 的 WS 事件聚合推进 | 既有 `checkTranscodeProgress` 依赖本地转码路径与 ffprobe，对渲染任务会误判失败（06 §4.2 进度经 WS 上报） |
| **派发成功即置 RUNNING(RUNNING 线协议)** | 渲染任务派发成功 → 内部 `StatusTranscoding`（`WireName()=="RUNNING"`），写 `server_id`/`server_name`/`remote_task_id`、`stage=prepare`、清空 `next_retry_at` | 06 §4.2「更新 status=RUNNING + server_id + next_retry_at=null」；未新增别名状态，避免前端/历史归档要处理两套 RUNNING |
| **未指定 `serverId`：自动选机（B-08 已接管）** | `resolveRenderNode` 调 `pickNode`：有候选即按打分派发；**无候选（全部离线/熔断/版本不符）时保持 QUEUE**（不耗重试次数、不计冷却，等节点恢复自然推进）；显式 `serverId` 满槽时仍返回该节点排队、不换机 | 06 §5.3「手工优先 + 打分降级」；B-05 期因无健康分/熔断数据不选机，B-08 补齐后接管 |
| **节点指标（尝试/失败/卡死/挂载失败/熔断）存内存，不落盘** | `nodeMetrics` 仅存于 `Scheduler.nodes`（`nodeMu` 保护）；FVCC 重启后窗口自愈重算 | 06 §2.1 未定义「近 N 分钟」样本表；落盘需额外表与清理策略，收益低 |
| **熔断与维护态无 `Server` 字段** | 既有 `Server` 模型无 `Maintenance`/`CircuitUntil` 字段，B-08 未改模型：熔断态由内存 `circuitUntil` 表达（自动选机跳过，显式指定仍放行）；维护态沿用 `Server.Status != "online"` 判定 | 避免为熔断改线上模型与持久化；06 §5.4 语义（熔断期内不被自动选机命中）已满足 |
| **健康分缺省值不再使用 `-1`** | `BroadcastNodeStatus` 现由 `HealthScoreOf` 传真实分；`healthScoreUnknown=-1` 仅保留为「无指标节点」的极端缺省（不参与 20 分档） | 06 §5.2 + §6；避免每次心跳都触发广播 |
| **错误码补充：`E_NODE_NOT_FOUND` / `E_PAYLOAD_MISSING`** | 06 §4.3 未定义，B-05 实现补充：前者 404 语义（`server_id` 指向的节点不存在）、后者（`payload_json` 为空）一律**不可重试**直接 FAILED | 前者为提交侧脏数据、后者为提交侧缺陷，重试无意义；建议回写 06 §4.3 |
| **未识别错误归一为 `E_RENDER_FAILED`** | `classifyRenderError` 先查不可重试黑名单，再查可重试白名单，均未命中则返回 `E_RENDER_FAILED`（可重试），由 5 次上限兜底 | 06 §4.3 未覆盖「未知错误」；归一到白名单内的可重试码，保证临时故障可自愈且不会无限重试 |
| **锁有效期缺省 300s** | `dispatchRenderLike` 取 `server.LockExpireSec`，<=0 时用 `defaultDispatchLockSec = 300`；派发返回后**立即** `ReleaseCodeLock`+`ReleaseTransLock` | 06 §4.5「进入 RUNNING 或派发失败后立即释放」；缺省值仅为崩溃兜底，避免节点未配置时锁永不过期 |
| **`RenderDispatcher` 以接口注入** | B-05 定义接口（`CreateRenderEDL`/`CreateGenProxy` `(Server, Task) (string, error)`），B-06 在 `remote.go` 实现并在 `main.go` 注入；未注入时渲染任务保持 QUEUE | 06 §4.2 的线协议属 B-06；接口注入让 B-05 单测可用替身覆盖全部分支，且 B-06 未落地时不会误置 RUNNING |
| **渲染下发仅带 `credentialId`，绝不下发口令** | `wsCmd` 新增 `TaskType`/`PriorityLevel`/`Payload`/`SMBOutputPath`/`CredentialID` 五个字段，**不设置** `SMBUser`/`SMBPassword`（结构体虽有该二字段，属既有转码路径用）；B-06 用例对整份上报 JSON 扫描 `SMBUser`/`SMBPassword`/`smbUser`/`smbPassword`/`Password`/`password`，要求 0 命中 | 07 §5.2/§5.5「凭据档案 + `credentialId` 引用，报文不出现明文口令」；既有 `CreateSMBTask` 仍走明文字段（转码路径历史行为，不在本轮范围） |
| **下发 `TaskId` 复用本端任务 ID** | `wsCmd.TaskId = t.ID`（FVCS `requestTaskID` 优先采用客户端 `TaskId`），跨端日志与 B-07 进度聚合按同一 ID 对齐 | 03 §3.1「同一 checksum 重发须复用 TaskId」；B-04 幂等键与 FVCS 校验一致，复用本端 ID 避免两侧各生成一套 ID |
| **载荷/共享根本地预检，失败不外标 RUNNING** | 空载荷 → `E_PAYLOAD_MISSING`；非法 JSON → `E_EDL_INVALID`；`sourceRoot`/`destRoot` 缺失 → `E_PAYLOAD_MISSING`；共享根无法解析 → `E_SMB_MOUNT_FAILED`；FVCS 返回空 `TaskId` → `E_RENDER_FAILED`；以上均在 `dispatchRender` 内返回 error，由 B-05 `classifyRenderError` 决定冷却/终止 | 06 §4.3 可重试白名单已含 `E_SMB_MOUNT_FAILED`（配置补齐后自愈）；提交侧缺陷类（空载荷/非法 JSON/缺根）不产生网络往返，节省一次 WS 往返 |
| **GEN_PROXY 输出根留空** | GEN_PROXY 无显式输出根，`SMBOutputPath` 传空串，由 FVCS 回落到共享根 | 04 §3.5 代理文件与源文件同根；避免 FVCC 侧凭空构造输出目录 |
| **UNC 归一化：分隔符统一 + 折叠重复/首尾分隔符** | `normalizeUNC` 把 `//host/share`、`\\host\share`、带重复斜杠或尾斜杠等形式统一为 `\\host\share`；空段丢弃，全部为空则判非 UNC | 03 §3.2 只约定 UNC 形式未约定容错；实测 `//10.0.0.5/media/exports/` 需归一化后才能被 FVCS 正确挂载 |
| **共享根解析不猜映射** | `toSMBUNC` 顺序：UNC 直传 → `smbUser` 走 `smbshare.BuildSMBURL`（复用既有转码映射）→ `smbSharePath` 前缀 + 共享相对路径 → 均不可用报 `E_SMB_MOUNT_FAILED` | 09 §2 的 `/media/videos`、`/media/exports` 为共享根相对路径；映射层缺失时宁可报可重试错误，也不拼一个假 UNC |
| **task_update 500ms 聚合 + 终态直发** | `Hub.BroadcastTaskUpdateFull` 以 `taskUpdateAggInterval=500ms` 按 taskId 聚合：窗口内多次上报只暂存最新值并 `time.AfterFunc` 到期补发；内容指纹（status+progress+stage+seg+message+outTime+total+speed）与上次广播一致则不广播；线协议终态（SUCCESS/FAILED/CANCELED）**立即直发**并从聚合表移除，不受节流 | 06 §6「task_update 500ms 聚合」；终态是生命周期最后一帧，若被节流吞掉前端会永久停在中间态 |
| **node_status 20 分档 + 健康分未知不分档** | `BroadcastNodeStatus` 仅在 `status` 变化或 `healthScore/nodeHealthBandSize(20)` 档位变化时广播；`healthScoreUnknown=-1`（无指标节点的缺省值）不参与分档，仅按状态变化广播 | 06 §6「状态变化或健康分跨 20 分档广播」；若按绝对值比较，会导致每次心跳都触发广播；B-08 起健康分由 `HealthScoreOf` 传入真实值 |
| **proxy_ready 同 assetId 10s 去重** | `BroadcastProxyReady` 同 assetId 在 `proxyReadyDedupeWindow=10s` 内只广播一次，窗口内重复上报直接丢弃；顺带惰性清理过期条目（表不无限增长）；空 `assetId` 直接拒绝 | 06 §6「proxy_ready 同 assetId 10s 去重」；代理重建/多端触发会对同一 assetId 重复上报 |
| **进度落库：progress 单调 + 其余空值保持 + 已收口不覆盖** | `Store.ApplyRenderProgress`：`progress` 只增不减、越界钳位到 [0,100]；`stage`/`seg_index`/`seg_total`/`out_time_ms`/`total_ms`/`speed` 为空串或 <=0 时保持原值，有值则覆盖（**seg_index 允许被后到的低值覆盖**，与 progress 不同）；任务已完成/取消/error/暂停时整体不覆盖 | 06 §4.2 进度 loop 允许丢帧与重放；新列对旧任务须可空，避免"半量上报"把列清空；分段事件可能不带 progress，故 seg 与 progress 解耦（观察项：若后续要求分段展示也严格单调，需在 `ApplyRenderProgress` 补 seg 回退钳制） |
| **进度回传按 server_id + remote_task_id 反查，兜底本端 ID** | `Scheduler.HandleRemoteProgress` → `findProgressTask`：跳过终态/error/暂停任务；`server_id` 双方均非空且不相等则跳过；`remote_task_id` 命中优先，未命中回落到 `task_id == 本端 ID` | 依据 B-06「下发 TaskId 复用本端任务 ID」的约定；两侧 ID 同源，兜底匹配可覆盖 FVCS 未回显 remoteTaskID 的情况；06 §4.2 状态只允许正向推进 |
| **仅渲染任务走结构化进度，转码任务保持原语义** | `isRenderTask(t)` 为真才落 `stage`/`seg` 等新列并广播 `task_update` 全量字段；为假时沿用既有路径（仅接受 `(0,100]` 且不回退的 progress，仅广播 `BroadcastTaskUpdate` 精简消息） | 转码任务进度由既有 HTTP 探活推进、渲染任务由 WS 上报，两条链路的字段语义与来源不同；避免为渲染需求重构转码路径 |
| **`RemoteProgress` 取代裸 progress 回调** | `RemoteClient.OnPushProgress` 签名由 `(string, float64)` 改为 `(string, RemoteProgress)`，新增 `RemoteProgress{TaskID, Stage, SegIndex, SegTotal, Progress, OutTimeMs, TotalMs, Speed}` | 06 §6 的 task_update 载荷需 stage/seg/out_time_ms 等结构化字段，仅回传百分比无法满足；变更点集中在 `main.go` 一处装配 |
| **权限挂载范围：只挂 `DELETE` 类 + `/render`（+未来的 `/proxy`）** | 项目读写（列表/详情/新建/保存）与素材扫描/预览**不挂** `requireAdmin`，对只读用户开放 | 07 §4.2 权限分级表明确「项目读写 ✅ 只读用户（P0 简化，团队共用）」，仅「提交渲染 / 生成代理 / 删除项目与任务 / 节点与凭据管理」为 admin；07 §7 实现清单也只列 `/render`、`/proxy`、`DELETE`，故不整域收紧 |
| **403 响应对齐统一失败契约（新增 `E_FORBIDDEN`）** | `requireAdmin` 拒绝体由 `{error}` 改为 `{ok:false,code:"E_FORBIDDEN",msg}`；03 §5.3 未定义该码，实现补充 | 03 §4.1 要求 EDL 域失败统一 `{ok,code,msg,detail?}`；若保留 `{error}`，前端需为权限失败单写一套解析分支。**建议回写 03 §5.3 与 07 §4.2** |

---

## 2C. C 组逐条状态与证据（2026-09-13 核对补齐）

> 核对方式：逐 item 对照 `FVCC/ui-src/src/**` 源文件，并以 `npm run build`（`tsc -b && vite build`）实测确认可编译。当前 10/10 代码就绪、已全部装配进 `pages/editor/index.ts::renderEditor`，状态为「**代码完成（待真机联调）**」，真机项见 §4 T10~T12。

| ID | 交付 | 状态 | 证据（文件 / 符号 / 行为） |
|---|---|---|---|
| C-01 | `types.ts`/`api.ts` 扩展（EDL、Project、RenderTask、stream ticket） | 完成 | `types.ts`：`Timeline`(318)/`EDLClip`(333)/`Project`(344)/`RenderRequest`(396)/`RenderTaskView`(414)/`StreamTicket`(432)/`ProxyRequest`(443)/WS 事件（477-503）；`api.ts`：`listProjects`~`deleteProject`(187-198)、`renderProject`(199)、`createStreamTicket`/`streamUrl`(207-210)、`thumbUrl`(223)、`createProxy`(233) |
| C-02 | 路由与页面骨架（`#/editor/:projectId` + 三段布局 + 拖拽持久化） | 完成 | `main.ts:25-36` hash 路由解析 + `main.ts:176-187` 分发；`pages/editor/layout.ts`（`buildEditorLayout`）；`wve.layout.v1` 布局持久化 |
| C-03 | `editorStore.ts`：EDL 状态机 + 撤销栈（≤50 步）+ 防抖自动保存（2s） | 完成 | `editorStore.ts`：`UNDO_MAX=50`(86)/`DEBOUNCE_MS=2000`(87)、`UNDOABLE` 动作集(90)、`canUndo`/`canRedo`(71-72)、`undoStack`/`redoStack` 快照栈(149-166)、防抖保存接线(187) |
| C-04 | 素材面板（目录树 + 列表 + "需代理"角标） | 完成 | `editor/assets.ts`：`visibility` 判定（`direct`/`need_proxy`，01 §5.2 表，46-57）、缩略图/时长/分辨率/编码列、排序与本地搜索、双击/拖拽整段添加、`onRequestProxy`→`POST /api/proxy`(19-20/298-304) |
| C-05 | 预览器（`<video>` + Range 播放 + 逐帧 + 时间码显示） | 完成（待真机） | `editor/preview.ts`：`createStreamTicket`→`streamUrl` 起播(235/253)、`markRange`(149)、`seekMs`/`pendingSeek`(280-288)、±1 帧(296)、时间码显示；**依赖的 M4 路由已就绪，未真机联调（T10）** |
| C-06 | 打点（`I`/`O`、微调、帧换算、片段条生成） | 完成 | `editor/format.ts`（帧↔时间码换算）+ `editor/shortcuts.ts`（`I`/`O` 入出点、`,`/`.` 微调）+ `editor/clipOps.ts`（片段条生成/增删） |
| C-07 | 时间线（片段条渲染、拖拽排序、删除、选中态、50 片段 ≥30fps） | 完成 | `editor/timeline.ts`（`buildTimeline`、拖放/换位/删除/选中）+ `editor/ruler.ts`（`buildRuler` 时间轴映射）+ `editor/clipOps.ts` |
| C-08 | 渲染弹窗（profile 下拉 + 输出名 + 节点选择）+ 提交 | 完成 | `editor/dialog.ts`（`openRenderDialog`）+ `editor/preset.ts`（`RENDER_PRESETS`/`DEFAULT_PRESET_KEY`，与 `handlers_render.go::edlRenderPresetTable` 对齐并排除代理专用项）；提交前强制 `flushSave`，支持 `force`/`serverId`（`index.ts:337 handleRender`） |
| C-09 | 任务中心分支（进度条 + `第 x/y 段 · stage` + 失败原因 + 成品回看/下载） | 完成（待真机） | `editor/taskDrawer.ts`（`buildTaskDrawer`）+ `editor/taskView.ts`（`stageText`/`progressLabel`/`isProxyTask`/成品 URL）+ `pages/tasks.ts`；`onRequestProxy`→`POST /api/proxy` |
| C-10 | 快捷键注册表（01 §6 全表） | 完成 | `editor/shortcuts.ts`（`bindEditorShortcuts`）：Space/`I`/`O`/`←→`/`,`/`.`/`Delete`/`Backspace`/`Ctrl+S`/`Ctrl+Enter`/`Ctrl+Z`/`Ctrl+Shift+Z`/`Esc`；弹窗激活时屏蔽剪辑键（12-74） |

**校验**：`npm run build` = `tsc -b && vite build` **全绿**（30 modules transformed，产物落 `FVCC/app/ui/`；2026-09-13 实测）。
**未做**：所有依赖真机的联调/性能项（T10/T11/T12，见 §4），代码侧无遗留缺口。

## 2D. M4 逐条状态与证据（预览网关 + 代理工作流，04 号文档，2026-09-13 第九轮）

| ID | 交付 | 状态 | 证据（文件 / 符号 / 用例） |
|---|---|---|---|
| M4-A | 预览网关：票据 + Range 流式预览 + 缩略图 | 完成（代码层） | `stream.go`(795 行)/`stream_test.go`(293)；`handleStreamTicket`/`handleStreamTicketDelete`/`handleStream`/`handleThumb`；票据非一次性、TTL 300s、绑 path+root+来源 IP、单 IP 60s 内限次；`Range`/`If-Range`/`HEAD`、200/206/416；`streamLimiter` 单文件 4 / 全局 64；`_wve_cache` 缩略图与失败回落占位图 |
| M4-B | 代理工作流：提交 → 下发 → 校验 → 登记 → `proxy_ready` | 完成（代码层） | `handlers_proxy.go`(171)/`handlers_proxy_test.go`(143)/`proxy_flow.go`(262)/`proxy_flow_test.go`(275)/`store_proxy.go`(144) + `FVCS/pkg/task/proxy.go`(217)；`handleProxyRequest`、`ProxyProber` 时长/帧率/音频轨校验、`ready`/`invalid` 状态登记、`BroadcastProxyReady`（同 assetId 10s 去重）、同素材进行中复用 `taskId` |
| 路由 | `router.go` 注册 M4 四条路由 | 完成 | `POST`/`DELETE /app/fvcc/api/stream/ticket`、`GET`/`HEAD /stream`、`GET /thumb`、`POST /proxy`（写操作挂 `requireAdmin()`） |
| 用例 | 10 个 `TestM4*` | 完成 | `stream_test.go`(4：票据契约/Range 语义/IP 限流/缩略图回落) + `proxy_flow_test.go`(4) + `handlers_proxy_test.go`(2)；`go test ./... -count=1` → `ok fvcc`（2026-09-13 复验） |

**未做**：真机联调（需 SMB/UNC 素材 + ffmpeg 在环）——§4 T10/T11/T12；代理配额清理按 04 §3.6 P0 仅登记不阻塞（`E_PROXY_QUOTA_EXCEEDED` 已定义）。

## 2E. D 组逐条状态与证据（安全与凭据，08 §4.4，2026-09-13 核对补齐）

| ID | 交付 | 状态 | 证据 / 缺口（实测） |
|---|---|---|---|
| D-01 | `FVCS/pkg/protocol/edl_validate.go`（FVCS 侧白名单全套） | **完成（2026-09-14 第十一轮）** | 新增 `pkg/protocol/edl_validate.go`：导出 `ValidateRelPath`/`ValidateRelPathDecoded`/`AllowedSourceExt`/`ValidatePayloadLimits` + 内部 `validateOutputNameStrict`，本轮新增 `ValidateRootRelPath`（空值放行、非空走 L1~L3）与 `ResolveRelPath` 系列（落地 `EvalSymlinks` + 根内判定）；`pkg/protocol/edl.go::ValidateRenderEDLPayload` 已统一调用上述函数，并**补上 `sourceRoot`/`destRoot` 白名单校验**（此前只校验 clips/timeline/profile/totalMs）。共享向量 `pkg/protocol/testdata/edl_vectors.json`（15663 字节，`version=1` + `spec=07 §3.2/§3.5/§3.6/§8` + `usage` 注明两侧共用；relpath 14 / output 11 / payload 14 / symlink 1），本轮补 `PL-ROOT-TRAVERSAL`/`PL-ROOT-ABS` 两根越界用例。用例 `edl_validate_test.go`（5613 字节）：`TestEDLVectorsRelPath`/`TestEDLVectorsOutputName`/`TestEDLVectorsPayload`/`TestValidatePayloadLimits`/`TestResolveRelPath`/`TestResolveRelPathSymlinkEscape` **6 顶层 / 46 子例全绿，0 FAIL**（`ok Fnos.VC_Service/pkg/protocol 1.367s`）。**实测勘误**：`P-DOUBLE-SLASH`（`a//b.mp4`）实际返回 `E_ASSET_NOT_IN_ROOT` 而非 `E_EDL_INVALID`（L2 空段先于 L3 命中），向量期望值已按实测修正并注明 |
| D-02 | `FVCC/server/edl_validate.go`（同规则）+ `PathValidator` L1~L4 扩展 | **完成（2026-09-14 第十二轮，代码层）** | `FVCC/server/edl_validate.go` 与 FVCS 同源复刻（`validateRelPath`/`validateRelPathDecoded`/`validateOutputNameStrict`/`validateRootRelPath`/`validatePayloadLimits`/`decodeRenderPayloadStrict`/`validateRenderEDLPayload`/`edlResolveRelPath`/`edlWithinRoot`/`edlValidateStatus`），并**直接加载同一份共用向量**（`edl_validate_test.go` 指向 `../../FVCS/pkg/protocol/testdata/edl_vectors.json`，未复制、未另起）；`security.go` 新增 `ValidateRel`（L1~L3 复用同源函数 + L4 落地 `EvalSymlinks` 带边界前缀校验，拦软链越权）与 `hasAuthorizedRootFor`；`handlers_render.go` 接入三闸门（严格素材预检按 `E_EDL_INVALID`→400/`E_ASSET_NOT_IN_ROOT`→403/`E_ASSET_MISSING`→404 契约化返回、输出名白名单、载荷合规）并落地**共享根映射层** `toShareRelRoot`（以 `settings.smbSharePath` 为基准把本地素材根/成品根映射为 `videos`/`exports` 类相对根，未配置时保留本地写法并对**仅根写法**错误降级告警）；证据：`go build/vet/test ./...` 三项全绿（`ok fvcc 4.680s`）、`go test -run TestD02 -v .` **14 顶层用例全 PASS（0 FAIL / 0 SKIP）**，B-04 既有用例同批复绿。**遗留**：08 §4.4 独立评审、S1~S15 真机用例（随 E-02/E-03）、多共享根映射（当前契约仅一根） |
| D-03 | `smb/credentials.go` + `local_admin.go`（DPAPI 档案、Scope 校验、本机接口） | **完成（2026-09-14 第十三轮，代码层）** | 新增 `FVCS/pkg/smb/credentials.go`（696 行 / 21821 字节）：档案表 `credentials` 落 `secret_cipher` **密文**（`winapi.DPAPIProtect` = CryptProtectData **LocalMachine** + 每档案 entropy），`Put` 幂等创建/轮换（更新 `rotated_at`，运行中任务不受影响）、`Get`/`List` 只回 `CredentialMeta`（**永不含 secret**）、`Resolve`（Scope 校验 → DPAPI 解密 → 调用方 `Wipe()` 覆写明文）、`Delete`（被 `RUNNING` 任务引用时 `E_CREDENTIAL_IN_USE` 拒绝）、`VerifyScope`/`VerifyTaskTarget`（共享根一致或目标落 Scope；绝对 UNC 越界拒、相对路径忽略）/`ScopeContains`/`ScopeWithinShare`、`Audit`（`audit_log` 保留 **180 天** + `purgeExpiredAudit`）、`MountSMBShareWithCredential`（挂载前共享根校验，失败按 `E_CREDENTIAL_INVALID` 记审计且不回显口令）；新增 `FVCS/pkg/server/local_admin.go`（232 行）：`/local/credentials` 支持 `GET/POST/PUT/DELETE`，**非环回来源一律 403 `E_CREDENTIAL_SCOPE_DENIED`**，响应永不回显密码（含 `secretStored=dpapi`），错误码→HTTP 映射（404 `E_CREDENTIAL_NOT_FOUND` / 403 `E_CREDENTIAL_SCOPE_DENIED` / 409 `E_CREDENTIAL_IN_USE` / 400 `E_CREDENTIAL_INVALID`）；`server.go` 新增 `precheckCredentialTarget` 并在 `CreateSMBTask`/`RenderEDL`/`GenProxy` **三处创建前置拒单**；`ipc.go` 与 `cmd/service/main.go` 启动期接入 `InitCredentialAdmin()`（幂等：库已存在仅重注册占用判定，失败不阻断）。**证据**：`pkg/smb` **13 个 `TestCredential*`/辅助用例全 PASS（`ok … 5.620s`）**、`pkg/server` **7 个本机接口 + 前置校验用例全 PASS**（含非环回拒绝、环回全生命周期、占用删拒、入参拒绝、库不可用、`TestIsLoopbackRequest`）、`go build ./pkg/... ./cmd/service` + `go vet` + `go test ./pkg/... -count=1` **三项全绿（0 FAIL）**；logger 调用逐条扫描 **0 处 `username`/`password`/`secret`**。**遗留**：08 §4.4 独立评审、S1~S15 真机用例（随 E-02/E-03，需 NAS/ffmpeg 在环）、M3 迁移（`config.go` 硬编码 `encryptKey` → DPAPI 主密钥 + `fvcs-cli reencrypt`，仍未做） |
| D-04 | 安全头 + 限流 + 审计埋点；`remote.go` 停止下发凭据（M2 迁移） | **完成（2026-09-14 第十四轮，代码层）** | 「`remote.go` 停止下发凭据」已随 B-06 落地（渲染/代理下发仅带 `credentialId`，用例逐字段扫描 0 明文口令）。本轮补齐另两项：① **FVCC 侧** `ratelimit.go` 新增 `securityHeaders()`（全链路 `X-Content-Type-Options: nosniff` / `Referrer-Policy: no-referrer` / `X-Frame-Options: SAMEORIGIN`，静态资源与 4xx/5xx、SPA 兜底均带全）+ 三档限流（`authFailThrottle` 鉴权失败冷却、`renderSubmitLimit` 渲染提交、`proxySubmitLimit` 代理提交，均带 `auditReject` 埋点）；新增 `security_audit.go` 把审计挂到 `edlErr` 统一失败出口（拒绝类 `E_EDL_INVALID`/`E_PAYLOAD_INVALID`/`E_ASSET_NOT_IN_ROOT` → `validate.reject`，按来源 IP 聚合 5min 阈值 `3`/`50` 触发 `security.alert`，Detail 仅「方法 + 路径 + IP」脱敏）；② **FVCS 侧** 新增 `pkg/server/audit.go`（`auth.fail`/`ratelimit.auth`/`validate.reject`/`task.submit`/`render.submit`/`proxy.submit`/`security.alert` 七类动作落 `audit_log`，同阈值告警，审计库不可用时降级 WARN 不影响请求）。**证据**：`TestD04*` — FVCC `security.go/ratelimit.go/security_audit.go` 侧 **13 个用例全 PASS**（含安全头、鉴权失败冷却、渲染限流成功才计数、令牌滚动、限流器驱逐、WS 连接上限、拒绝记账、软链越权记账、阈值告警），`go vet ./...` + `go test ./... -count=1` 全绿（`ok fvcc 7.281s`）；FVCS `pkg/server/audit_test.go` **5 个用例全 PASS**（鉴权失败记账、限流拒绝记账、提交/拒绝记账、越权告警、库不可用降级），CGO 开启下 `go test ./... -count=1` 全绿（`pkg/protocol`/`pkg/server`/`pkg/smb`/`pkg/task` 逐包 ok）。**遗留**：08 §4.4 独立评审；S1~S15 真机安全用例随 E-02/E-03；`config.go` 硬编码 `encryptKey` 的 M3 迁移仍未做 |

**结论**：D 组 **4/4（代码层全部收口）**——D-01（FVCS 侧白名单 + 共享向量 + 单测全绿）、D-02（FVCC 侧同源复刻 + `PathValidator` L1~L4 + 渲染提交接入 + 共享根映射层，14 个 `TestD02*` 全绿）、D-03（DPAPI 密文档案 + Scope/目标校验 + 环回本机接口 + 三处创建前置拒单，`pkg/smb` 13 用例 + `pkg/server` 7 用例全绿）与 **D-04（安全响应头 + 三档通用限流 + 双侧审计/阈值告警埋点，FVCC 13 用例 + FVCS 5 用例全绿）** 均已完成。08 §4.4 注明 D-01/D-02 需独立评审人——**规则与向量已落地，建议 D-02 开工前由评审人对齐 07 §3.2/§3.5/§3.6/§8 与向量期望值**（已发现 `P-DOUBLE-SLASH` 一例期望需按 L2 优先级修正）。

---

## 3. 本轮（2026-09-10）改动明细

| 文件 | 类型 | 改动 |
|---|---|---|
| `FVCS/pkg/ffmpeg/edl_test.go` | 新增 | 覆盖 T1 同源 copy_all（3+1 命令/阶段序列/清单 3 行）、T2 转码+NVENC 降级、T3 混源 filter_complex、T4 缺音轨补静音、T7 素材缺失可定位、载荷闸门（非法 presetKey / 越界出点）、A-03 清单转义、A-03 40 段分批；全部用构造 `MediaInfo` 作 probes，不依赖真实 ffmpeg/媒体文件 |
| `FVCS/pkg/ffmpeg/edl.go` | 修复 | `silentFill` 补静音片段改用 `audioEncodeArgs(p, true)`，补上 **`-c:a aac`**（原先只给 `-b:a/-ar/-ac`，lavfi 裸 PCM 入 mp4 不可控） |
| `FVCS/pkg/ffmpeg/ffmpeg.go` | 修复 | 3 处 `logger.Error` 拼接参数→占位符 |
| `FVCS/pkg/task/{proxy,render_edl,task}.go` | 修复 | 共 31 处 `logger.*` 拼接参数→占位符 |
| `FVCS/pkg/server/server.go` | 修复 | 7 处 `logger.*` 拼接参数→占位符 |

校验结果：`go vet ./pkg/...`、`go build ./pkg/...`、`go test ./pkg/... -count=1` 三项**全绿**。

### 2026-09-10 第二轮（B 组：B-02 契约对齐收尾 + B-03）

| 文件 | 类型 | 改动 |
|---|---|---|
| `FVCC/server/handlers_edl.go` | 新增 | 项目 5 个 handler + 统一失败响应 `{ok,code,msg,detail}` + 契约级闸门（名称、timeline 范围、clips 数量/时长关系/出点超素材/speed/transition、file 的 L1/L2 结构），体积上限 256 KB |
| `FVCC/server/handlers_edl_test.go` | 新增 | CRUD 契约（`clips:[]`、`clipCount`/`totalMs` 可见、409 `E_REV_CONFLICT` 带 `serverRev`/`clientRev`、重名 409、缺 rev 400、删除 404）+ 13 条校验用例 + 上限用例 |
| `FVCC/server/router.go` | 修改 | 注册 `/api/edl/projects`（GET/POST）与 `/api/edl/projects/:id`（GET/PUT/DELETE），标注 `TODO(B-09)`（权限中间件）与 `TODO(B-04)`（render） |
| `FVCC/server/models.go` | 修改 | `Project` 的 `clipCount`/`totalMs` 对 API 可见（去掉 `json:"-"`）；`schemaVer`/`lastRenderTaskId` 改为可落盘 |
| `FVCC/server/store_edl.go` | 修改 | `persistProjects` 写盘前清零派生字段（03 §2.2） |
| `FVCC/server/store.go` | 修改 | `Load` 后对每个项目 `normalizeProject` 重算派生字段并补 `schemaVer=1` |

校验结果：`go build ./...`、`go vet ./...`、`go test ./...`（`fvcc` 包）三项**全绿**。

> 勘误：上一版此处误附一行 FVCS 侧校验结果（`./pkg/...`），与本节（FVCC 第二轮）无关，本次删除。

### 2026-09-11 第三轮（B 组：B-04 渲染提交）

| 文件 | 类型 | 改动 |
|---|---|---|
| `FVCC/server/handlers_render.go` | 新增 | `renderEDLProject`（POST `/edl/projects/:id/render`）主体 + 辅助：`bindRenderSubmit`/`edlRenderPresetTable`（presetKey 枚举，含 `proxyOnly` 拒绝）、`normalizeOutputBase`/`isReservedOutputBase`/`uniqueOutputName`（补 `.mp4`、最多 99 次 `_1`/`_2` 追加、Windows 保留名拒绝）、`resolveEDLRoots`/`toPOSIXRoot`（`Settings.sourceRoot`/`destRoot` 缺省，本地绝对路径语义）、`precheckAssets`/`pathWithin`/`fileExistsIn`/`isDir`（本地可见才预检，否则降级）、`outputArtifactExists`（成品仍在才复用）、`buildRenderPayload`、`computeFastCopyAllowed`、`computeRenderChecksum`（sha256 前 16 位，与 FVCS 同形）、`msToTimecode`/`parseTimecode`、`newRenderTaskID`、`renderSubmitBody`；失败统一走 B-03 的 `edlErr` 契约 `{ok,code,msg,detail}` |
| `FVCC/server/handlers_render_test.go` | 新增 | 9 个用例（含 27 个子例）：提交响应契约（taskId/status/serverId/output/totalMs/fastCopyAllowed）、`totalMs=Σ(outMs−inMs)` 与 `fastCopyAllowed` 三种变体、幂等复用（活动任务 / 成品仍在的 SUCCESS 任务 / `force:true` 绕过）、checksum 稳定性与作用域（output/FastCopyAllowed 不影响）、输出名规范化 17 子例、重名 `_1`/`_2` 追加、错误契约 10 子例（404/400/409）、指定节点固定；复用 `newEDLTestRouter`/`doEDLRequest`，并用 `t.TempDir()` 注入本地可见素材根以覆盖预检分支 |
| `FVCC/server/handlers_render.go` | 修复 | `reOutputBase` 首字符类补 `\x{4e00}-\x{9fa5}`：原正则首字符仅允许 ASCII，导致「成片_第一版」等纯中文输出名被 400 拒绝，与 07 §3.5 `\p{Han}` 首字符口径及函数自身提示语不符 |

校验结果：`build ./...`、`vet ./...`、`test ./...` 三项**全绿**（`ok fvcc 1.582s`；仅跑 B-04：`-run TestB04` → 9 用例 / 36 项含子例全 PASS）。
### 2026-09-11 第四轮（B 组：B-05 调度分流与冷却）

| 文件 | 类型 | 改动 |
|---|---|---|
| `FVCC/server/scheduler.go` | 修改 | ① 新增 `RenderDispatcher` 接口 + `Scheduler.dispatcher` 字段 + `SetRenderDispatcher`（下发通道留待 B-06 实现注入）；② `processTask`：`StatusQueue` 按 `TaskType` 分流三路、`StatusCooldown` 显式 `return`、渲染任务的 `StatusTranscoding` 直接返回（不走 `checkTranscodeProgress`）；③ `tick`：新增冷却到期扫描 `promoteCooldownTasks`，QUEUE 选取从「双循环冒泡 + 内联判断」改为 `sortQueueTasks` + `nextQueueTask`（键 = 优先级档位 + `OrderID`，每 tick 只推进 1 个，`processing` 去重语义不变）；④ 新增 `handleRenderEDL`/`handleGenProxy`/`dispatchRenderLike`/`resolveRenderNode`/`classifyRenderError`/`failRenderTaskWithCooldown`/`failRenderTaskPermanent`/`promoteCooldownTasks`/`isRenderTask`/`taskPriorityRank`/`lessQueueTask`/`sortQueueTasks`/`nextQueueTask` 及退避/锁/错误码常量 |
| `FVCC/server/scheduler_edl_test.go` | 新增 | 6 个用例覆盖：派发成功（RUNNING + `server_id`/`remoteTaskId`/`stage=prepare` + 清空 `next_retry_at` + 双锁释放）、GEN_PROXY 派发与低优（代理 OrderID 最小仍排最后、同档位按 OrderID）、退避全序列（30/60/120/240/480s + `attempt`/`cooldown_reason`/`OrderID` 不变 + 未到期不提升 + 到期→QUEUE + 超限→FAILED 并 `handleError` 归档）、错误码白名单（4 个不可重试码直接失败且不计重试、4 组可重试归一）、节点闸门（离线→冷却 30s、不存在→不可重试失败、未指定节点→保持 QUEUE）、侧门（空载荷→FAILED、通道未注入→保持 QUEUE、`processTask` 对 COOLDOWN/RUNNING 空转） |

校验结果：`build ./...`、`vet ./...`、`test ./... -count=1` 三项**全绿**（`ok fvcc 1.827s`；仅跑 B-05：`-run TestB05 -v` → 6 用例全 PASS，日志实测退避 30→60→120→240→480 后 FAILED）。
工具链：`C:\Users\xx318\sdk\go1.25.3\go\bin\go.exe` + `GOTOOLCHAIN=local`（`D:\fntv-tools\go\bin\go.exe` 本机不存在）。
B-05 未做（明确留给后续）：`remote.go` 真实下发（B-06，`main.go` 尚未 `SetRenderDispatcher`）、`GEN_PROXY` 的 freeSlots 扣减（B-08）、WS 进度聚合（B-07）。

### 2026-09-11 第五轮（B 组：B-06 渲染下发 + 装配）

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/server/remote.go` | 修改 | ① `var _ RenderDispatcher = (*RemoteClient)(nil)` 编译期断言；② `CreateRenderEDL`/`CreateGenProxy` → 共用 `dispatchRender`；③ 新增 `SetSettingsProvider`/`settings()`/`renderPriorityLevel`/`resolveRenderSharePaths`/`toSMBUNC`/`normalizeUNC`/`renderShareDirs` 与优先级常量；④ `RemoteClient` 增加 `getSettings` 字段；⑤ `wsCmd` 增加 `TaskType`/`PriorityLevel`/`Payload`/`SMBOutputPath`/`CredentialID`（**不含** `SMBUser`/`SMBPassword`）；⑥ import 补 `path/filepath`、`strings`、`fvcc/smbshare` |
| `FVCC/server/main.go` | 修改 | `NewScheduler` 之后装配：`remote.SetSettingsProvider(store.GetSettings)` + `scheduler.SetRenderDispatcher(remote)` |
| `FVCC/server/remote_edl_test.go` | 新增 | 4 用例 / 7 子例（359 行），含假 WS 服务端回放 `createTaskData`、`rawStrOpt` 辅助函数 |

下发链路（06 §4.2）：`Scheduler.dispatchRenderLike` → `CreateRenderEDL`/`CreateGenProxy` → 载荷非空 + `json.Valid` 校验（否则 `E_PAYLOAD_MISSING`/`E_EDL_INVALID` 本地拦截，不产生网络往返）→ `resolveRenderSharePaths`（RENDER_EDL 取载荷 `sourceRoot`/`destRoot`；GEN_PROXY 取 `settings.smbSharePath` 作素材根、输出根留空由 FVCS 回落）→ `toSMBUNC` 四段解析 → `wsCmd` 经 `roundTrip` 60s 下发 → 解析 `resp.Data.TaskId` 返回。

**安全红线（07 §5.2/§5.5）**：渲染下发报文只带 `credentialId`，**不出现** `SMBUser`/`SMBPassword`（用例逐字段扫描 `SMBUser`/`SMBPassword`/`smbUser`/`smbPassword`/`Password`/`password` 全字段，确保 0 命中），凭据由 FVCS 侧按 `credentialId` 解析；同时断言渲染报文不携带 `FFmpegArgs`/`SourceFileName`/`OutputName`（转码路径专用字段）。

校验结果：`build ./...`、`vet ./...`、`test ./...` 三项**全绿**（`ok fvcc 2.003s`，全量 30 个顶层用例 0 FAIL；仅跑 B-06：`-run TestB06 -v` → 4 用例 / 7 子例全 PASS：`TestB06RenderDispatchWireContract`（RENDER_EDL 常规优先级 / GEN_PROXY 低优先级）、`TestB06SharePathResolution`（UNC 直传 / 斜杠 UNC 归一化 / 共享相对路径拼接 / 多级拼接 / 带前导斜杠 / 无配置报 `E_SMB_MOUNT_FAILED`）、`TestB06RenderDispatchPayloadGuards`、`TestB06RenderDispatchSurfacesFVCSErrors`）。
工具链：`C:\Users\xx318\sdk\go1.25.3\go\bin\go.exe` + `GOTOOLCHAIN=local`。
B-06 已解除的开关：`main.go` 已注入 `RenderDispatcher` → 未指定 `serverId` 时仍保持 QUEUE（选机属 B-08），一旦 `serverId` 指向在线节点即可真实下发。
B-06 未做（留给后续）：`GEN_PROXY` 的 freeSlots 扣减（B-08）、WS 进度聚合 `node_status`/`proxy_ready`（B-07）、`credentialId` 档案解析真机联调（D 组）。

---

### 2026-09-11 第六轮（B 组：B-07 WS 事件聚合与进度回传）

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/server/ws.go` | 修改 | 新增 B-07 段：`SegInfo`/`TaskUpdateFullMsg`/`ProxyReadyMsg`/`NodeStatusMsg` + 常量 `taskUpdateAggInterval`/`proxyReadyDedupeWindow`/`nodeHealthBandSize`/`healthScoreUnknown`；`Hub` 增加 `agg`/`proxySeen`/`nodeState` 与 `clock`/`emitHook`；`BroadcastTaskUpdateFull`、`BroadcastProxyReady`、`BroadcastNodeStatus` 三个广播入口（440 行） |
| `FVCC/server/scheduler.go` | 修改 | 新增 `HandleRemoteProgress`（line 855）、`findProgressTask`（894）、`renderSegInfo`（914）、`renderProgressLabel`（922）、`BroadcastNodeStatus`（945），并复用 `isRenderTask`（724）区分渲染/转码两条进度路径 |
| `FVCC/server/store_edl.go` | 修改 | 新增 `RenderProgress`（line 125）+ `ApplyRenderProgress`（144）：单调不回退、空值保持、终态不覆盖 |
| `FVCC/server/remote.go` | 修改 | `OnPushProgress` 签名改为 `func(serverID string, p RemoteProgress)`（line 28），新增 `RemoteProgress` 结构（line 94），WS `Progress` 帧解析后回传结构化字段（line 237） |
| `FVCC/server/main.go` | 修改 | `OnPushProgress` → `scheduler.HandleRemoteProgress`（line 165）；`OnDisconnect` → `node_status offline` 广播（line 172） |
| `FVCC/server/handlers.go` | 修改 | 节点连通性测试成功/失败分别广播 `node_status online`/`offline`（line 697/702） |
| `FVCC/server/ws_edl_test.go` | 新增 | 4 用例：500ms 聚合（窗口内不追加 + 到期补发最新值 + 终态立即广播）、防护（进度未变不广播 / 越界钳位 / 空 taskId 拒绝）、proxy_ready 10s 去重（窗口内丢弃 + 到期重发 + 空 assetId 拒绝）、node_status 分档（跨 20 分档广播 / 状态 online↔offline 变化广播 / 健康分 -1 仅按状态） |
| `FVCC/server/ws_progress_edl_test.go` | 新增 | 3 用例：`ApplyRenderProgress` 落库与重载（含启动崩溃恢复语义断言）、`HandleRemoteProgress` 渲染链路（反查 → 落库 → 广播 stage/seg，且进度不回退不重复广播）、转码链路保持原语义 |

回传闭环（06 §4.2 / §6）：FVCS `Progress` 帧 → `remote.go` 解析为 `RemoteProgress` → `main.go` 转交 `scheduler.HandleRemoteProgress` → `findProgressTask` 按 `server_id`+`remote_task_id`（兜底本端 ID）反查 → `Store.ApplyRenderProgress` 落库（单调/空值保持）→ `renderSegInfo`/`renderProgressLabel` 生成「第 x/y 段 · stage」文案 → `Hub.BroadcastTaskUpdateFull` 500ms 聚合后广播前端。

**聚合与去重口径**：`task_update` 同任务 500ms 内只广播最新值，内容未变不广播，终态立即直发；`proxy_ready` 同 assetId 10s 去重；`node_status` 仅状态变化或健康分跨 20 分档广播（B-08 落地前健康分统一 `-1`，等价于"仅按状态变化"）。

校验结果：`build ./...`、`vet ./...`、`test ./... -count=1` 三项**全绿**（`ok fvcc 3.628s`）；仅跑 B-07：`-run TestB07 -v` → 7 用例全 PASS（`TestB07TaskUpdateFullAggregation` / `TestB07TaskUpdateFullGuards` / `TestB07ProxyReadyDedupe` / `TestB07NodeStatusBroadcast` / `TestB07ApplyRenderProgressStore` / `TestB07HandleRemoteProgressRender` / `TestB07HandleRemoteProgressTranscode`，`ok fvcc 1.764s`）；`gofmt -l ws.go ws_edl_test.go ws_progress_edl_test.go` 无输出（格式干净）。
工具链：`C:\Users\xx318\sdk\go1.25.3\go\bin\go.exe` + `GOTOOLCHAIN=local`（无 CGO、未用 `-race`）。
B-07 已解除的开关：`ws.go` 事件聚合落地后，B-05 渲染任务「RUNNING 阶段不由 HTTP 探活推进」的空转现状有了真实的进度来源；`node_status` 也已接入 WS 断连与连通性测试两条触发路径。
B-07 未做（留给后续）：健康分实际取值与熔断打分（B-08）、`GEN_PROXY` 的 freeSlots 扣减（B-08）、前端消费 `task_update` 的 stage/seg 文案渲染（C 组）、真机 FVCS 在环的 WS 端到端联调（需 ffmpeg 在环）。

---

### 2026-09-11 第七轮（B 组：B-08 选机 / 健康分 / 熔断 / Hello 能力落库）

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/server/node_select.go` | 新增（455 行） | B-08 全量：`nodeMetrics` 内存指标窗口 + `NoteNodeAttempt`/`NoteNodeFailure`/`NoteNodeSuccess`/`nodeCircuitOpen`；`healthScoreAt`/`HealthScoreOf`（clamp[0,100]）；`NodeFreeSlots`/`runningCount`/`nodeMaxConcurrent`；`requiredEncoder`/`capabilityMatch`；`agentVersionAtLeast`/`leadingInt`/`nodeVersionOK`；`nodeSelectable`/`onlineNodes`/`earliestFreeNode`；`pickNode`（显式优先 + 打分降级 + 全忙退化）；`ApplyNodeHello`（落库 + 真实健康分广播） |
| `FVCC/server/scheduler.go` | 修改 | `resolveRenderNode`（line 958）改为调 `pickNode`（取代 B-05 的「仅显式 serverId」实现）；`dispatchRenderLike` 派发前 `NoteNodeAttempt`（line 817）、失败 `NoteNodeFailure`、成功 `NoteNodeSuccess` |
| `FVCC/server/remote.go` | 修改 | 新增 `OnPushHello`（line 30）+ `helloPush`（line 124）+ `parseHelloCaps`（line 140，camel/snake 双命名逐字段合并 + 嵌套 `caps` 兼容 + 载荷缺失以连接身份兜底）；`readPump` 在 `Cmd=="Hello"` 分支触发回传（line 325） |
| `FVCC/server/main.go` | 修改 | 装配 `remote.OnPushHello` → `scheduler.ApplyNodeHello`；`OnDisconnect` 离线广播由 `healthScoreUnknown` 改为 `scheduler.HealthScoreOf(serverID)` |
| `FVCC/server/node_select_test.go` | 新增（403 行） | 10 用例：显式优先（不存在/离线/满槽排队）、自动选机（空闲槽 / 健康分 / 能力匹配）、全忙退化 `earliestFreeNode`、健康分公式（含多项叠加与 clamp）、熔断（3 次失败开启 / 熔断期不可选 / 到期后探测放行 / 成功恢复）、`ApplyNodeHello` 落库 + 广播、`parseHelloCaps` 双命名与嵌套、自动化选机后真实下发 |

关键口径（06 §5.1~§5.4 落地细节）：

- **选机**：显式 `serverId` 一律不改判（离线/版本不符 → `E_NODE_OFFLINE` 可重试；满槽 → 返回该节点由传输/编码锁排队）；未指定时打分 `0.5×freeSlots/maxConcurrent×100 + 0.3×健康分 + 0.2×能力匹配`，全忙退化为「已占用槽最少」节点排队，无任何候选则保持 QUEUE。
- **健康分**：`100 − 40×近 1h 失败率 − 15×近 24h 有卡死 − 10×近 1h 挂载失败≥2 − 20×离线`，clamp[0,100]；实测组合样例：1 次失败 + 1 次卡死 → 61，叠加挂载失败×2 → 35。
- **熔断**：同节点 5 分钟内 3 次失败 → 熔断 10 分钟；到期后置 `probing` 放行 1 个探测任务，`NoteNodeSuccess` 清空失败/卡死/挂载失败窗口并解熔断。
- **能力落库**：`Hello` 上报 → `UpsertNodeCaps` → 按真实健康分 `BroadcastNodeStatus`（20 分档生效）；Agent 低于 `0.2.0` 不参与渲染选机并在广播 reason 中标注。

校验结果：`build ./...`、`vet ./...`、`test ./... -count=1` 三项**全绿**（`ok fvcc 3.403s`，47 个用例全 PASS，含 B-08 的 10 个）；仅跑 B-08：`-run TestB08 -v` → 10 用例全 PASS。
工具链：`C:\Users\xx318\sdk\go1.25.3\go\bin\go.exe` + `GOTOOLCHAIN=local`（无 CGO、未用 `-race`）。
B-08 已解除的开关：B-05 的 `GEN_PROXY` freeSlots 扣减（`proxySlotCost`）、B-07 的 `healthScore` 真实取值（`OnDisconnect` / `ApplyNodeHello` / 连通性测试三处广播均带真实分）。
B-08 未做（留给后续）：`Server` 模型未新增 `Maintenance`/`CircuitUntil` 字段（熔断仅存内存，重启即清）；健康分样本未按 06 §2.1 落 `node_health_samples`（仅内存窗口）；真机 FVCS 在环的 `Hello` 上报联调（需真实节点建链）。

---

### 2026-09-12 第八轮（B 组：B-09 权限中间件挂载）

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/server/router.go` | 修改 | ① 7 条 `DELETE` 路由挂 `requireAdmin()`（`/log`、`/video-cache`、`/servers/:id`、`/profiles/:id`、`/tasks/:id`、`/history/:id`、`/edl/projects/:id`）；② `POST /edl/projects/:id/render` 挂 `requireAdmin()`；③ 移除 `TODO(B-09)`，改为「删除类与提交渲染挂 requireAdmin（07 §4.2、§7）；项目读写对只读用户开放；/proxy 路由落地时同步挂载」 |
| `FVCC/server/gateway.go` | 修改 | 新增 `const errCodeForbidden = "E_FORBIDDEN"`；`requireAdmin` 拒绝响应由 `{error:"需要管理员权限"}` 改为 03 §4.1 统一失败契约 `{"ok":false,"code":"E_FORBIDDEN","msg":"需要管理员权限"}`（HTTP 403 与「无网关头放行」语义不变） |
| `FVCC/server/router_auth_test.go` | 新增 | 3 用例：`TestB09RenderAndDeleteRequireAdmin`（非 admin 调 `/render` → 403 `E_FORBIDDEN`；admin/root 放行；7 条 DELETE 路由非 admin 403 / admin 放行）、`TestB09ProjectReadWriteOpenToReadonlyUser`（只读用户 新建/列表/详情/保存 全放行，删除 403）、`TestB09StandaloneModeBypassesAdmin`（无网关头 `/render` 与 `DELETE /log` 均放行） |
| `FVCC/server/remote.go` | 修复 | `gofmt -w`：`OnPushHello` 字段与其他回调对齐（B-08 遗留格式差异，无语义变更） |

权限挂载口径（07 §4.2 / §7、08 §4.2）：

- **仅 admin**：提交渲染 `POST /edl/projects/:id/render`、生成代理 `/proxy`（**2026-09-13 已落地并挂载**，见 §3 第九轮）、全部 `DELETE` 类（删除项目/任务/历史/节点/方案，清日志、清缓存）；
- **只读用户可用（不挂权限）**：素材扫描/预览、项目读写（列表/详情/新建/保存）——按 07 §4.2「P0 简化，团队共用」；
- **独立模式（无 `X-Trim-*` 网关身份）放行**，与改造前行为一致（07 §4.2 注释：P0 单一共享密钥场景视所有登录者为 admin，但**接口权限位必须已就位**）。

校验结果：`gofmt -l .` 无输出（`remote.go` 已随本轮格式化）、`build ./...`、`vet ./...`、`test ./... -count=1` 三项**全绿**（`ok fvcc 3.749s`）；仅跑 B-09：`-run TestB09 -v` → 3 用例全 PASS（`ok fvcc 0.159s`）。
工具链：`C:\Users\xx318\sdk\go1.25.3\go\bin\go.exe` + `GOTOOLCHAIN=local`。
B-09 已解除的开关：`router.go` 内 `TODO(B-09)` 清零，08 §4.2 验收口径「未授权调用 `/render` 返回 403」由 `TestB09RenderAndDeleteRequireAdmin` 覆盖；**B 组 9/9 全部完成**。
B-09 未做（留给后续）：FNOS 网关真机透传 `X-Trim-User-Role` 联调（需网关在环）；`/proxy`（GenProxy 提交）路由落地时同步挂 `requireAdmin` —— **已于 2026-09-13 第九轮落地**（见下）；07 §7 的「审计埋点」随 D 组统一处理。

---

### 2026-09-13 第九轮（M4：预览网关 + 代理工作流，解除 C 组联调阻塞）

M4 对应 `04-预览网关与代理工作流设计.md`：M4-A 预览网关（票据 + Range 流式预览 + 缩略图）、M4-B 代理工作流（提交 → 下发 → 校验 → 登记 → `proxy_ready`）。

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/server/stream.go` | 新增（795 行） | **M4-A**：① `POST /stream/ticket` 签发 `tk_<32hex>` 票据，**短时复用而非一次性**（04 §2.3 明确澄清：`<video>` 同一文件会发多次 Range 请求），TTL 300s、绑定 `path`+`root`+来源 IP、单 IP 60s ≤60 个（超出 429 `E_STREAM_BUSY`）、`DELETE /stream/ticket` 可撤销（query 或 JSON body）；② `GET/HEAD /stream` 校验票据后按 `Range` 流式返回（`a-b`/`a-`/`-n` 三种形态、非法区间 416、`If-Range` 命中 ETag 才认、非 Range 全量 200 且带 `Accept-Ranges`/`ETag`/`Last-Modified`），全局并行 ≤64（满则 429），只读方法闸门（其余 405）；③ `GET /thumb` 抽帧 320×180 JPEG（`-ss` 前置 + `-frames:v 1`），缓存键含 root+rel+时间点+mtime+size 落 `_wve_cache`，ffmpeg 缺失/抽帧失败回落内置 1×1 透明 JPEG 占位且不污染缓存 |
| `FVCC/server/proxy_flow.go` | 新增（262 行） | **M4-B 收尾**：`SetProxyProbe` + `ProxyProber`（`Probe`/`Available`）注入；`pollGenProxy` 按窗口限流轮询 FVCS 终态；`finishGenProxy` 成功后 ffprobe 比对源/代理（时长容差、帧率、音频轨）→ `UpsertAssetProxy(state=ready)` → `BroadcastProxyReady`（沿用 B-07 同 assetId 10s 去重）；校验失败 `markProxyInvalid`（`state=invalid` + 删残件 + `E_PROXY_VERIFY_FAILED`） |
| `FVCC/server/handlers_proxy.go` | 新增（171 行） | `POST /api/proxy` 提交侧：body ≤256 KB、`file` 复用 `validateClipFile` 结构闸门、`assetId` 1~64、仅允许 `root=src`；同 `file` 已有 QUEUE/RUNNING 代理任务 → **200 返回既有 `taskId`**（`deduplicated=true`，04 §3.5）；源素材不存在 → 404 `E_ASSET_MISSING`；入队 `TaskTypeGenProxy`（`GenProxyPayload{type,assetId,srcFile,proxyFile,template}`），响应 `{taskId,status,proxyFile}`，写审计 `proxy.submit` |
| `FVCC/server/store_proxy.go` | 新增（144 行） | `asset_proxies.json` 持久化：`GetAssetProxies`/`GetAssetProxy`/`UpsertAssetProxy`/`MarkAssetProxyState`/`TouchAssetProxy`/`DeleteAssetProxy`/`FindActiveProxyTask`；写盘风格对齐 `store_edl.go` |
| `FVCC/server/security.go` | 修改 | 三根解析 `resolveMediaRoots`/`resolveMediaRootsFor`（src=`videoRoot`；proxy=`settings.proxyRoot` 或 `<videoRoot>/_proxy`；dest=输出根；cache=`settings.cacheRoot` 或 `<videoRoot>/_wve_cache`）+ `ResolveRoot(root)` 供三条网关路由复用 + `localPathOf` POSIX↔本地换算 |
| `FVCC/server/handlers.go` | 修改 | ① 素材扫描过滤保留名：`isReservedMediaName` 判 `_` 前缀目录/文件（`_proxy`/`_wve_cache`/`_exports` 不进素材库，04 §2.4）；② `Handlers.probe` 由 `*FFprobe` 抽象为 `MediaProber` 接口（`Probe`+`Available`），便于测试注入 |
| `FVCC/server/router.go` | 修改 | 注册 4 条 M4 路由：`POST/DELETE /stream/ticket`、`GET/HEAD /stream`、`GET /thumb`（只读，不挂权限）、`POST /proxy`（写操作，**挂 `requireAdmin()`**，补齐 B-09 遗留的挂载口径） |
| `FVCC/server/models.go` | 修改 | `Settings.ProxyRoot`/`CacheRoot`（可选，缺省回落 `<videoRoot>/_proxy`、`<videoRoot>/_wve_cache`）；`AssetProxy` + `AssetProxiesFile`（`asset_proxies.json`，version=1）；`ProxyModeFull`/`ProxyModeKeyframeOnly`；`GenProxyPayload`（与 FVCS 同形）；`defaultProxyTemplate()` |
| `FVCC/server/store.go` | 修改 | `assetProxies` 纳入 Store 并在 `Load` 装载（`loaded: ... proxies=%d`） |
| `FVCC/server/scheduler.go` | 修改 | `ProxyProber` 接口字段 + `proxyPollAt` 轮询窗口；RUNNING 的 `GEN_PROXY` 在进度推进后追加 `pollGenProxy` 收尾钩子（B-05 分流/低优/冷却逻辑不变） |
| `FVCS/pkg/task/proxy.go` | 新增（217 行） | FVCS 侧 `CreateGenProxyTask`（代理载荷严格校验 → 低优任务 → SMB 直读，无分片上传）+ `executeGenProxy`（挂载共享 → 构造代理命令 → 执行 → 代理落盘 → 状态回传）；`server.go` 的 `CmdCreateGenProxy` 分支已接线 |
| 用例 | 新增 | 10 个 `TestM4*`：`stream_test.go`(4) 票据契约 / Range 语义（含 416、If-Range）/ 单 IP 限流 / 缩略图回落与非授权守门；`proxy_flow_test.go`(4) 时长帧率校验 / `finishGenProxy` 登记 ready / invalid 标记 / 无远端不误判；`handlers_proxy_test.go`(2) `/proxy` 提交契约与同素材去重 / `_` 前缀保留目录过滤 |

**收尾修复（本轮末尾）**：`Handlers.probe` 抽象为 `MediaProber` 接口后，`proxy_flow_test.go` 的 `stubProber` 需补 `Available() bool`；并新增 `enrichStubInfo`，让桩按真实 ffprobe 同构回填 `Path/FileName/Format/Size`，否则 `TestM4ScanDirectoryFiltersReservedDirs` 因扫描结果文件名为空而失配（对应 C 组素材列表依赖文件名）。

**契约口径（供前端与联调对齐）**

- 票据：`POST /api/stream/ticket {path,root}` → `{ticket,expiresAt}`；`DELETE /api/stream/ticket?ticket=` → `{ok}`；票据非一次性、TTL 300s、绑定 path+root+IP。
- 预览：`GET /api/stream?path=&root=&ticket=`，支持 `Range`/`If-Range`/`HEAD`，并发上限 64，越权 403、区间非法 416。
- 缩略图：`GET /api/thumb?path=&root=&t=`，失败回落占位图（前端无需处理错误分支）。
- 代理：`POST /api/proxy {assetId,file,root?}` → `{taskId,status,proxyFile}`；`proxyFile` 为**相对 ProxyRoot** 的 POSIX 路径（`demo/a_01.mp4` → `demo/a_01.proxy.mp4`）；同素材进行中任务返回既有 `taskId`。

**校验结果**：FVCC `gofmt -l .` 无输出、`go build ./...`、`go vet ./...` 通过、`go test ./... -count=1` **全绿**（`ok fvcc 4.42s`）；FVCS `go build ./pkg/...`、`go vet ./pkg/task/... ./pkg/ffmpeg/...` 通过、`pkg/ffmpeg` 用例全绿（`pkg/task` 无测试文件）。
**工具链**：`C:\Users\xx318\sdk\go1.25.3\go\bin\go.exe` + `GOTOOLCHAIN=local`。

**M4 未做（留给后续）**：真机联调（需 SMB/UNC 素材 + ffmpeg 在环）：Range 拖拽起播、缩略图实际抽帧、代理端到端下发与 `E_PROXY_VERIFY_FAILED` 判定、`proxy_ready` WS 推送（见 §4 T10~T12）；代理配额清理按 04 §3.6 P0 仅登记不阻塞（`E_PROXY_QUOTA_EXCEEDED` 已定义）；D 组安全/凭据仍未开工。

### 2026-09-13 第十轮（A 组：A-06 断点续跑 + workDir 复用/孤儿清理 + `pkg/task` 单测）

A-06 缺口收口（06 §3.3 要点 1/3、§3.4 启动恢复；08 §4.1 A-06；05 §4.5）。

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCS/pkg/task/task.go` | 修改 | ① `Task` 新增 `SegDone`(79)/`SegTotal`/`ResumePending`(90) 三字段；② `createTables` 与 `addColumnIfNotExists` 增 `seg_done`/`seg_total` 两列（`INTEGER DEFAULT 0`，旧库自动迁移）；③ `saveTaskToDB` 的 INSERT/UPDATE 增两列参数，`loadTasksFromDB` 以 `COALESCE(seg_done,0)`/`COALESCE(seg_total,0)` 读回；④ 启动恢复：`StatusTranscoding` 分支改调 `recoverRuntimeTask`(365) —— 渲染类（`RENDER_EDL`/`GEN_PROXY`，`isRenderTaskType` 379）置 `Waiting` + `ResumePending=true`（重启后可续跑），传统转码仅置 `Waiting`（与既有实现一致）；⑤ 新增 `effectiveResumeSegDone`(393，`ResumePending` 为假或 `SegDone<=0` 时返回 0)、`mergeRenderRunInfo`(411，进度/阶段/SegDone 单调合并)、`planOrphanWorkDirCleanup`(459，纯函数：按 mtime 倒序保留最近 3 个目录、删除早于 7 天且无对应任务的目录)、`cleanupOrphanWorkDirs`(482，扫描 `config.TempDir` 并排除活跃任务目录后调用 plan 删除)；⑥ `startGCTimer` tick 与 `Init`（load 之后）各接入一次 `cleanupOrphanWorkDirs`；⑦ `cleanupTempFiles`(572) 增「跳过活跃任务目录」判定，避免全局清理误删续跑锚点 |
| `FVCS/pkg/ffmpeg/edl_exec.go` | 修改 | ① `StageRunInfo` 增 `SegDone`(35) 字段（已完成片段数，供落库）；② 新增 `RunRenderPlanResume`(53)：`plan` 准备后按 `resumeSegDone` **跳过前 N 个 segment 阶段**（只上报进度不重跑），其余阶段照旧；③ 新增 `ResumableSegmentPrefix`(174)：逐个校验前 `want` 段产物（`segmentArtifactComplete` 219：存在 + 普通文件 + 非空）确定真实可信前缀，**异常回退 + 清理断点起（含断点）的全部残留产物**（半成品参与拼接会污染成品），返回 `[0, want]` 的可信段数；`want<=0` 时零校验零清理（等价整任务重跑）；④ 原 `RunRenderPlan`(44) 改为 `RunRenderPlanResume(..., 0, ...)` 的薄封装，既有调用方语义零变更 |
| `FVCS/pkg/task/render_edl.go` | 修改 | ① `executeRenderEDL` 在 prepare 阶段计算 `resumeSegDone = effectiveResumeSegDone(task)` 并记日志，执行改调 `ffmpeg.RunRenderPlanResume(taskID, plan, resumeSegDone, ...)`；② 新增 `markRenderPlan`(406)：记录 `SegTotal` 并清除本次已消费的 `ResumePending`；③ `setRenderStage`(425) 签名改为收 `ffmpeg.StageRunInfo`，合并 `SegDone` 锚点后仅在推进时写库（避免无谓写盘） |
| `FVCS/pkg/task/proxy.go` | 修改 | 同步 `setRenderStage` 新签名（GenProxy 链路编译对齐，无语义变更） |
| `FVCS/pkg/task/task_resume_test.go` | 新增（376 行） | `pkg/task` **首建单测**，8 个 `TestA06*` 用例：`TestA06IsRenderTaskType`（渲染类识别，空/TRANSCODE 判否）、`TestA06EffectiveResumeSegDone`（无标记/SegDone=0/有标记三分支）、`TestA06MergeRenderRunInfo`（进度单调、SegDone 单调、非推进不落库）、`TestA06RecoverRuntimeTask`（渲染类+GEN_PROXY 可续跑 / 传统转码仅重排队 / 非 RUNNING 状态不改动）、`TestA06ResumeAnchorLifecycle`（`markRenderPlan` 消费续跑标记 + `setRenderStage` 落锚点 + 二次崩溃重启仍能续跑）、`TestA06ResumableSegmentPrefix`（完整前缀保留 + 半成品/缺失片段处回退 + 断点后残留清理 + `want<=0` 零副作用）、`TestA06PlanOrphanWorkDirCleanup`（7 天阈值 + 保留最近 3 个 + 活跃任务不计入）、`TestA06CleanupOrphanWorkDirs`（真实目录扫描与删除） |

**续跑语义（本轮落地口径）**

| 环节 | 行为 |
|---|---|
| 崩溃/重启时 | `loadTasksFromDB` 将 `RUNNING` 的渲染任务置 `Waiting` + `ResumePending=true`，**不丢任务**；`SegDone`/`SegTotal` 从 `task_list` 读回 |
| 重跑准备 | `effectiveResumeSegDone` 判定是否需要续跑（仅渲染类且 `ResumePending` 为真时生效） |
| 段复用 | `ResumableSegmentPrefix` 校验前 N 段产物完整性，**首个缺失/空文件处即断点**；断点起（含断点）的残留产物被清理，防止半成品进入拼接 |
| 执行 | `RunRenderPlanResume` 跳过可信前缀，仅执行断点之后的分段 + concat + mux + finalize；进度以「第 x/y 段」继续上报 |
| 完成后 | `markRenderPlan` 落 `SegTotal` 并清 `ResumePending`；成功/失败均走既有 `cleanupTaskFiles` 清理 workDir |
| 孤儿 workDir | 启动时 + GC tick 扫描 `config.TempDir`：无对应任务的目录按 mtime 倒序保留最近 3 个、删除早于 7 天者；活跃任务目录永不清理 |

**与 06 §3.3 的差异（建议回写 06）**：06 §3.3 要点 3 称「片段完整（**时长校验通过**）则复用」，本轮实现以「存在 + 非空」判定（`segmentArtifactComplete`）。理由：时长校验需逐段 ffprobe，成本高且与并发上限冲突；且 ffmpeg 写片段失败时通常落 0 字节或不存在，非空判定已能覆盖绝大多数半成品；若后续要求更严，可在 `segmentArtifactComplete` 内接 ffprobe。

**校验结果**：`go vet ./pkg/...`、`go build ./pkg/...`、`go test ./pkg/... -count=1` **三项全绿**（`ok Fnos.VC_Service/pkg/ffmpeg`、`ok Fnos.VC_Service/pkg/task`）；仅跑 A-06：`-run TestA06 -v` → 8 用例全 PASS（`ok Fnos.VC_Service/pkg/task 1.100s`）；`gofmt -l ./pkg` 无输出。
**工具链**：`C:\Users\xx318\go\pkg\mod\golang.org\toolchain@v0.0.1-go1.24.6.windows-amd64\bin\go.exe` + `GOTOOLCHAIN=local`（无 CGO/gcc：`CGO_ENABLED=0`，故单测不依赖 sqlite3 真实建表，`saveTaskToDB` 在 `manager.db == nil` 时直接返回，内存 TaskManager 即桩）。
**A-06 已解除的阻塞**：§4「断点续跑」从「待做（阻塞：代码缺口）」转为「仅剩真机验证」；§5.1 的 P3① 闭环，A 组代码缺口仅剩 A-07 的真机联调。
**A-06 未做（留给后续）**：真机杀进程重启验证（08 E2E-05）、`-race` 并发校验（本机无 CGO，P0 阶段不启用）、「重试时 workDir 完整则复用」中更严的时长校验（见上差异说明）。

---

### 2026-09-14 第十一轮（D 组：D-01 FVCS 侧白名单 + 共享向量 + 单测）

D 组首次开工：把 FVCS 侧的安全校验从 `pkg/protocol/edl.go` 内联逻辑抽为独立同源模块，并建立**两侧共用的测试向量**（07 §3.2/§3.5/§3.6/§8）。

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCS/pkg/protocol/edl_validate.go` | 新增 | 导出 `ValidateRelPath`（相对路径 L1~L3：非空/非绝对/无 `..` 逃逸/扩展名白名单）、`ValidateRelPathDecoded`（先 `url.PathUnescape` 再校验，防 URL 编码绕过）、`AllowedSourceExt`（素材扩展名白名单查询）、`ValidatePayloadLimits`（载荷体积与规模上限，07 §3.6）；内部 `validateOutputNameStrict`（输出名结构化白名单）；**本轮新增** `ValidateRootRelPath`（`sourceRoot`/`destRoot` 用：空值放行、非空走 L1~L3）与 `ResolveRelPath` 系列（`EvalSymlinks` 落地 + 解析结果必须仍在根内，符号链接逃逸判 `E_ASSET_NOT_IN_ROOT`） |
| `FVCS/pkg/protocol/edl.go` | 修改 | `ValidateRenderEDLPayload` 改为统一调用 `edl_validate.go` 的上述入口（原先只有 clips/timeline/profile/totalMs 检查）；**新增 `sourceRoot`/`destRoot` 白名单校验**，与 `E_ASSET_NOT_IN_ROOT`/`E_EDL_INVALID` 错误码对齐 |
| `FVCS/pkg/protocol/testdata/edl_vectors.json` | 新增（15663 字节） | 共享向量集，含 `version=1`、`spec=07 §3.2 / §3.5 / §3.6 / §8`、`usage`（FVCS 与 FVCC 共用，规则以 07 为唯一来源，两侧不得各自改向量）；用例分组：`relpath` 14 / `output` 11 / `payload` 14 / `symlink` 1；覆盖白名单通过与各类拒绝场景（空段、绝对路径、`..` 穿越、非法扩展名、非法输出名、超限载荷、符号链接逃逸、根路径穿越/绝对路径 `PL-ROOT-TRAVERSAL`/`PL-ROOT-ABS`） |
| `FVCS/pkg/protocol/edl_validate_test.go` | 新增（5613 字节） | 向量驱动测试，从 testdata 加载同一份 JSON：`TestEDLVectorsRelPath`、`TestEDLVectorsOutputName`、`TestEDLVectorsPayload`、`TestValidatePayloadLimits`、`TestResolveRelPath`、`TestResolveRelPathSymlinkEscape` |

**实测勘误（已回写向量）**：`P-DOUBLE-SLASH`（`a//b.mp4`）实际返回 `E_ASSET_NOT_IN_ROOT` 而非 `E_EDL_INVALID`——校验顺序为 L2（空段→不在根内）先于 L3（形状非法），向量期望值已按实测修正并在用例内注明。**该顺序属实现口径，D-02 复用同一向量时必须保持一致**。

**校验结果**：本轮复跑 `go test ./pkg/protocol -count=1 -v` → **6 顶层 / 46 子例全绿，0 FAIL**（`ok Fnos.VC_Service/pkg/protocol 1.367s`）；`go build ./pkg/...` 无输出（成功）、`go test ./pkg/... -count=1` 全绿（`protocol`/`ffmpeg`/`task` 均 `ok`）。`go build ./...` 仍仅 `cmd/settings` 因 fyne + go-gl 构建约束失败（既有环境限制，非本轮引入）。

**D-01 未做（留给后续）**：① 规则需按 08 §4.4 走独立评审（与 07 文档口径逐条对齐）；② D-02 需把同源规则落到 `FVCC/server/edl_validate.go` 并让 `PathValidator` 复用，且加载**同一份**向量；③ S1~S15 真机用例（07 §8、08 E2E-08）需在 D-02/D-03 与真机环境就绪后跑；④ D-01 未接入 `pkg/server` 下发侧调用链的端到端验证（当前仅单测层覆盖）。

### 2026-09-14 第十二轮（D 组：D-02 FVCC 侧同源校验 + 渲染提交接入 + 共享根映射层）

把 D-01 的规则在 FVCC 侧复用并接入渲染提交链路，同时补齐「协议内不出现磁盘绝对路径」的映射层（03 §2.3、09 §2）。

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/server/edl_validate.go` | 已有，本轮完成接入与对齐 | 与 FVCS 同源复刻：`validateRelPath`（L1~L3）/`validateRelPathDecoded`/`validateOutputNameStrict`/`validateRootRelPath`/`validatePayloadLimits`/`decodeRenderPayloadStrict`/`validateRenderEDLPayload`/`edlResolveRelPath`/`edlWithinRoot`/`edlValidateStatus`；**加载同一份共用向量**（`edl_validate_test.go` 指向 `../../FVCS/pkg/protocol/testdata/edl_vectors.json`，未复制也未另起） |
| `FVCC/server/security.go` | 修改 | 新增 `PathValidator.ValidateRel(root, rel, allowExt)`：L1~L3 复用同源函数 + **L4 落地 `EvalSymlinks` 后带边界前缀校验**（拦软链越权），返回结构化 `edlValidationError`；新增 `hasAuthorizedRootFor`（判定素材根是否已纳入授权目录，含软链解析）；`resolveCacheRoot` 缺省推导改用 `resolveMediaRootsFor`（本地素材根语义，避免被映射层污染为相对根导致 `_wve_cache` 不可解析） |
| `FVCC/server/handlers_render.go` | 修改 | ① `resolveEDLRoots` 增加 `rootMapped` 返回值：配置 `settings.smbSharePath` 时用 `toShareRelRoot` 把本地素材根/成品根映射为共享相对根；② 新增 `toShareRelRoot`/`normalizeRootForCompare`/`isRootPathStyleErr`；③ 闸门 6 `precheckAssets`：素材根已授权时走 `ValidateRel` 严格分支（`E_EDL_INVALID`→400 / `E_ASSET_NOT_IN_ROOT`→403 / `E_ASSET_MISSING`→404）并**另做 `fileExists` 存在性判定**，未授权则回退轻量前缀判定；④ 闸门 8 对**仅根写法**错误降级为告警 |
| `FVCC/server/edl_validate_test.go` | 修改 | 新增 `TestD02RenderSubmitStrictAssetGate`（严格闸门：缺失素材 404 / 软链越权 403 / 输出名白名单拒绝）、`TestD02RenderOutputNameStrictGate`、`TestD02ShareRelRootMapping`（10 子例：共享内 / 多级 / 斜杠混用与尾斜杠 / 大小写不敏感 / UNC / 重复斜杠 / 等于共享根 / 不在共享内 / 空共享根 / 前缀伪装 `media2`）、`TestD02RenderPayloadRootMappedToShareRel`（映射后载荷无盘符且通过 FVCS 侧同源白名单） |

**实测踩坑（已修复）**：① `PathValidator.ValidateRel` 的 L4 沿用 `edlResolveRelPath`，该函数对**不存在的文件**走「父目录解析 + 文件名拼接」的宽松路径（输出文件场景需要），故**素材存在性必须在提交闸门内另行判定**，否则缺失素材会以 200 漏过预检（本轮首跑即由此暴露）；② `resolveCacheRoot` 原先复用 `resolveEDLRoots` 的 src 推导 `_wve_cache`，映射层落地后 src 会变成相对根（如 `videos`）→ 已改用本地语义的 `resolveMediaRootsFor`。

**校验结果**：`go build ./...` 与 `go vet ./...` 无输出（成功）；`go test ./...` 全绿（`ok fvcc 4.680s`）；`go test -run TestD02 -v .` → **14 个顶层用例全 PASS（0 FAIL / 0 SKIP，含软链越权用例）**；B-04 既有渲染提交用例同批复绿（严格分支接入未改变既有契约）。

**D-02 遗留（留给后续）**：① 08 §4.4 要求的独立评审（与 07 §3.2/§3.5/§3.6/§8 逐条对齐，含 `P-DOUBLE-SLASH` 的 L2 优先口径）；② S1~S15 真机安全用例（07 §8、08 E2E-08）随 E-02/E-03 跑；③ 映射层当前以 `settings.smbSharePath` 为唯一共享根基准，尚未支持「多共享根/多授权目录各自映射」（当前契约仅一根）。

### 2026-09-14 第十三轮（D 组：D-03 DPAPI 凭据档案 + 本机管理接口 + 创建前置拒单）

D 组第三项：按 07 §5.3/§5.4/§5.6 落地凭据档案库与只服务本机的管理接口，满足 08 §4.4 D-03 判据「**落库为密文；`Resolve` 越 Scope 被拒**」，并把校验前移到**任务创建前**（不再等到挂载时才失败）。

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCS/pkg/smb/credentials.go` | 新增（696 行 / 21821 字节） | SQLite 表 `credentials` 只落 `secret_cipher`，口令经 **DPAPI（`winapi.DPAPIProtect` = CryptProtectData，LocalMachine 作用域 + 每档案 entropy）** 加密；`Put` 幂等创建/轮换（更新 `rotated_at`）、`Get`/`List` 只回 `CredentialMeta`（无 secret）、`Resolve`（Scope 校验 → DPAPI 解密 → 明文只在内存且由调用方 `Wipe()` 覆写）、`Delete`（`RUNNING` 任务引用时 `E_CREDENTIAL_IN_USE`）、`VerifyScope`/`VerifyTaskTarget`/`ScopeContains`/`ScopeWithinShare`/`normalizeUNCPath`/`maskUNC`、`Audit` + `purgeExpiredAudit`（**保留 180 天**）、`MountSMBShareWithCredential`（挂载前共享根校验，失败按 `E_CREDENTIAL_INVALID` 记审计） |
| `FVCS/pkg/smb/credentials_test.go` | 新增（432 行） | 13 个用例：`TestCredentialStoredAsCiphertext`（库内无明文）、`TestCredentialMetaHasNoSecret`、`TestCredentialRotate`、`TestCredentialScopeDenied`、`TestCredentialScopePrefixBoundary`（前缀伪装 `\share2` 不越权）、`TestCredentialWipeClearsSecret`、`TestCredentialLoginNameWithDomain`、`TestCredentialDeleteRejectedWhenInUse`、`TestCredentialInvalidAndNotFound`、`TestPutCredentialValidation`、`TestAuditRetention`、`TestScopeHelpers`、`TestDefaultCredStoreInjection` |
| `FVCS/pkg/server/local_admin.go` | 新增（232 行） | `/local/credentials`：`POST`/`PUT`（创建或轮换）、`GET`（无 `id` 列表 / 带 `id` 单条）、`DELETE`；**非环回 `RemoteAddr` 一律 403 `E_CREDENTIAL_SCOPE_DENIED`**（与 `config.ListenLocalOnly` 叠加）；响应统一 `{ok,code,message,data}`，**永不回显口令**（PUT 回 `credentialId`/`scope`/时间戳/`secretStored=dpapi`）；请求体上限 64 KB；错误码→HTTP：404 `E_CREDENTIAL_NOT_FOUND` / 403 `E_CREDENTIAL_SCOPE_DENIED` / 409 `E_CREDENTIAL_IN_USE` / 400 `E_CREDENTIAL_INVALID`；`InitCredentialAdmin()` 幂等（库已存在仅重注册占用判定），并把占用判定注入 `task` |
| `FVCS/pkg/server/local_admin_test.go` | 新增（249 行） | 6 个用例：`TestLocalCredentialsRejectsNonLoopback`、`TestLocalCredentialsLoopbackLifecycle`（建→查→轮换→删全链路）、`TestLocalCredentialsDeleteInUse`、`TestLocalCredentialsBadRequests`、`TestLocalCredentialsStoreUnavailable`、`TestIsLoopbackRequest` |
| `FVCS/pkg/server/server.go` | 修改 | ① `Start` 按 `config.ListenLocalOnly` 选择 `127.0.0.1`/`0.0.0.0` 绑定，并在起服务前调用 `InitCredentialAdmin()`（失败记错误不阻断）；② 新增 `precheckCredentialTarget`，在 **`CreateSMBTask` / `RenderEDL` / `GenProxy` 三处创建前置拒单**（越界即拒，不产生任务） |
| `FVCS/pkg/server/credential_precheck_test.go` | 新增（64 行） | `TestPrecheckCredentialTarget`：共享根一致放行、绝对 UNC 越界拒、相对路径忽略（交由相对路径校验层）、无凭据/未配置场景放行 |
| `FVCS/pkg/ipc/ipc.go`、`FVCS/cmd/service/main.go` | 修改 | 两条启动路径（IPC `StartService`、开机自启 `AutoStart`）均在 `task.Init` 前调用 `server.InitCredentialAdmin()`；IPC 分支失败即返回错误，自启分支仅记日志 |

**实测踩坑（已修复）**：
1. **首版前置校验用错语义**：初版 `precheckCredentialScope` 直接调 `VerifyScope`（只判「目标 ∈ Scope 条目」），导致**合法目标（正好是共享根）被误拒** `E_CREDENTIAL_SCOPE_DENIED`（`TestPrecheckCredentialScope` 首跑即 FAIL）。按 07 §5.3 口径改为新增 `VerifyTaskTarget(id, shareBase, targets...)`：**目标共享根一致 → 放行；目标落在 Scope 内 → 放行；空目标 → 放行（仅取档案用于挂载）；绝对 UNC 越界 → 拒；相对路径 → 忽略**（相对路径的形状/越权由 D-01/D-02 的 `ValidateRelPath` 负责，避免两套规则打架）。`Resolve` 侧原有 Scope 校验保留不变（纵深防御）。
2. **初始化必须幂等**：`ipc.StartService` 与 `main.AutoStart` 两条路径都会触发，二次打开同一 SQLite 库会报错 → 已改为 `DefaultCredStore()` 非空时只重新注册占用判定并直接返回。
3. **循环依赖**：`smb` 与 `task` 互相不可 import，占用判定经 `task.RegisterCredentialInUseChecker(store)` 回调注入（`pkg/task/credential.go:73`），删除前查该凭据被哪些 `RUNNING` 任务引用。
4. **CGO 链接必须把 mingw 目录加进 PATH**（本轮复验时踩到）：只设 `CGO_ENABLED=1` + `CC=C:\msys64\mingw64\bin\gcc.exe` 仍会链接失败，报 `link.exe: running C:\msys64\mingw64\bin\gcc.exe failed: exit status 1` 且 `FAIL … [build failed]`——gcc 内部还要调 `as`/`ld` 等辅助程序，缺 PATH 时 gcc 的真实报错被吞掉。正确姿势：`set PATH=C:\msys64\mingw64\bin;%PATH%` + `CGO_ENABLED=1` + `CC=gcc`（`gcc --version` 与手工链接测试均需先加 PATH 才可用）。加上后同一批用例由 build failed 直接转为全 PASS。
5. **长耗时命令用后台进程落盘再读**（§0 既有坑升级版）：`go test`（含 CGO 链接）在当前 shell 直接跑时，回显只剩首行 `# Fnos.VC_Service/pkg/smb.test`，且重定向文件甚至不生成（shell 未等落盘就返回）。规避：`Start-Process cmd.exe /c "… > <工作区 temp>\x.txt 2>&1"` 起隐藏后台进程 → `Start-Sleep` → `read_text` 读文件；本轮即以 `d03_bg2.txt`（`ok pkg/smb 0.420s` / `ok pkg/server 0.302s`）与 `d03_full.txt`（BUILD/VET/TEST 三退出码 0）拿到完整证据。切勿使用 `$env:TEMP`。

**校验结果**：`go build ./pkg/... ./cmd/service`、`go vet ./pkg/... ./cmd/service`、`go test ./pkg/... -count=1` **三项全绿 0 FAIL**；`pkg/smb` **13 用例**、`pkg/server` **7 用例**全 PASS；**收尾复验（同一天，加 mingw PATH 后重跑）**：`BUILD_EXIT=0` / `VET_EXIT=0` / `TEST_EXIT=0`，逐包 `ok pkg/ffmpeg 0.257s`、`ok pkg/protocol 0.185s`、`ok pkg/server 0.425s`、`ok pkg/smb 0.609s`、`ok pkg/task 0.248s`，`config/ipc/logger/winapi` 无测试文件；`gofmt -w` 后无格式差异。**硬约束核查**：`credentials.go`/`local_admin.go`/`server.go` 全部 `logger.*` 调用逐条扫描 **0 处 `username`/`password`/`secret`**（只记 `credentialId` + 已掩码共享名 `maskUNC`）。

**D-03 遗留（留给后续）**：① 08 §4.4 要求的独立评审；② S1~S15 真机安全用例（07 §8、08 E2E-08）随 E-02/E-03 跑（需 NAS/SMB/ffmpeg 在环）；③ ~~**M3 迁移未做**~~ → **已完成（2026-09-15 第十五轮）**：`FVCS/pkg/config/reencrypt.go` + `fvcs-cli reencrypt/status`（DPAPI 主密钥、幂等、`-dry-run`），旧硬编码 `encryptKey` 仅用于识别 legacy 密文、不再参与解密；④ `M2` 之后的 `SMBUser/SMBPassword` 明文列删除（`M4`）不在本轮范围。

---

### 2026-09-14 第十四轮（D 组：D-04 安全响应头 + 通用接口限流 + 双侧审计/告警埋点收口）

D 组第四项：按 07 §4.4/§4.5/§7 落地安全响应头、通用接口限流与审计告警，**D 组至此 4/4 代码层收口**。

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/server/ratelimit.go` | 新增 | ① `securityHeaders()`：全链路注入 `X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer`、`X-Frame-Options: SAMEORIGIN`（`router.go` 中挂载顺序 **早于** 鉴权/业务中间件，故静态资源、404/SPA 兜底、4xx/5xx 均带全）；② 三档通用限流：`authFailThrottle`（鉴权失败按 IP 冷却 → 429 `E_RATE_LIMITED`，并 `auditReject("ratelimit.auth")`）、`renderSubmitLimit`、`proxySubmitLimit`（提交类接口令牌桶，仅成功放行才计数）；③ 通用滑动窗口限流器 + WS 连接上限，保留既有 `streamLimiter`（M4 预览）不动 |
| `FVCC/server/router.go` | 修改 | `securityHeaders()` 置于路由链首位；`authFailThrottle` 挂在全局；`renderSubmitLimit` 挂 `/render`、`proxySubmitLimit` 挂 `/proxy` |
| `FVCC/server/security_audit.go` | 新增（132 行） | D-04 审计与告警集中层：挂载点选在 **`edlErr`**（03 §4.1 统一失败出口，40+ 处 handler 共用），只对拒绝类码记账（`E_EDL_INVALID`/`E_PAYLOAD_INVALID`/`E_ASSET_NOT_IN_ROOT` → `validate.reject`）；**按来源 IP 聚合 5min 滑动窗口**，`E_ASSET_NOT_IN_ROOT` 达 **3** 次、载荷类达 **50** 次时首次跨阈值触发 `security.alert`（ERROR 日志 + 审计）；`Detail` 仅记「方法 + 路径 + 来源 IP」，**不含素材绝对路径/凭据**；`globalStore` 为 nil 时降级为纯日志 |
| `FVCC/server/security_audit_test.go` | 新增 | 3 个用例：`TestD04RejectIsAudited`（拒绝落 `validate.reject` + 脱敏校验）、`TestD04AssetScanAlertThreshold`（越权码达阈值出 `security.alert`，且**冷启动计数正确清理**）、`TestD04AssetNotInRootRejectedAndAudited`（**改走渲染提交链路**：素材根内软链指向授权根外 → 403 `E_ASSET_NOT_IN_ROOT` 且落审计） |
| `FVCC/server/ratelimit_test.go` | 新增 | 覆盖安全头（含 404 兜底）、鉴权失败冷却与令牌滚动、渲染限流「仅成功计数」、限流器过期驱逐、WS 连接上限等用例 |
| `FVCS/pkg/server/audit.go` | 新增（102 行） | 七类审计动作常量（`auth.fail`/`ratelimit.auth`/`validate.reject`/`task.submit`/`render.submit`/`proxy.submit`/`security.alert`，与 FVCC 侧 Action 命名对齐便于跨端聚合），统一经 `smb.AuditEvent` 落 `audit_log`（actor 取来源 IP，缺失回退 `local`）；`auditReject` 复用 07 §7 同阈值（3 / 50，5min 滑动窗口）触发告警；**审计库未就绪时降级 WARN 日志，不影响请求可用性**（属旁路能力） |
| `FVCS/pkg/server/security.go`、`server.go` | 修改 | `security.go` 在鉴权失败与限流冷却处分别记 `auth.fail`/`ratelimit.auth`；`server.go` 在 `CreateSMBTask`/`RenderEDL`/`GenProxy` 的成功与拒绝分支分别记 `task.submit`/`render.submit`/`proxy.submit` 与 `validate.reject` |
| `FVCS/pkg/server/audit_test.go` | 新增 | 5 个用例：`TestD04AuditAuthFailWritten`、`TestD04AuditRateLimitRejectAudited`、`TestD04AuditSubmitAndReject`、`TestD04AlertOnAssetScan`、`TestD04AuditSkippedWhenStoreUnavailable`（库不可用时不 panic、不影响请求） |

**实测踩坑（已修复）**：
1. **越权用例最初选错链路**：`TestD04AssetNotInRootRejectedAndAudited` 首版走「项目 CRUD 保存」，实际返回 `E_EDL_INVALID`（项目保存闸门 2 统一折叠错误码），而 `E_ASSET_NOT_IN_ROOT` **只在渲染提交的 strictly 分支 `precheckAssets`（闸门 6）产生**。修正为「素材根内建软链指向授权根外 → `/render` 提交」，并补 `target == p.ID` 断言后转绿。
2. **告警计数器单例污染**：告警限流器为包级变量，前序用例打满窗口会让后续用例断言失败 → 用例 setUp 改为 **重建限流器实例**（`newSlidingWindowLimiter`），而非调用不存在的 reset 方法。
3. **测试包路径**：FVCC 模块根在 `FVCC/server`（不是 `FVCC`），`go vet/test` 必须在 `FVCC/server` 目录执行 `./...`；FVCS 侧 `pkg/smb` 依赖 `go-sqlite3`，**必须开 CGO**，否则 stub 报错——用 PowerShell 进程级注入 `$env:CGO_ENABLED='1'` + `$env:PATH='C:\msys64\mingw64\bin;'+$env:PATH` 后全绿（勿用 `$env:TEMP` 落盘）。

**校验结果**：FVCC `go vet ./...` + `go test ./... -count=1` **全绿**（`ok fvcc 7.281s`），13 个 `TestD04*` 全 PASS；FVCS 开 CGO 后 `go test ./... -count=1` **逐包全绿**（`pkg/protocol`/`pkg/server`/`pkg/smb`/`pkg/task`），`pkg/server` 5 个 `TestD04*` 全 PASS。收尾已把本轮验证产生的 `_*.txt` 落盘文件从仓库移出（避免污染仓库；本会话已移入工作区 temp）。

**D-04 遗留（留给后续）**：① 08 §4.4 独立评审；② S1~S15 真机安全用例（07 §8、08 E2E-08）随 E-02/E-03 跑；③ `config.go:55` 硬编码 `encryptKey` 的 **M3 迁移**（DPAPI 主密钥 + `fvcs-cli reencrypt`）仍未做——**这是 D 组之后仅存的代码级安全欠账**。（③ 已于第十五轮关闭，见下。）

---

### 2026-09-15 第十五轮（D 组收尾：M3 主密钥迁移落地 + E2E-08 HTTP 层全链路安全链）

本轮关闭 D 组最后两项遗留：M3（DPAPI 主密钥 + `fvcs-cli reencrypt`）与 E2E-08 的 HTTP 层全链路安全链验证（真机环境部分仍见 §4）。

**① M3 主密钥迁移（关闭 D-03/D-04 遗留 ③）**

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCS/pkg/config/reencrypt.go` | 新增（79 行） | `ConfigPath()`/`MasterKeyPath()`（诊断用，**不触发密钥生成**）、`NeedsReencrypt(path)`（由 `decryptForLoad` 的 `usedLegacy` 判定是否仍为旧硬编码密钥密文）、`ReencryptFile(path, dryRun)`：**JSON 明文与字段值完全不变，仅更换密钥与 nonce**；非 legacy 文件返回 `(false, nil)` 幂等退出；解密失败（密文损坏/非本机密钥）原样报错**绝不覆盖**；落盘走「同目录 `.m3tmp` + rename」防半截密文 |
| `FVCS/cmd/fvcs-cli/main.go` | 新增（4350 字节） | `fvcs-cli reencrypt [-dry-run] [-config <path>]` / `status [-config <path>]`（打印配置路径、主密钥路径与是否已生成、是否需要迁移）/ `version`(1.0.0 M3) / `help`；退出码 0 成功 / 1 失败 / 2 用法错误 |
| `FVCS/pkg/config/config.go` | 修改 | `Reencrypt()`（第 365 行）为迁移入口；加载/保存统一走 DPAPI 主密钥，旧硬编码 `encryptKey` **仅用于识别 legacy 密文**，不再参与解密 |
| `FVCS/pkg/config/reencrypt_test.go` | 新增（4652 字节） | `TestM3ReencryptFileMigratesLegacy`（legacy→主密钥，迁移后 `NeedsReencrypt=false`）、`TestM3ReencryptFileIdempotentOnMasterKey`（反复执行不改盘）、`TestM3ReencryptFileErrors`（文件缺失/密文损坏报错且不写盘） |
| `FVCS/pkg/config/config_d04_test.go` | 新增（4085 字节） | 6 用例：`TestD04MasterKeyRoundTrip`、`TestD04LegacyKeyMigration`、`TestD04TamperedCiphertextRejected`（篡改密文拒解）、`TestD04MasterKeyFileStable`、`TestD04NormalizeDefaults`、`TestD04SaveLoadRoundTrip` |

**实测行为（均为预期）**：`fvcs-cli status` 在无配置环境报「配置文件（不存在）/ 主密钥（未生成，下次启动服务或迁移时创建）」并退出 0；`reencrypt` 在配置缺失时报 `读取配置失败: … cannot find the file specified` 退出 1 —— **不造空配置、不静默创建密钥**。

**② E2E-08 全链路安全链（HTTP 层，走生产路由）**

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/server/security_e2e_test.go` | 新增 | `TestD04SecurityE2ERejectChain`：经**真实 `newRouter`（生产中间件链）**逐向量提交，断言 状态码 + `code` + **任务表零新增** + **审计增量** + 脱敏 + 安全头；`TestD04SecurityE2EAssetScanAlert`：素材根内软链越权第 3 次触发 1 条 `security.alert` |

**实测勘误（HTTP 层真实行为，可作真机 E2E-08 对照表）**：

| 向量 | 状态码 | 错误码 | 落 `validate.reject` |
|---|---|---|---|
| `file` 含 `../`（`../../etc/passwd`） | 400 | `E_EDL_INVALID` | 是 |
| `file` 绝对路径（`/media/videos/../exports/x.mp4`） | 400 | `E_EDL_INVALID` | 是 |
| `file` URL 编码穿越（`a%2F..%2F..%2Fsecret.mp4`） | **404** | `E_ASSET_MISSING` | 否 |
| `file` 控制字符（换行） | 400 | `E_EDL_INVALID` | 是 |
| 时间码非法（出点不晚于入点） | 400 | `E_EDL_INVALID` | 是 |
| 非法 `presetKey`（`libx264 -y /etc/x`） | 400 | `E_PROFILE_INVALID` | 否 |
| `outputName` 穿越（`../../windows/system32/x`） | 400 | `E_EDL_INVALID` | 是 |
| 正向对照（合法提交） | 200 | — | 入队 1（非拒绝） |

**两点口径说明**：① 「编码穿越」按实现走 L2 空段判定，落在**素材缺失 404** 而非 400（与 D-01 向量 `P-DOUBLE-SLASH` 的 L2 优先口径一致），且该分支不属 07 §7 拒绝记账类；② 08 E2E-08 的 ③「未声明额外字段 → `E_PAYLOAD_INVALID`」在 `/render` 侧**不可达**——`/render` 载荷由服务端自建（`ShouldBindJSON` 不拒未知字段），该码只在 D-02 的 `decodeRenderPayloadStrict` 校验层覆盖（S6/S12 向量）。

**D-04 端到端清单（E2E-08 五向量对照）**：①`../` → `E_EDL_INVALID` ✅；②非法时间码 → `E_EDL_INVALID` ✅；③未声明额外字段 → `E_PAYLOAD_INVALID`（校验层，见上）；④非法 `presetKey` → `E_PROFILE_INVALID` ✅；⑤软链/绝对路径逃逸根目录 → `E_ASSET_NOT_IN_ROOT` 403 ✅（`TestD04SecurityE2EAssetScanAlert`）。**「任务表无新增记录」四项均可断言，全部通过。**

**校验结果**：FVCS `go build ./pkg/... ./cmd/fvcs-cli ./cmd/service` + `go vet ./...` + `go test ./pkg/... -count=1` 全绿（`config 0.262s` / `ffmpeg 0.210s` / `protocol 0.145s` / `server 3.260s` / `smb 0.380s` / `task 0.185s`；`BUILD_EXIT=0`、`VET_EXIT=0`、`TEST_EXIT=0`）；FVCC `gofmt -l` 空 + `go vet ./...` 空 + `go test ./... -count=1` 全绿（`ok fvcc 3.912s`），含扩展后的 7 向量安全链与告警阈值用例。`gofmt -w` 已回填 D-03 遗留的 2 个未格式化文件（`pkg/server/local_admin_test.go`、`pkg/task/credential.go`）；`cmd/settings`、`cmd/test_start`、`cmd/ui`（fyne 遗留）保持原状不动。

**遗留（D 组代码层至此无欠账）**：① 08 §4.4 独立评审需外部评审人；② **S1~S15 真机用例（07 §8、08 E2E-08）**仍需 NAS/SMB/ffmpeg 在环（随 E-02/E-03），但**非法载荷拦截 + 零任务副作用已在本轮以生产路由单测覆盖**；③ 映射层「多共享根」与 `M2` 之后 `SMBUser/SMBPassword` 明文列删除（M4）不在本轮范围。

---

### 2026-09-16 第十七轮（缺陷修复二：agentVersion 版本对齐 + FVCC↔FVCS 断链自愈 + 重出包）

**① FVCS 上报节点版本号硬编码（`agentVersion = "1.2.0"` 与实际包 1.2.1 不一致）**

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCS/pkg/version/VERSION` | 新增 | 内容 `1.2.1`，作为**唯一版本来源**（与 FVCC manifest 版本同步） |
| `FVCS/pkg/version/version.go` | 新增 | `//go:embed VERSION` 注入 `var Version string`，启动期 `TrimSpace` 规整 |
| `FVCS/pkg/server/hello.go` | 修改 | `const agentVersion = "1.2.0"` → `var agentVersion = version.Version`（第 20 行），`Hello` 上报的 `AgentVersion` 随包版本自动对齐，杜绝再次漏改 |
| `FVCS/cmd/service/main.go` | 修改 | 启动日志改为 `FVCS Service starting... version=%s`，运行时即可核对版本 |
| `FVCS/pkg/version/version_test.go`、`pkg/server/hello_push_test.go` | 新增/修订 | `TestVersionEmbeddedFromFile`（embed 与 VERSION 文件一致且非空）+ Hello 契约用例（字段名/非空），全 PASS |

**实测证据**：重编译产物 `D:\FVCS\fvcs-service.exe`（25,734,551 B）二进制扫描 `1.2.1` × 10、`1.2.0` × 0（旧包含 `1.2.0` × 1）；替换重启后日志 `[2026-09-16 12:01:01] [INFO] [service] FVCS Service starting... version=1.2.1`，端口 `WS:8080 / HTTP:10000 / IPC:127.0.0.1:5000` 全部恢复监听（新 PID 28980）。

**② FVCC ↔ FVCS WebSocket 约 15s 断链重连（`read error` / `wsarecv aborted` / `close 1006`）**

**根因定位（先证伪服务端）**：FVCS `pkg/server/server.go` 无任何 15s 级主动断链逻辑——`wsReadTimeout=120s`（数据帧与 Pong 均刷新）、`wsPingInterval=30s`、`wsHeartbeatTimeout=150s`（超时才主动断开）。日志显示断链**紧跟在 `Hello pushed` 之后数秒内发生**，来源覆盖 `127.0.0.1`（本机回环）与 `192.168.1.104→192.168.1.106`（LAN）两个方向，且 `192.168.1.106` 会在 1 秒内打出 3 条并发连接（`01:25:36~38`、`08:02:52~53` 两组）→ 判定为**客户端/网络中继侧发起的中断**（本机在环的 `clash-verge(PID 11824)` / `verge-mihomo-alpha(PID 2056)` 会重置长连接）。而 `FVCC/server/remote.go` 存在三处放大缺陷：

1. `Connect` 无单飞：每次请求/UI 轮询各拨一条 WS，连接成簇堆积，而 FVCS 侧仅放行 `ws_conns=3`，极易被顶满后表现为「连上就被踢」；
2. `readPump` 读错仅 `delete` 连接，**无自动重连**，节点掉线后必须人工点「测试连接」；
3. 关闭连接**不发 Close 帧**，导致对端一律记 `close 1006 unexpected EOF`（日志噪声 + 真实断因被掩盖）。

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/server/remote.go` | 修改 | ① 新增 `connMu` + `serverCfg` 快照，`Connect` 改为**单飞**（双检 + 复用既有连接），拨号/鉴权抽为 `dialAndAuth`；② 新增 `reconnectLoop`：读错后指数退避重连（1s→30s 封顶），成功后恢复 `readPump`；③ 新增 `gracefulCloseWS`（发送 Close 帧再关，消除 1006）；④ `CloseConn`/`CloseAll` **先停重连再关闭**（主动关闭不再触发自愈）；⑤ `NewRemoteClient` 同步初始化新字段 |
| `FVCC/server/remote_reconnect_test.go` | 新增 | `TestB09*` 4 组：单飞只拨号 1 次 / 异常断链自动重连 / 优雅关闭发 Close 帧 / 主动关闭停重连 |

**校验结果**：`gofmt -l` 无输出；`go test ./... -count=1` 全绿（含 `TestB09*` 与既有 `TestB07/TestB08/TestD02/TestD04/M4/EDLProject` 回归），`CGO_ENABLED=1 -race` 通过。**遗留**：`ws_conns=3` 的连接上限是否上调、以及是否在 FVCS 侧增加「同 ClientID 旧连接顶替」策略，待真机联调观察后再决策。

**③ 重新出包（两处缺陷修复的交付物）**

| 产物 | 构建口径 | 结果 |
|---|---|---|
| `D:\FVCS\fvcs-service.exe` | go1.24.6 + `CGO_ENABLED=1` + msys64 gcc（GOOS=windows/amd64），已备份旧包 | 25,734,551 B；已替换并重启（PID 28980） |
| `D:\Fnos.VideoConversion\FVCC\app\fvcc` | go1.27.1 + `CGO_ENABLED=0 GOOS=linux GOARCH=amd64`（在 `FVCC/server` 执行） | 33,814,287 B |
| **`FVCC_v1.2.1_fnos_x86.fpk`** | `fnpack-1.2.1.exe build`（`FVCC\` 工作目录） | **17,708,825 B，SHA256 `74B77404…E4638C`**，manifest `version = 1.2.1`，`checksum` 与包内 `app.tgz` md5 一致 |

包内结构逐项比对（新旧 fpk 差分）：仅 `fvcc` 一个文件变化（+55,712 B），`ui/` 6 个前端资源与 `cmd/`、`config/`、`ICON_*.PNG` **完全一致**（前端本轮未改动，未重复构建）。新包已覆盖 `D:\Fnos.VideoConversion\FVCC\FVCC_v1.2.1_fnos_x86.fpk` 与 `D:\Fnos.VideoConversion\FVCS\FVCC_v1.2.1_fnos_x86.fpk` 两处；旧包与旧二进制回滚副本存于本轮中间产物目录 `rollback_20260916\`。

---

### 2026-09-16 第十八轮（全局版本递增 1.2.1 → 1.2.2 + 重出包）

**触发约束（用户明确）**：fnos **限制同版本号应用不可重复安装**，故每次出包**版本号必须递增**；同时「所有相关版本号位置必须同步更新，以防不一致」——本轮即把全链路版本从 1.2.1 递增到 1.2.2，并把历史散落的硬编码版本字面量一并收口。

**① 全链路版本号同步（消除散落硬编码）**

| 文件 | 动作 | 要点 |
|---|---|---|
| `FVCC/manifest` | 修改 | `version = 1.2.1` → `version = 1.2.2` |
| `FVCC/ui-src/package.json` | 修改 | `"version": "1.2.0"` → `1.2.2`（前端版本落后包版本的历史漂移一并修正） |
| `FVCC/ui-src/package-lock.json` | 修改 | 两处 `1.0.0` → `1.2.2` |
| `FVCC/server/VERSION`、`FVCC/server/version.go` | 新增 | `//go:embed VERSION` → `var appVer = strings.TrimSpace(versionFile)`，作为 **FVCC 运行时版本唯一来源**（与 `FVCS/pkg/version` 同构） |
| `FVCC/server/main.go`、`handlers.go` | 修改 | 删除硬编码 `appVer = "1.0.0"`；启动日志（`main.go:110/202`）与 `/app/fvcc/api/info`（`handlers.go:58`，即前端「设置」页展示）统一取 `appVer` |
| `FVCC/server/version_test.go` | 新增 | `TestAppVerEmbeddedFromFile`（非空 / 无首尾空白 / 三段式）+ `TestAppVerMatchesPackagingFiles`（二进制版本须与 `manifest`、`ui-src/package.json` 一致，防再次漂移） |
| `FVCS/pkg/version/VERSION` | 修改 | `1.2.1` → `1.2.2` |
| `FVCS/pkg/version/version_test.go` | 修改 | 断言值改 `1.2.2` |
| `FVCS/cmd/fvcs-cli/main.go` | 修改 | 硬编码 `fvcs-cli 1.0.0` → `fmt.Printf("fvcs-cli %s (M3)\n", version.Version)` |

**② 构建与产物**

| 产物 | 构建口径 | 结果 |
|---|---|---|
| `D:\FVCS\fvcs-service.exe` | **go1.24.6**（`GOTOOLCHAIN=go1.24.6`，与上轮同口径）+ `CGO_ENABLED=1` + msys64 gcc | **25,734,551 B**（与 1.2.1 版**字节数完全相同**，仅版本串变化）；已替换并重启（新 PID 29288） |
| `D:\FVCS\fvcs-cli.exe` | 同上口径 | 3,541,504 B；`fvcs-cli version` → `fvcs-cli 1.2.2 (M3)` |
| `D:\Fnos.VideoConversion\FVCC\app\fvcc` | go1.27.1 + `CGO_ENABLED=0 GOOS=linux GOARCH=amd64`（在 `FVCC/server` 执行） | 33,814,772 B（较上轮 +485 B） |
| `FVCC\app\ui\*` | `npm run build`（`tsc -b && vite build`，30 modules） | 产物哈希与上轮**完全一致**（`index-BvU2m5Tb.js` / `index-D90joZQ6.css`，前端本轮无代码改动） |
| **`FVCC_v1.2.2_fnos_x86.fpk`** | `fnpack-1.2.1.exe build`（`FVCC\` 工作目录，输出默认名 `fvcc.fpk` → 改名） | **17,708,933 B，SHA256 `FF01333C…93EBC3`**；包内 manifest `version = 1.2.2`、`checksum = 9a3d2623b35d363076711b143037911c` |

**③ 校验证据（可复现）**

- **FVCC 单测**：`go test ./... -count=1` → `ok fvcc 10.623s`（含新增 `TestAppVer*` 两项，版本一致性自动把关）。
- **FVCS 单测**：`go test ./pkg/version/ ./pkg/server/` 全绿。
- **二进制版本扫描**：`app\fvcc` 与 `fvcs-service.exe` 均含版本串 `1.2.2`（FVCC 出现 13 处，其中 `…streamticketimage2thumbs1.2.2.` 即 embed 出的版本串）；两者残余 `1.2.1` 命中项已逐条核验上下文，**全部为 Go 标准库符号名**（形如 `crypto/tls.(*certificateMsgTLS13).marshal…1.2.1`），非业务版本字面量。
- **运行时核验**：替换重启后 `D:\FVCS\logs\app_2026-09-16.log` 与 `svc_manual_122.log` 首段即 `[2026-09-16 13:24:22] [INFO] [service] FVCS Service starting... version=1.2.2`，`WS:0.0.0.0:8080 / HTTP:0.0.0.0:10000 / IPC:127.0.0.1:5000` 全部 LISTENING（PID 29288），`Auto start completed`。
- **包内差分（1.2.1 → 1.2.2 逐文件 SHA256 比对）**：仅 `fvcc`（33,814,287 → 33,814,772 B）与 `manifest` 两个文件变化；`ui/` 6 个前端资源、`cmd/` 9 个脚本、`config/` 2 个文件、`ICON.PNG`/`ICON_256.PNG` **完全一致**。
- **双处落盘一致**：`D:\Fnos.VideoConversion\FVCC\FVCC_v1.2.2_fnos_x86.fpk` 与 `D:\Fnos.VideoConversion\FVCS\FVCC_v1.2.2_fnos_x86.fpk` SHA256 相同（`FF01333C725A7AA62B48BC4E52E644CF5CECC3428EA79EFD51033F030093EBC3`）。
- **回滚备份**：本轮中间产物 `rollback_1.2.2\`（`fvcs-service_1.2.1.exe` 25,734,551 B / `fvcs-cli_1.2.1.exe` 3,472,896 B / `FVCC_v1.2.1_fnos_x86.fpk` 17,708,825 B）。
- **风险控制留痕**：停止 FVCS 进程（PID 28980）换 exe 属**需确认的中风险操作**，已向用户垂询并获得明确授权「停止并重启」后执行；`fvcs-cli.exe` 替换前已备份。

**④ 遗留（需用户侧 / 真机）**：NAS 需用最新 `FVCC_v1.2.2_fnos_x86.fpk` 覆盖安装（**1.2.0 / 1.2.1 旧包均已作废**）；升级后核对 `Hello pushed to client … agentVersion=1.2.2` 与 WS 断链自愈表现。

---

## 4. 待验证清单（需要 ffmpeg 在环 / 真机 NAS 环境）

| 项 | 对应 | 验收口径 | 状态 |
|---|---|---|---|
| T6 可播性 | A-04 | 成品 mp4 在浏览器与 PotPlayer 均可播，`moov` 前置（`-movflags +faststart` 已构造） | 待做 |
| T8 卡死保护 | A-06/edl_exec | 构造挂起 ffmpeg，5 分钟内判 `E_TIMEOUT_STALL` 并回收进程（错误码已定义） | 待做 |
| T9 并发 | A-06/B-05 | 2 个 RenderEDL 按节点并发上限排队，进度互不干扰 | 待做 |
| 40 段拼接 | A-03 | 手工跑 40 段，检查分批滤镜图实际可执行 | 待做 |
| 时长误差 | A-04 | 输出时长误差 ≤ 0.5s | 待做 |
| 无 NVENC 降级 | A-05 | 降级 libx264 并在结果中上报说明 | 待做 |
| WS 端到端 | A-07/B-07 | 手工调用可入队并收到分段进度（`第 x/y 段 · stage`）；FVCC 侧聚合链路（B-07）已就绪，待 FVCS 在环联调 | 待做 |
| 权限位真机验证 | B-09 | FNOS 网关透传非 admin 身份时 `/render` 与 `DELETE` 类返回 403 `E_FORBIDDEN`；admin / root 与独立模式放行 | 待做 |
| T10 预览拖拽 | M4-A/C-05 | 浏览器 `<video>` 申请票据后拖拽进度条命中 `206`+`Content-Range`，换素材重新签票，票据过期自动重试一次；并发 64 上限返回 429 | 待做 |
| T11 缩略图抽帧 | M4-A/C-07 | 首帧缩略图实际由 ffmpeg 抽出（320×180），ffmpeg 缺失时回落占位图且不报错；`_proxy`/`_wve_cache`/`_exports` 不出现在素材列表 | 待做 |
| T12 代理端到端 | M4-B/C-09 | `/proxy` 提交 → FVCS 生成 → 校验通过登记 `ready` + WS `proxy_ready`；构造时长不符素材判 `E_PROXY_VERIFY_FAILED` 并落 `invalid`；同素材重复提交复用同一 `taskId` | 待做 |
| 断点续跑 | A-06 | 杀 FVCS 进程重启后，进行中的 RenderEDL 不丢任务、不重复渲染（08 E2E-05）；**代码侧 resume 已于 2026-09-13 第十轮落地并通过 8 个 `TestA06*` 单测**，剩余为真机重启验证（需 ffmpeg/SMB 在环） | 待做（仅剩真机验证，代码缺口已闭环） |
| S1~S15 安全用例 | D-01/D-02/D-03 | 07 §8 全绿（路径穿越/越 Scope/非法载荷等），非法 EDL 被拒且不产生任务（E2E-08） | 部分具备条件：**D-01 单测层已覆盖路径/输出名/载荷/符号链接逃逸向量（6 顶层 46 子例全绿）**；**Scope/凭据项（D-03）已于第十三轮以 13 + 7 用例覆盖（`Resolve` 越 Scope 拒、被占用删除拒、非环回 403、任务创建前置拒单、库不可用）**；**E2E-08 已于第十五轮以生产路由 HTTP 层覆盖（7 向量 + 正向对照 + 零任务副作用 + 审计/告警 + 安全头，全 PASS）**；剩余为真机环境下的 S1~S15 与 curl 复现（需 NAS/SMB/ffmpeg 在环，随 E-02/E-03） |
| E2E 8 场景 / 稳定性演练 | E-02/E-03 | 08 §6 全部 P0 场景 + §7.3 重启/断网/卡死三类演练 | 待做（依赖 E-01 部署） |

> 真机验证需要：一段可访问的 SMB/UNC 素材、可用的 ffmpeg 可执行文件、FVCS 以 `FVCC_DEV=1`（或实际 Socket）启动。

---

## 5. 下一步：从这里继续（**2026-09-15 复核：A/B/C/M4/D 代码层全部收口（含 M3），剩余只有真机联调 + E 组**）

> **2026-09-16 修订（第十六轮：缺陷修复 + 方案决策）**
> ① **C 组回归缺陷修复——剪辑项目列表不显示历史项目**：现象为历史创建的剪辑项目在剪辑标签页不显示、新建同名却提示已存在。根因是前后端契约不一致——`FVCC/server/handlers_edl.go::listEDLProjects` 只返回 `{items}`，而前端 `ui-src/src/api.ts` 的 `listProjects` 与 `pages/editor/index.ts` 按 `{projects,total}` 解析，渲染恒为空。已改为服务端同时下发 `items` 与 `projects`/`total`，前端改为 `r.projects ?? r.items ?? []` 兼容解析。新增 `handlers_edl_list_contract_test.go`：`TestEDLProjectListFieldsContract`（字段契约 + 同行 409 同名拦截）、`TestEDLProjectListAfterReload`（同一数据目录两次生命周期，重载后历史项目须完整列出），`go test ./... -count=1` → `ok fvcc 5.643s`；另以真实进程 E2E 复核（创建 2 个项目 → 杀进程重启 → 列表 `items=2 / projects=2 / total=2`，同名新建返回 409）。
> ② **A 组加固——FVCS ffmpeg 子进程未纳入 Job 对象**：日志 `[ffmpeg] Failed to assign process ... to job object: The handle is invalid.` 的根因是 `FVCS/pkg/winapi/winapi.go::AssignProcessToJob` 把 PID 当句柄传给 `AssignProcessToJobObject`（`os.FindProcess(pid).Pid` 并非 HANDLE）。已改为 `windows.OpenProcess(PROCESS_SET_QUOTA|PROCESS_TERMINATE)` 取真实句柄 + `defer CloseHandle`，新增 `IsProcessInJob` 复核；`CreateJobObject` 同步校验 `SetInformationJobObject` 返回值（失败即关句柄返回 error，避免 `KILL_ON_JOB_CLOSE` 静默失效）。新增 `pkg/winapi/winapi_test.go` 3 用例全绿（`ok Fnos.VC_Service/pkg/winapi`），其中 `TestAssignProcessToJob_RealChildProcess` 以真实子进程验证「分配成功 → `IsProcessInJob=true` → `TerminateJob` 连带结束」。
> ③ **交付包**：FVCC 前端 `npm run build`（`tsc -b && vite build`，30 modules）全绿 → `index-BvU2m5Tb.js`；后端 `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build` 重编译 → `app/fvcc`（33,758,575 B）；`fnpack-1.2.1.exe build` 产出 **`FVCC_v1.2.1_fnos_x86.fpk`**（17,697,910 B，manifest `version = 1.2.1`），包内 `app.tgz` 已确认含新 `fvcc` 与 `ui/assets/index-BvU2m5Tb.js`；包已同时落 `FVCC\` 与 `FVCS\`，供 NAS 安装/升级。
> ④ **方案决策（变更项）**：**EasyTier 方案 A/B/C 全部作废**，不再引入虚拟组网；**FVCS 保持独立应用、手动启动**（不注册系统服务/计划任务），B-1（服务化）待办随之关闭。
> ⑤ **遗留与限制**：`pkg/smb`、`pkg/server` 用例在 `CGO_ENABLED=0` 下因 go-sqlite3 需 cgo 而失败（本机无 gcc），属既有环境限制、非本轮改动引入；`pkg/winapi`/`pkg/ffmpeg`/`pkg/config`/`pkg/protocol`/`pkg/task` 全绿。ffmpeg Job 告警是否真正消除，仍需 NAS 真机转码日志复核。

### 5.0 本轮交付物与验证证据（2026-09-16）

| 项 | 产物 / 用例 | 结果 |
|---|---|---|
| FVCC 后端修复 | `server/handlers_edl.go`（`listEDLProjects` 下发 `items` + `projects` + `total`） | `go test ./... -count=1` → `ok fvcc 5.643s`（0 FAIL） |
| FVCC 回归用例 | `server/handlers_edl_list_contract_test.go`（2 用例） | `TestEDLProjectListFieldsContract` / `TestEDLProjectListAfterReload` 均 PASS |
| FVCC 前端修复 | `ui-src/src/api.ts`、`ui-src/src/pages/editor/index.ts`（`r.projects ?? r.items ?? []`） | `npm run build` 全绿，产出 `index-BvU2m5Tb.js` |
| FVCC 端到端复核 | 本机 dev 服务（`-dev`，临时数据目录）两轮生命周期 | 重启后 `items=2 / projects=2 / total=2`；同名新建 409 |
| FVCS Job 修复 | `FVCS/pkg/winapi/winapi.go`（`OpenProcess` 真句柄 + `CloseHandle` + `IsProcessInJob` + `SetInformationJobObject` 校验） | `go build ./pkg/...` 通过 |
| FVCS 回归用例 | `FVCS/pkg/winapi/winapi_test.go`（`TestCreateJobObject_Success` / `TestAssignProcessToJob_InvalidArgs` / `TestAssignProcessToJob_RealChildProcess`） | 3/3 PASS，`ok Fnos.VC_Service/pkg/winapi` |
| 交付包 | `FVCC_v1.2.1_fnos_x86.fpk`（17,697,910 B） | 落 `D:\Fnos.VideoConversion\FVCC\` 与 `D:\Fnos.VideoConversion\FVCS\`；**已被第十七轮新包（17,708,825 B）取代，勿再分发，见 §5.0B** |
| 台账/待办 | `D:\Fnos.VideoConversion\FVCS\下一步.md`（已完成 + 待办 + EasyTier 作废） | 已更新 |

### 5.0B 第十七轮交付物与验证证据（2026-09-16）

| 项 | 产物 / 用例 | 结果 |
|---|---|---|
| FVCS 版本对齐 | `pkg/version/VERSION` + `pkg/version/version.go`（go:embed）+ `pkg/server/hello.go`（`var agentVersion = version.Version`）+ `cmd/service/main.go`（启动打印 version） | 二进制 `1.2.1` × 10 / `1.2.0` × 0；`TestVersionEmbeddedFromFile`、`TestHelloPushFrameContract`、`TestHelloPushFrameFieldNames` 3/3 PASS |
| FVCS 运行时核验 | 替换 `D:\FVCS\fvcs-service.exe` 后重启（新 PID 28980） | 日志 `FVCS Service starting... version=1.2.1`；`WS:8080 / HTTP:10000 / IPC:5000` 均 LISTENING；`Auto start completed` |
| FVCC 断链自愈 | `server/remote.go`（Connect 单飞 + `reconnectLoop` 指数退避 1s→30s + `gracefulCloseWS` + 关闭先停重连） | `gofmt -l` 空；`go test ./... -count=1` 全绿 |
| FVCC 回归用例 | `server/remote_reconnect_test.go`（`TestB09*` 4 组） | 4/4 PASS（单飞 1 次拨号 / 自动重连 / 发 Close 帧 / 主动关闭停重连） |
| 交付包（第十七轮） | `FVCC_v1.2.1_fnos_x86.fpk`（17,708,825 B，SHA256 `74B77404…E4638C`） | 落 `D:\Fnos.VideoConversion\FVCC\` 与 `D:\Fnos.VideoConversion\FVCS\`（包内仅 `fvcc` 变化，UI/cmd/config 零改动）；**已被第十八轮 `FVCC_v1.2.2_fnos_x86.fpk`（17,708,933 B）取代，勿再分发，见 §5.0C** |
| 回滚备份 | 中间产物 `rollback_20260916\`（旧 fpk / 旧 app_fvcc / 旧 fvcs-service.exe） | 已留存，可随时回退 |
| 台账/待办 | `D:\Fnos.VideoConversion\FVCS\下一步.md` 增补本轮三项（版本对齐 / WS 自愈 / 重出包）并追加 3 条 NAS 复核待办 | 已更新 |

### 5.0C 第十八轮交付物与验证证据（2026-09-16）

| 项 | 产物 / 用例 | 结果 |
|---|---|---|
| 版本号全链路同步 | `FVCC/manifest`、`FVCC/ui-src/package.json`、`package-lock.json`、新增 `FVCC/server/VERSION`+`version.go`（`go:embed` → `appVer`，删 `main.go` 硬编码 `1.0.0`）、`FVCS/pkg/version/VERSION`、`cmd/fvcs-cli/main.go` | 全链路 `1.2.2`，无残留业务版本字面量 |
| 防漂移用例 | `FVCC/server/version_test.go`（`TestAppVerEmbeddedFromFile` / `TestAppVerMatchesPackagingFiles`）、`FVCS/pkg/version/version_test.go`（断言 1.2.2） | FVCC `go test ./...` → `ok fvcc 10.623s`；FVCS `./pkg/version/ ./pkg/server/` 全绿 |
| FVCS 服务端更新 | go1.24.6 + `CGO_ENABLED=1` 重编译 → `D:\FVCS\fvcs-service.exe`（25,734,551 B，与 1.2.1 版同字节数）+ `fvcs-cli.exe`（3,541,504 B） | 重启后 PID 29288；`FVCS Service starting... version=1.2.2`；`WS:8080 / HTTP:10000 / IPC:5000` LISTENING；`fvcs-cli version` → `fvcs-cli 1.2.2 (M3)` |
| 交付包（最新） | **`FVCC_v1.2.2_fnos_x86.fpk`（17,708,933 B，SHA256 `FF01333C…93EBC3`，manifest `version=1.2.2`）** | 双处落盘（`FVCC\` 与 `FVCS\`），SHA256 一致；包内差分仅 `fvcc` 与 `manifest` 变化 |
| 回滚备份 | 中间产物 `rollback_1.2.2\`（1.2.1 版 exe×2 + 1.2.1 fpk） | 已留存，可随时回退 |
| 台账/待办 | `D:\Fnos.VideoConversion\FVCS\下一步.md` 增补本轮版本递增条目 + 待办目标版本改 v1.2.2 | 已更新 |

> **结论**：A/B/C/M4/D **五组代码实现已全部落地并通过编译/单测**（A 组 7/8 代码层收口、B 组 9/9、C 组 10/10、M4 2/2，历史见 §3 第二~第十轮；D 组 4/4 于 2026-09-14 第十一~十四轮收口，M3 + E2E-08 HTTP 层于 2026-09-15 第十五轮补完）——**代码待办清单已完全清空**（D 组唯一加固项 `config.go:55` 硬编码 `encryptKey` 的 M3 迁移已落地：DPAPI 主密钥 + `fvcs-cli reencrypt`）。剩余工作仅两类：①**真机联调验收**（§4 全部条目：T6~T12、S1~S15/E2E-08 真机复现、A-06 重启续跑、A-07 WS 端到端、B-09 权限位）；②**E 组部署与 E2E 验收**。**下一轮建议首选 P1（C 组 + M4 真机联调）**，需要 SMB/UNC 素材 + ffmpeg 在环 + FVCS 以 `FVCC_DEV=1` 启动。

### 5.1 当前真实待办（按优先级）

| 优先级 | 事项 | 具体内容 | 前置 / 阻塞 |
|---|---|---|---|
| P0 | **NAS 升级最新 FVCC 包 + 缺陷复核** | 用**最新** `FVCC_v1.2.2_fnos_x86.fpk`（17,708,933 B，SHA256 `FF01333C…93EBC3`，见 §5.0C）覆盖 NAS 上的旧包（1.2.0 / 1.2.1 已作废）；升级后复核：① `D:\FVCS\logs\app_2026-09-16.log` 出现 `Hello pushed to client … agentVersion=1.2.2`（版本对齐生效）；② 前端「设置」页版本与 FVCC 启动日志显示 `1.2.2`（`appVer` 由 `server/VERSION` embed，不再硬编码 1.0.0）；③ WS 断链后**自动重连**、无需人工点「测试连接」，且不再出现 `close 1006`（自愈生效） | 需用户侧操作 NAS 安装/升级 |
| P1 | **C 组 + M4 真机联调验收** | §4 T10 预览拖拽（206/`Content-Range`、换素材重签票、过期重试一次、并发超限 429）、T11 缩略图实际抽帧（320×180，缺失回落占位）、T12 代理端到端（`/proxy`→FVCS 生成→校验登记 `ready`→WS `proxy_ready`；不符判 `E_PROXY_VERIFY_FAILED` 落 `invalid`；同素材复用 `taskId`） | 需 SMB/UNC 素材 + ffmpeg 在环 + FVCS 启动 |
| P2 | **A 组剩余真机验证** | §4 T6 可播性、T8 卡死保护 `E_TIMEOUT_STALL`、T9 并发排队、A-03 手工 40 段拼接、A-04 时长误差 ≤0.5s、A-05 无 NVENC 降级、**A-06 断点续跑（杀进程重启不丢任务、不重复渲染，08 E2E-05）**、A-07 WS 端到端（`Hello`/`Progress`） | 同上 |
| P3 | **A 组剩余代码缺口（先补代码）** | ① **A-06 断点续跑（resume）实现 + 单测 — 已完成（2026-09-13 第十轮）**；② A-07 的手工联调随 P2 一并做 | ① 已闭环（见 §3 第十轮）；② 依赖真机 |
| P4 | **D 组安全与凭据（4/4，代码层完成）** | ✅ **D-01 已完成（2026-09-14 第十一轮）**：`FVCS/pkg/protocol/edl_validate.go` + 共享向量 + 6 顶层 46 子例全绿。✅ **D-02 已完成（2026-09-14 第十二轮）**：`FVCC/server/edl_validate.go` 同源复刻（加载同一份向量）+ `PathValidator.ValidateRel` L1~L4 + 渲染提交三闸门接入（严格素材预检 / 输出名白名单 / 载荷合规）+ 共享根映射层 `toShareRelRoot`，14 个 `TestD02*` 全 PASS、`go build/vet/test ./...` 三项全绿。✅ **D-03 已完成（2026-09-14 第十三轮）**：`FVCS/pkg/smb/credentials.go`（DPAPI 密文档案 + Scope/目标校验 + 被占用删除拒 + 180 天审计）+ `FVCS/pkg/server/local_admin.go`（`/local/credentials` 环回接口，非环回 403，永不回显口令）+ 三处任务创建前置拒单（`precheckCredentialTarget`），13 + 7 用例全绿。✅ **D-04 已完成（2026-09-14 第十四轮）**：FVCC 安全响应头（全链路 nosniff/no-referrer/SAMEORIGIN）+ 三档限流（鉴权失败冷却 / 渲染提交 / 代理提交）+ `security_audit.go`（`edlErr` 集中记账、5min 阈值 3/50 告警、Detail 脱敏），FVCS `pkg/server/audit.go`（七类动作落 `audit_log` + 同阈值告警 + 库不可用降级），FVCC 13 个 + FVCS 5 个 `TestD04*` 全绿（「`remote.go` 停止下发凭据」随 B-06 先行）。**后续仅剩加固项**：`config.go:55` 硬编码 `encryptKey` 的 **M3 迁移**（DPAPI 主密钥 + `fvcs-cli reencrypt`）→ **已于 2026-09-15 第十五轮完成**（`FVCS/pkg/config/reencrypt.go` + `fvcs-cli reencrypt/status`，幂等 + `-dry-run`，旧密钥仅识别 legacy 密文）；D 组至此代码层无欠账 | D-01/D-02 需独立评审人（08 §4.4 注）；真机 S1~S15 随 P1/P6 |
| P5 | **B 组权限真机验证** | §4「权限位真机验证」：FNOS 网关透传非 admin 时 `/render`、`/proxy`、`DELETE` 类返回 403 `E_FORBIDDEN`，admin/root 与独立模式放行 | 需 FNOS 网关在环 |
| P6 | **E 组部署与验收** | E-01 打包部署（`fnpack` + 静态资源落盘）、E-02 E2E 8 场景、E-03 稳定性演练、E-04 验收报告 | 依赖 A~D 全部收口 |

### 5.2 A 组剩余代码缺口（仅剩一处；A-01~A-06、A-08 已完成）

- **A-06**：**已完成（2026-09-13 第十轮）**。除既有 `CreateRenderEDLTask`/`executeRenderEDL`/`setRenderProfile`/`taskWorkingDir`/`resolveOutputUNC`/`previewFastCopy`/`makeSrcResolver` 与 workDir 终态清理外，本轮补齐 **断点续跑（resume）**、**workDir 复用/孤儿清理** 与 **`pkg/task` 单测**（8 个 `TestA06*` 全绿）；剩余仅真机重启验证（§4「断点续跑」）。
- **A-07**：`pkg/server` 的 `CreateRenderEDL`/`CreateGenProxy` 分支、`hello.go` 的能力上报已就绪；**缺**真机 WS 端到端手工联调（随 P2）。

### 5.3 已完成主线（历史，仅供追溯）

- **A 组 7/8（代码层）**：A-01~A-06、A-08 完成；**A-06 断点续跑 + workDir 复用/孤儿清理 + `pkg/task` 单测于 2026-09-13 第十轮补齐**（8 个 `TestA06*` 全绿，逐条见 §2A + §3 第十轮）；A-07 仅剩 WS 真机联调。
- **B 组 9/9**：B-01~B-09 全部完成（逐条见 §2B + §3 第二~第八轮）；`TODO(B-09)` 清零；权限挂载 `TestB09*` 3 用例全绿。
- **M4 2/2**：M4-A/M4-B 代码落地（逐条见 §2D + §3 第九轮），C 组联调阻塞解除。
- **C 组 10/10**：C-01~C-10 完成并装配进 `renderEditor`（逐条见 §2C）；`npm run build` 全绿，待真机联调。

---

### 附：历史推进建议（已失效，仅供追溯）——原「路线 A（推荐，P0 主线关键路径）：先做 B 组」

B 组是 C 组的前置（C-08 提交依赖 B-04），且 A 组剩余缺口（续跑/联调）也依赖 B 组下发链路，故优先。（该建议已随 B 组 9/9 收口而失效，下表仅作历史对照；当前待办见 §5.1。）

| 顺序 | ID | 状态 | 首个子任务输入/输出 |
|---|---|---|---|
| 1 | B-01 | 完成 | `store.go`：`tasks` 列迁移 + 四集合建表 |
| 2 | B-02 | 完成 | 项目 CRUD + `rev` 乐观锁（03 §4.3）+ 派生字段加载重算 |
| 3 | B-03 | 完成 | `/api/edl/projects*` 5 条路由 + 契约级闸门 + 用例 |
| 4 | B-04 | 完成 | `renderEDLProject`：`POST /edl/projects/:id/render`，请求 `{presetKey, outputName, serverId?, force?}`；响应 `{taskId, status, serverId, output, totalMs, fastCopyAllowed}`；幂等键 = clips+profile 规范化 sha256 前 16 位（与 FVCS 同形，**不含 output**），命中进行中/成品仍在 → 复用同一 `taskId`；`outputName` 不含扩展名、重名追加 `_1`/`_2` |
| 5 | B-05 | 完成 | `scheduler.go`：`handleRenderEDL`/`handleGenProxy` 分流 + 冷却到期扫描 + 代理低优先级（退避 30/60/120/240/480s）；实现方式：`RenderDispatcher` 接口（B-06 注入）+ `dispatchRenderLike` + `promoteCooldownTasks` + `sortQueueTasks`/`nextQueueTask`；用例 `scheduler_edl_test.go` 6 个全绿 |
| 6 | B-06 | 完成 | `remote.go` 实现 `RenderDispatcher`（`CreateRenderEDL`/`CreateGenProxy`，WS 下发 `cmd=CreateRenderEDL`/`CreateGenProxy`，**仅传 `credentialId`** 禁传密码）+ `main.go` 装配 `SetRenderDispatcher`/`SetSettingsProvider`；用例 `remote_edl_test.go` 4 用例 / 7 子例；判据：未装配时渲染任务保持 QUEUE（B-05 保护已在），装配后可派发到在线节点 |
| 7 | B-07 | 完成 | `ws.go`：`BroadcastTaskUpdateFull` + 事件聚合（500ms 同任务聚合、进度未变不广播、终态直发；`node_status` 20 分档 + `proxy_ready` 同 assetId 10s 去重）+ `remote_task_id` 反查本端任务（兜底本端 ID）+ `store_edl.go` 的 `ApplyRenderProgress` 落库（progress 单调、其余空值保持）+ `main.go`/`remote.go` 进度回传接线；用例 `ws_edl_test.go` 4 个 + `ws_progress_edl_test.go` 3 个全绿 |
| 8 | B-08 | 完成 | 选机 `pickNode` + 健康分 + 熔断 + `Hello` 能力落库（`node_select.go` 455 行 + `node_select_test.go` 10 用例全绿）；判据：未指定 `serverId` 时按 `0.5/0.3/0.2` 打分自动选机并真实下发，全忙退化排队，无候选保持 QUEUE（不耗重试）；`GEN_PROXY` freeSlots 扣减与 `healthScore` 真实取值已一并收口 |
| 9 | B-09 | 完成 | `router.go` 7 条 `DELETE` 路由 + `POST /edl/projects/:id/render` 挂 `requireAdmin()`（403 对齐 `E_FORBIDDEN`）；用例 `TestB09RenderAndDeleteRequireAdmin` / `TestB09ProjectReadWriteOpenToReadonlyUser` / `TestB09StandaloneModeBypassesAdmin` 全绿 |

B-08 已完成：`resolveRenderNode` 的 `serverId==""` 自动选机分支、健康分、熔断、`Hello` 能力落库全部落地，B-05/B-07 遗留的两处开关（`GEN_PROXY` freeSlots 扣减、`healthScore` 真实取值）同步收口，B 组进度 8/9。进度回传闭环（FVCC 调度 → WS `CreateRenderEDL` → FVCS 执行 → WS `Progress`/`Hello` → FVCC 落库 + 聚合广播）在代码与单测层面已通，**A-06 断点续跑** 与 **A-07 WS 联调** 现已具备端到端触发条件（以下为历史建议，已失效）当时的下一步为 **B-09 权限中间件**（`requireAdmin` 挂载），其后转 C 组前端剪辑页。**2026-09-12 更新（历史）：B-09 已完成，`TODO(B-09)` 清零，B 组 9/9 收口——下一步为 C 组（C-01 `types.ts`/`api.ts` 扩展 EDL/Project/RenderTask 类型）或 D 组（D-02/D-03 安全与凭据）。**

> **历史记录（保留证据链；其中「下一步」描述已于 2026-09-13 失效，C-01 亦已完成，见 §2C/§5.3）**：**2026-09-13 更新：M4（预览网关 + 代理工作流，04 号文档）已落地并全绿，C 组对 M4 后端的硬依赖阻塞解除（见 §3 第九轮）。当时的下一步优先级为：① C 组真机联调验收（§4 T10~T12）；② A 组剩余真机验证（T6/T8/T9）；③ D 组安全与凭据（D-01~D-04）。**

### 附：历史推进建议（已失效，仅供追溯）——原「路线 B（想先看到界面）：先做 C 组」

`FVCC/ui-src` 已具备 Vite + TS + Tailwind 骨架与 6 个既有页面（`pages/`），可直接扩展：

| 顺序 | ID | 首个子任务输入/输出 |
|---|---|---|
| 1 | C-01 | `src/types.ts`/`api.ts` 扩展 EDL、Project、RenderTask、stream ticket 类型；判据：字段与 03 §2 一一对应（含毫秒约定） |
| 2 | C-02 | 路由 `#/editor/:projectId` + 三段布局 + 拖拽持久化（`wve.layout.v1`）；判据：01 §4.1 布局 |
| 3 | C-03 | `editorStore.ts`：EDL 状态机 + 撤销栈（≤50）+ 防抖自动保存（2s） |

**C 组实际落地记录（本次续做，代码已装配进 `pages/editor/index.ts::renderEditor`）**

| ID | 文件 | 落地要点 |
|---|---|---|
| C-04 | `editor/assets.ts` | 目录流式扫描 + 素材列表；新增可选 `onPick`（单击 = 装配预览器并 seek 到 0），与双击/拖拽整段添加并存 |
| C-05/C-06 | `editor/preview.ts`、`editor/format.ts` | 预览器 + 打点（in/out 恒按源素材时间轴）；`/stream/ticket` 票据起播、`/stream` Range 拖拽（**M4 路由 2026-09-13 已就绪，待真机联调**） |
| C-07 | `editor/ruler.ts`、`editor/timeline.ts`、`editor/clipOps.ts` | Ruler 映射 + 轨道拖放/换位/裁剪，工具条渲染方案下拉改接 `preset.ts` |
| C-08 | `editor/preset.ts`、`editor/dialog.ts` | `presetKey` 渲染方案枚举（与 `handlers_render.go` 的 `edlRenderPresetTable` 对齐，排除代理专用项）；弹窗提交前强制 `flushSave`，支持 `force`、指定 `serverId` |
| C-09 | `editor/taskDrawer.ts`、`pages/tasks.ts` | 顶栏入口 → 右侧任务抽屉（关闭不销毁，`renderTasks` 返回退订句柄）；任务卡片按 `taskView.stageText/progressLabel` 展示阶段与段序号，终态提供成品预览与路径复制；`onRequestProxy` → `POST /api/proxy` |
| C-10 | `editor/shortcuts.ts` | 01 §6 全表快捷键（Space/I/O/←→/,./Delete/Ctrl+S/Ctrl+Enter/Ctrl+Z），弹窗激活时屏蔽剪辑键 |

**阻塞项（已解除，2026-09-13）**：原先 `FVCC/server/router.go` 仅注册 `/edl/*` 与 `/ws`，
`/stream`、`/stream/ticket`、`/thumb`、`/proxy` 四条路由缺失 → 预览起播、素材缩略图（`api.thumbUrl`）、代理下发三处前端已按
02 §7、04 §2 契约实现但无法联调。**第九轮（M4）已补齐这四条路由**（含 `/proxy` 挂 `requireAdmin`、`/stream` 支持 `HEAD`），
C 组随之从「部分完成（待 M4 后端）」转为「代码完成（待真机联调）」，验收口径见 §4 T10~T12。

交互与时间线实现可参考 `D:\Fnos.VideoConversion\freecut`（Vite + TS 前端工程，含时间线/播放器实现与工程规范）。

### 无论走哪条路线，都需先锁定的接口契约

- `03-EDL数据模型与接口契约.md` §2（字段与毫秒约定）、§4（接口签名与示例）
- `05-RenderEDL渲染引擎与FFmpeg命令构造器设计.md` §4（命令构造仅收结构化载荷、输出 `[]string`、路径校验）
- `07-安全校验与凭据管理细则.md`（下发仅 `credentialId`，禁传密码）

---

## 6. 命令速查

```powershell
# FVCS 内核校验
cd D:\Fnos.VideoConversion\FVCS
$env:GOTOOLCHAIN='local'
$go='C:\Users\xx318\go\pkg\mod\golang.org\toolchain@v0.0.1-go1.24.6.windows-amd64\bin\go.exe'
& $go vet ./pkg/...; & $go build ./pkg/...; & $go test ./pkg/... -count=1 -v

# 仅跑 A-06 断点续跑 / 孤儿 workDir 清理用例（pkg/task 首个测试文件）
& $go test ./pkg/task/ -run 'TestA06' -count=1 -v

# 仅跑 EDL 用例
& $go test ./pkg/ffmpeg/ -run 'TestT1|TestT2|TestT3|TestT4|TestT7|TestPayloadGate|TestA03' -v

# FVCC 调度端校验（本机可用工具链；`D:\fntv-tools\go\bin\go.exe` 已不存在）
cd D:\Fnos.VideoConversion\FVCC\server
$env:GOTOOLCHAIN='local'
$go='C:\Users\xx318\sdk\go1.25.3\go\bin\go.exe'
& $go build ./...; & $go vet ./...; & $go test ./... -count=1
# 仅跑 EDL 项目用例
& $go test . -run 'TestEDLProject' -v
# 仅跑 B-04 渲染提交用例
& $go test . -run 'TestB04' -v
# 仅跑 B-05 调度分流与冷却用例
& $go test . -run 'TestB05' -v
# 仅跑 B-06 渲染下发用例
& $go test . -run 'TestB06' -v
# 仅跑 B-07 WS 聚合与进度回传用例
& $go test . -run 'TestB07' -v
# 仅跑 B-08 选机/健康分/熔断/Hello 用例
& $go test . -run 'TestB08' -v
# 仅跑 M4 预览网关 + 代理工作流用例
& $go test . -run 'TestM4' -v

# 前端（FVCC UI）
cd D:\Fnos.VideoConversion\FVCC\ui-src
npm run build     # tsc -b && vite build
```

---

## 7. 设计文档索引

| 文件 | 内容 |
|---|---|
| `01-产品需求与交互规格.md` | 需求、US 场景、布局与快捷键 |
| `02-前端架构与时间线编辑器设计.md` | 前端架构、时间线编辑器 |
| `03-EDL数据模型与接口契约.md` | EDL 数据模型、接口契约（字段/毫秒约定） |
| `04-预览网关与代理工作流设计.md` | 预览、代理生成（GenProxy） |
| `05-RenderEDL渲染引擎与FFmpeg命令构造器设计.md` | 三阶段命令构造、硬编协商、§9 测试口径 |
| `06-调度持久化与渲染节点管理设计.md` | 调度、持久化、节点管理 |
| `07-安全校验与凭据管理细则.md` | 安全校验、凭据管理 |
| `08-P0实施计划与验收清单.md` | **计划与验收口径（A/B/C/D 组）** |
| `09-附录-部署与迁移说明.md` | 部署与迁移 |
| `10-实施进度与下一步.md` | **本文件：进度台账** |









# FVCS 下一步

当前日期：2026-09-16

## 已完成

- [x] FVCC 交付包版本号更新为 1.2.0，新包 FVCC_v1.2.0_fnos_x86.fpk（8,420,942 B）
- [x] FVCS 二进制 + 配置落地 D:\FVCS\，ffmpeg_path 指向 D:\YTSage\ffmpeg.exe
- [x] 素材上机（5 段，约 937 MB），写入权限已解除
- [x] Guest 凭据录入 FVCS（vmware-guest），scope 覆盖四目录
- [x] 防火墙白名单：8080/10000 仅放行 192.168.1.106
- [x] FVCS SMB 实跑一条转码任务成功（task_1789516469113369600，h264_nvenc 硬编、成品回写 exports）
- [x] （2026-09-16）修复 FVCS ffmpeg 子进程未纳入 Job 对象告警：`pkg/winapi` 改用 `OpenProcess` 取真实句柄 + `defer CloseHandle`，新增 `IsProcessInJob` 复核，`CreateJobObject` 校验 `SetInformationJobObject` 返回值；新增 `winapi_test.go` 3 用例全绿（含 TerminateJob 连带结束子进程的回归用例）
- [x] （2026-09-16）修复 FVCC 剪辑项目列表不显示历史项目 BUG（后端 `listEDLProjects` 同时下发 `items` 与 `projects`/`total`，前端 `api.ts`/`editor/index.ts` 兼容解析），新增 2 个回归用例全绿；前端已重新构建、后端已交叉编译（linux/amd64）并重打包
- [x] （2026-09-16）新交付包 **FVCC_v1.2.1_fnos_x86.fpk**（17,697,910 B，manifest version 1.2.1），已同时落 `D:\Fnos.VideoConversion\FVCC\` 与 `D:\Fnos.VideoConversion\FVCS\`
- [x] （2026-09-16）**修复 agentVersion 版本不一致**：新增 `pkg/version/VERSION`（1.2.1）+ `pkg/version/version.go`（`go:embed` 注入 `Version`），`hello.go` 由 `const agentVersion = "1.2.0"` 改为 `var agentVersion = version.Version`；`cmd/service/main.go` 启动日志打印 `version=%s`。新增 `version_test.go`、`hello_push_test.go` 契约用例全绿；`TestVersionEmbeddedFromFile` / `TestHelloPushFrameContract` / `TestHelloPushFrameFieldNames` PASS
- [x] （2026-09-16）**修复 FVCC↔FVCS WebSocket 频繁断连（约 15s 一次）**：FVCS 侧核查无任何 15s 级主动断链逻辑（`wsReadTimeout=120s` / `wsPingInterval=30s` / `wsHeartbeatTimeout=150s`），断链由客户端/网络中继侧发起；FVCC 侧 `remote.go` 原有缺陷为「连接不可自愈 + 无单飞 + 关闭不发 Close 帧」。修复：①`Connect` 单飞（`connMu` + 参数快照 + `dialAndAuth`），避免一次请求风暴打出多条 WS（FVCS 侧 `ws_conns=3` 上限易被顶满）；②新增 `reconnectLoop` 指数退避重连（1s→30s）；③读错触发自动重连，主动关闭走 `gracefulCloseWS`（发 Close 帧，消除 close 1006）；④`CloseConn`/`CloseAll` 先停重连再关闭。新增 `remote_reconnect_test.go`（单飞只拨号 1 次 / 异常断链自动重连 / 优雅关闭发 Close 帧 / 主动关闭停重连）`TestB09` 系列全绿，`go test ./...` 全绿
- [x] （2026-09-16）**重新出包**：FVCS 以 go1.24.6 + `CGO_ENABLED=1` 重编译（25,734,551 B，二进制含 `1.2.1` 10 处、`1.2.0` 0 处），已替换 `D:\FVCS\fvcs-service.exe` 并重启（PID 28980，启动日志 `FVCS Service starting... version=1.2.1`，WS:8080 / HTTP:10000 / IPC:5000 正常）；FVCC 以 go1.27.1 + `CGO_ENABLED=0` 交叉编译 linux/amd64（33,814,287 B）后 `fnpack build` 重打包 → **FVCC_v1.2.1_fnos_x86.fpk 17,708,825 B（SHA256 74B77404…E4638C）**，已覆盖 `D:\Fnos.VideoConversion\FVCC\FVCC_v1.2.1_fnos_x86.fpk` 与 `D:\Fnos.VideoConversion\FVCS\FVCC_v1.2.1_fnos_x86.fpk`（旧包回滚副本在中间产物 `rollback_20260916\`）
- [x] （2026-09-16）**全局版本递增 1.2.1 → 1.2.2 并重出包**（fnos 限制同版本号不可重复安装，故版本必须递增且全链路同步）：①版本源同步——`FVCC/manifest` 1.2.2、`FVCC/ui-src/package.json` 1.2.2（原 1.2.0）、`package-lock.json` 两处 1.2.2、新增 `FVCC/server/VERSION`+`version.go`（`go:embed` → `appVer`，**删除 `main.go` 硬编码 `appVer="1.0.0"`**）+ `version_test.go`（校验与 manifest/package.json 一致）、`FVCS/pkg/version/VERSION` 1.2.2、`version_test.go` 断言 1.2.2、`cmd/fvcs-cli/main.go` 由硬编码 `fvcs-cli 1.0.0` 改为 `version.Version`；②FVCS 以 go1.24.6 + `CGO_ENABLED=1` 重编译（25,734,551 B，与 1.2.1 版字节数相同），已替换 `D:\FVCS\fvcs-service.exe` 并重启（PID 29288，启动日志 `FVCS Service starting... version=1.2.2`，WS:8080 / HTTP:10000 / IPC:5000 均 LISTENING），`fvcs-cli.exe` 同步更新（`fvcs-cli version` → `fvcs-cli 1.2.2 (M3)`）；③FVCC 前端 `npm run build` + 后端 go1.27.1 交叉编译 linux/amd64（33,814,772 B）→ `fnpack build` → **FVCC_v1.2.2_fnos_x86.fpk 17,708,933 B（SHA256 FF01333C…93EBC3，manifest version=1.2.2）**，已同时落 `D:\Fnos.VideoConversion\FVCC\` 与 `D:\Fnos.VideoConversion\FVCS\`；包内差分仅 `fvcc` 与 `manifest` 变化，`ui/`、`cmd/`、`config/`、`ICON*.PNG` 完全一致（旧包/旧二进制回滚副本在中间产物 `rollback_1.2.2\`）

- [x] （2026-09-16）**修复剪辑渲染（RenderEDL）混源拼接必失败缺陷 + 实跑验证剪辑链**：RenderEDL 混源用例（h264+h264+hevc 三片段）稳定 `Failed(10%)`，日志 `[Parsed_fps_10] Media type mismatch … concat input pad 1 (audio)`、ffmpeg `exit status 0xffffffea(-22)`。根因：`pkg/ffmpeg/edl.go` `buildFilterGraph()` 的 concat 输入标签排列为「全部视频在前」（`[v0][v1][v2][a0][a1][a2]`），而 `concat=n:v=1:a=1` 要求按片段交错（`[v0][a0][v1][a1][v2][a2]`）；隔离实验复现 -22 与逐字报错、修正后退出码 0。修复：新增 `joinInterleaved()`（无音轨退化 `joinLabels`），`flush()` 批内 + 多批次收尾两处 concat 均改交错；顺带去除视频链重复 `setsar=1`。新增 `edl_concat_order_test.go`（三片段交错+扁平负向、无音轨、40 段批内/批间交错、`joinInterleaved` 单测），更新 `edl_test.go` 批量断言；`go test ./...` 全绿。以 go1.24.6 + `CGO_ENABLED=1`（gcc=`C:\msys64\mingw64\bin`）重编译 25,736,148 B，旧二进制备份 `D:\FVCS\backup\fvcs-service_1.2.2_pre_concatfix.exe.bak`，替换后重启（**PID 29288 → 31276**，启动日志 `version=1.2.2`、`Hello pushed … agentVersion=1.2.2`，8080/10000/5000 均 LISTENING）。**修复后混源实跑 PASS**：`task_1789541911767966400`（14:58:31 创建 / 14:58:38 完成，约 6.2 s，无错误码），成品 `exports\fvcs_render_edl_mixed_20260916_145829.mp4` = h264 High 1920×1080 30fps / aac 48kHz 双声道 / 16.016 s / 28,937,424 B（SHA256 `BE6AC0A7…2A81`）；**同源回归 PASS** `task_1789541980237113600`（7.893 s），产物与修复前**字节级一致**（18,492,209 B / `7A5E441C…E34D`）证明无回归。版本号保持 1.2.2（FVCS 为手动启动的独立应用，本次未改交付包内容），建议下次出包统一递增 1.2.3。验证报告：`output\FVCS_剪辑渲染链验证报告_20260916.md`

## 待办

- [ ] （可选）`QueryTask` 终态失败时回传错误码/错误摘要（现仅 `Status=Failed`，失败原因只能翻 `D:\FVCS\logs\`）；建议 `GetTaskResponseData` 增加 `ErrorCode`/`ErrorMessage` 便于 NAS 面板展示
- [ ] NAS 安装/升级 FVCC 至 **v1.2.2**（用户自行操作，请用最新 `FVCC_v1.2.2_fnos_x86.fpk`，17,708,933 B，SHA256 `FF01333C…93EBC3`；1.2.0/1.2.1 旧包均已作废）
- [ ] NAS 升级后核对 `D:\FVCS\logs\app_2026-09-16.log` 中 `Hello pushed to client … agentVersion=1.2.2`（验证版本对齐生效）
- [ ] NAS 升级后观察 WS 连接是否仍有 ~15s 断链（期望：断链后自动重连、不再需要手动「测试连接」）
- [ ] 从 NAS 192.168.1.106 验证 FVCS 8080/10000 连通性
- [ ] FVCC 注册渲染节点 → FVCS
- [ ] 跑 P1 联调：T10/T11/T12
- [ ] ~~处理 EasyTier 通配规则（方案 A/B/C 由项目侧决策）~~ → **2026-09-16 决策：EasyTier 方案（A/B/C）全部作废**，不再引入虚拟组网；FVCS 保持独立应用、手动启动（不注册系统服务/计划任务）
- [ ] 考虑用专用服务账号替代 Guest
- [ ] NAS 实跑复核 ffmpeg Job 告警是否消除（本机已单测验证，需真机转码日志确认）




# Fnos.VideoConversion 项目分析与改进方向

> **文档快照漂移说明（P3-2）**：快照基线 2026-09-17（应用版本 1.4.0 / go 1.27.1）；2026-09-19 更新至 1.4.4 基线并回写各条目落实状态（P0-1 / P1-3 / P2 组详见 `docs/IMPROVEMENT_LOG.md`、`docs/FVCC_混乱度评价报告_细化版.md`）。本文档为改进方向索引，各条目落实状态以代码与 `docs/SECURITY.md`、`BUILD.md` 等配套文档为准；源码变动后需回写本文档。版本一致性可用 `scripts/check-versions.ps1` 校验。

> 生成日期：2026-09-17
> 分析范围：FVCC（NAS 调度端 Web UI）+ FVCS（Windows 渲染端）+ freecut（独立浏览器剪辑编辑器）
> 文档性质：项目现状基线 + 改进方向清单，供后续开发与版本规划参考

---

## 1. 项目定位

Fnos.VideoConversion 是一套**双端远程视频转码与剪辑系统**：

- **FVCC（调度端）**：部署于 NAS，以 Web UI 形式提供转码任务调度、素材管理与后续 Web 剪辑入口；
- **FVCS（渲染端）**：部署于局域网内的高算力 Windows 主机，负责接收任务并调用 FFmpeg 完成转码/渲染，成品回写 NAS 共享目录；
- **freecut**：仓库内含的独立浏览器剪辑编辑器（上游参考项目），提供完整的 Web 端视频编辑能力。

架构要点：素材不离开 NAS，渲染节点通过 SMB 直挂共享目录读写素材与成品，免分片上传/下载链路。

---

## 2. 技术栈

| 模块 | 技术栈 | 备注 |
|---|---|---|
| FVCC 后端 | Go 1.27.1 + gin + gorilla/websocket | 无 CGO，交叉编译 linux/amd64 |
| FVCC 前端 | TypeScript 7 + Vite + Tailwind CSS | 纯手写 DOM（无框架） |
| FVCS | Go 1.27.1 + CGO/Fyne + go-sqlite3 + DPAPI | 本地凭据 DPAPI 加密 |
| freecut | React 19 + WebGPU | 独立编辑器，代码量最大 |

---

## 3. 模块组成与代码规模

| 模块 | 目录 | 文件数 | 代码行数 |
|---|---|---|---|
| FVCC 后端 | `FVCC/server` | 49（.go） | 16,326 |
| FVCC 前端 | `FVCC/ui-src` | 29（.ts） | 9,103 |
| FVCS | `FVCS/cmd + pkg` | 50（.go） | 13,455 |
| freecut | `freecut/src` | 2,365（.ts） | 459,679 |

其他组成：
- `FVCC/manifest`、`FVCC/ui-src/package.json`：版本与打包描述；
- `WebVideoEditor_Design/`：Web 剪辑功能的细化设计文档包（01-10 + README）；
- 根目录 `_archive_md/`：历史分析/说明文档归档。

---

## 4. 现状分析

### 4.1 优点

- 双端架构清晰，调度端（NAS）与渲染端（Windows）职责分离，素材免迁移；
- FVCC 前端零框架、轻量，构建产物小；
- FVCS 凭据使用 DPAPI 本地加密，渲染端作为挂载方持有凭据，NAS 侧不落明文；
- freecut 提供完整 WebGPU 剪辑能力，可作为 FVCC 编辑器功能的参照与演进方向。

### 4.2 不足

| # | 问题 | 影响 |
|---|---|---|
| 1 | FVCC 使用 JSON 文件持久化而非数据库 | 并发与一致性弱，任务/项目数据量增大后不可控 |
| 2 | `build.ps1` 仍设 `GOTOOLCHAIN=local` | 与 `go.mod` 声明版本不一致，fpk 打包链路失败 |
| 3 | 前端手写 DOM | 页面单体大（如 scanner.ts 47KB），维护成本高 |
| 4 | freecut 与 FVCC editor 功能重叠 | 双份编辑器逻辑，功能与维护双轨 |
| 5 | 文档版本滞后 | 分析报告写 Go 1.22/1.25，实际 go.mod 已 1.27.1 |
| 6 | 根目录堆积历史 .fpk/exe 备份 | 仓库治理差，易误用旧包 |

---

## 5. 改进方向建议清单

### P0（阻塞/正确性，优先处理）

- [x] 修复构建链路：`build.ps1` 的 `GOTOOLCHAIN` 由 `local` 改为 `auto`，消除 fpk 打包失败；✅ 已落地（P0-1，2026-09-19 复核）
  - 建议动作：
    - 统一 `GOTOOLCHAIN=auto`，或回退 `go.mod` 声明版本与本地 SDK 一致（二选一，消除版本漂移）；
    - 同步更新 `BUILD.md` 构建说明，与新的构建策略保持一致；
    - 增加 CI 版本一致性检查：`go.mod` / `manifest` / `package.json` / `VERSION` 四方对齐校验。

- [ ] FVCC 存储迁 SQLite：任务、项目、渲染节点数据由 JSON 文件迁移至数据库，提供事务与恢复能力。❌ 未落地（P0-2 续延，需独立里程碑）
  - 建议动作：
    - 将 `store.go` 的 JSON 文件存储迁移至 SQLite，替换全量扫描/读写逻辑；
    - 引入事务（批量写入）与索引（任务状态、时间、节点维度），保证并发与查询性能。

### P1（体验与安全增强）

- [ ] 调度器优化：任务排队、失败重试、节点健康分选机；🟡 部分落地（P1-1 完整版未落地；1s tick 仍为全量扫描，事件驱动未做）
  - 建议动作：
    - 由 1s tick 全量扫描改为事件驱动 + 内存索引 + 定时落盘，降低 IO 与调度延迟；
    - 任务状态变更统一走 WS Hub 广播，替代前端轮询。

- [ ] 前端收敛：以 freecut 为参照逐步收敛 FVCC editor，减少双轨维护；🟡 部分落地（P1-2 未定案；P2-2 组件化 4 件套已收口，编辑器区已拆 15 模块）
  - 建议动作：
    - 二选一并定案：引入轻量框架（Vue3/React）重构 FVCC editor，或直接复用 freecut 底座；
    - 收敛后删除冗余 editor 代码，避免长期双轨维护。

- [ ] 数据面加密：渲染任务与凭据传输加密，消除 SMB 明文凭据残留；🟡 部分落地（WSS 已支持：节点配置 useWSS/tlsCACert/tlsSkipVerify 后数据面走 WSS，2026-09-18 实测；默认仍明文，公网部署需显式开启）
  - 建议动作：
    - 控制面与数据面启用 HTTPS/WSS（局域网内可自签证书）；
    - 输出安全策略文档：公网环境禁用 `0.0.0.0` 监听，仅绑定内网网卡。

### P2（可观测性与质量）

- [ ] 可观测性：接入结构化日志、指标与告警（当前失败原因需翻 `D:\FVCS\logs\`）；🟡 部分落地（/metrics/prometheus 指标端点与测试已实现，见代码注释 P2-1"可观测性端点验证"；结构化日志/告警未做）
  - 建议动作：
    - 接入 Prometheus，暴露 `/metrics` 端点；
    - 上报任务耗时、失败率、节点健康度等关键指标；
    - 日志引入追踪 ID（trace ID），贯穿任务从下发到完成的全生命周期。

- [ ] 测试补强：补齐 FVCC/FVCS 契约与回归用例；🟡 部分落地（FVCC 侧 36 个 *_test.go 随 P2-1 分层迁移，CI + 覆盖率门禁 55% 已建；FVCS 侧 GUI 抽离未做，EDL fuzz 已存在）
  - 建议动作：
    - 将 FVCS GUI 逻辑抽离为可测的纯函数/服务层；
    - 增加 EDL 构造 fuzz 测试与并发压力测试（多任务、多节点场景）。

- [ ] 仓库治理：清理根目录历史 .fpk/exe 备份，纳入版本管理与发布目录；🟡 部分落地（.gitignore 已覆盖 *.exe/*.fpk、temp 130MB 已清理、archive/ 已收纳 v1.1.0~v1.3.1 旧包；根目录 fvcc.exe/5 个版本 fpk/fvcs-service.exe 与 FVCS/ 残留仍待物理清理）
  - 建议动作：
    - `.fpk` / `.exe` 与备份统一归入 `release/`、`archive/` 目录；
    - 补充 `.gitignore` 排除构建产物与备份；
    - 清理根目录临时文件与历史旧包，避免误用。

### P3（文档与一致性）

- [ ] 文档同步：分析报告、升级指南与实际 go.mod/package.json 版本对齐；✅ 已落地（P3-1，2026-09-19 复核）：check-versions.ps1 四方校验已建，混乱报告/README/BUILD/FVCC-README 版本表已对齐 1.4.4
  - 建议动作：
    - 建立"代码即真相"机制，文档引用版本一律以源码为准；
    - 版本数字定期对齐，或由构建脚本自动生成版本报告（go.mod / manifest / package.json / VERSION 汇总）。

- [ ] 建立文档快照漂移说明机制，源码变动时同步回写设计文档；✅ 已落地（P3-2）：docs/ 下混乱报告/项目分析/TASK_REFERENCE 均带维护日期与版本基线，README 版本表随 check-versions.ps1 复核更新
  - 建议动作：
    - 文档头部追加快照漂移说明字段（记录源码基线时间与版本），源码变动时回写；
    - 与"代码即真相"机制合并执行，由同一版本报告驱动文档更新。

---

*本文档基于源码分析生成，作为项目后续迭代的基线参考。*

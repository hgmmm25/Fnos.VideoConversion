# Fnos.VideoConversion 项目分析与改进方向

> **文档快照漂移说明（P3-2）**：快照基线 2026-09-17（应用版本 1.4.0 / go 1.27.1）；2026-09-19 更新至 1.4.4 基线并回写各条目落实状态；**2026-09-20 更新至 1.4.9 基线**（manifest / VERSION / package.json 三处一致，2026-09-20 实测）；**2026-09-21 更新至 1.5.3 基线**（manifest / VERSION / package.json 三处一致，2026-09-21 实测）。各条目落实状态详见 `docs/IMPROVEMENT_LOG.md`、`docs/FVCC_混乱度评价报告_细化版.md`，最新目录结构与代码规模见 `docs/FVCC_项目现状与开发步骤.md`。本文档为**分析基线 + 改进方向索引**，源码变动后需回写；版本一致性可用 `scripts/check-versions.ps1` 校验。

> 生成日期：2026-09-17
> 最近更新：2026-09-21（基线 1.5.3）
> 分析范围：FVCC（NAS 调度端 Web UI）+ FVCS（Windows 渲染端）+ freecut（独立浏览器剪辑编辑器）
> 文档性质：项目现状基线 + 改进方向清单，供后续开发与版本规划参考
> 与 docs/ 其他文档的分工（2026-09-20 明确，避免双轨漂移）：
> - 本文档 = **改进方向索引与落实状态回写**（P0/P1/P2/P3 清单）；
> - `FVCC_项目现状与开发步骤.md` = 当前现状与下一步开发指引（v1.4.9 实测，含代码级 TODO）；
> - `FVCC_混乱度评价报告_细化版.md` = 工程卫生治理台账（分维度评分 + P 项状态汇总）；
> - `IMPROVEMENT_LOG.md` = 逐轮落实日志；`TASK_REFERENCE.md` = 编号对照表；`SECURITY.md` = 数据面安全策略；`FVCC_UI设计优化方向.md` = UI 设计方向与落地记录。

---

## 1. 项目定位

Fnos.VideoConversion 是一套**双端远程视频转码与剪辑系统**：

- **FVCC（调度端）**：部署于 NAS，以 Web UI 形式提供转码任务调度、素材管理与后续 Web 剪辑入口；
- **FVCS（渲染端）**：部署于局域网内的高算力 Windows 主机，负责接收任务并调用 FFmpeg 完成转码/渲染，成品回写 NAS 共享目录；
- **freecut**：仓库内含的独立浏览器剪辑编辑器（上游参考项目），提供完整的 Web 端视频编辑能力。

架构要点：素材不离开 NAS，渲染节点通过 SMB 直挂共享目录读写素材与成品，免分片上传/下载链路。

---

## 2. 技术栈

| 模块 | 技术栈 | 备注 |
|---|---|---|
| FVCC 后端 | Go 1.27.1 + gin + gorilla/websocket | 无 CGO，交叉编译 linux/amd64 |
| FVCC 前端 | TypeScript 7 + Vite + Tailwind CSS | 纯手写 DOM（无框架），公共样板已收敛至 src/lib/ 4 件套 |
| FVCS | Go 1.27.1 + CGO/Fyne + go-sqlite3 + DPAPI | 本地凭据 DPAPI 加密 |
| freecut | React 19 + WebGPU | 独立编辑器，代码量最大 |

---

## 3. 模块组成与代码规模

（2026-09-20 晚实测，基线 1.4.9）

| 模块 | 目录 | 文件数 | 代码行数 |
|---|---|---|---|
| FVCC 后端 | `FVCC/server` | 87（.go，49 实现 + 38 测试；internal 11 包 + store/model 子包，根包仅剩 main.go） | 19,315（实现 13,107 + 测试 6,208） |
| FVCC 前端 | `FVCC/ui-src` | 33（.ts，src/ 含 lib/ 4 件套 + pages/editor/ 14 模块） | 10,035 |
| FVCS | `FVCS/cmd + pkg` | 52（.go） | 13,704 |
| freecut | `freecut/src` | 1,846（.ts） | 294,057 |

其他组成：
- `FVCC/manifest`、`FVCC/ui-src/package.json`：版本与打包描述（与 `server/internal/version/VERSION` 三处一致 1.4.9，由 version_test.go 自动校验）；
- `WebVideoEditor_Design/`：Web 剪辑功能的细化设计文档包（01-10 + README，已入 FVCC/README 速查表）；
- 根目录 `_archive_md/`：历史分析/说明文档归档；
- 根仓库 `archive/`：历史 .fpk（v1.1.0~v1.3.1 + v1.4.6）与备份目录。

---

## 4. 现状分析

### 4.1 优点

- 双端架构清晰，调度端（NAS）与渲染端（Windows）职责分离，素材免迁移；
- FVCC 前端零框架、轻量，构建产物小（主 chunk 192.6 KB，图表库异步加载）；
- FVCS 凭据使用 DPAPI 本地加密，渲染端作为挂载方持有凭据，NAS 侧不落明文；
- freecut 提供完整 WebGPU 剪辑能力，可作为 FVCC 编辑器功能的参照与演进方向；
- 工程治理闭环已建立（2026-09-17 首版以来）：凭据 AES-GCM 加密 / 删除回收站化 / 后端分层 internal（87 个 .go）/ 前端组件化 4 件套 / CI + 覆盖率门禁 / 破坏性操作审计闭环，P0/P1/P2 治理项全部落地（详见混乱报告第五版）。

### 4.2 不足

| # | 问题 | 影响 | 现状（2026-09-20 复核，基线 1.4.9） |
|---|---|---|---|
| 1 | FVCC 使用 JSON 文件持久化而非数据库 | 并发与一致性弱，任务/项目数据量增大后不可控 | 未变（P0-2 续延，需独立里程碑；混乱报告 P3-5 登记 store 持久化健壮性：定时备份 / 配置迁移 / 快照回滚待做） |
| 2 | `build.ps1` 仍设 `GOTOOLCHAIN=local` | 与 `go.mod` 声明版本不一致，fpk 打包链路失败 | ✅ 已修复（P0-1，2026-09-19 复核） |
| 3 | 前端手写 DOM | 页面单体大，维护成本高 | 已基本收口（P2-2 组件化 4 件套接入 6 页、样板残留 0，编辑器区已拆 14 模块；scanner.ts 1,332 行仍为最大单文件，但已分页 + 组件化） |
| 4 | freecut 与 FVCC editor 功能重叠 | 双份编辑器逻辑，功能与维护双轨 | 未定案（P1-2 续延；UI 优化方向已落地（1.4.9），"引入框架或复用 freecut 底座"产品决策仍开放） |
| 5 | 文档版本滞后 | 分析报告与源码版本漂移 | ✅ 已解决（P3-1：check-versions.ps1 四方校验；docs/ 内 7 份台账已对齐 1.4.9；README（1.4.4）/ BUILD（1.4.6）版本表仍滞后，属 docs/ 外，混乱报告 P3-4 登记待根仓库同步） |
| 6 | 根目录堆积历史 .fpk/exe 备份 | 仓库治理差，易误用旧包 | 部分改善（archive/ 已收纳 v1.1.0~v1.3.1 旧包；根目录 v1.4.0~v1.4.9 共 10 个版本包 + fvcc.exe / fvcs-service.exe 仍物理堆积；temp/ 回潮至 549.7MB（node_modules 全量 / chrome-profile / 调试残留 / secret.key 敏感残留）；task.db / credentials.db / master.key / secret.key / locks.json 等运行时数据混入根目录，待清理归位） |

---

## 5. 改进方向建议清单

### P0（阻塞/正确性，优先处理）

- [x] 修复构建链路：`build.ps1` 的 `GOTOOLCHAIN` 由 `local` 改为 `auto`，消除 fpk 打包失败；✅ 已落地（P0-1，2026-09-19 复核）。已由 `scripts/check-versions.ps1` 覆盖 go.mod / manifest / package.json / VERSION 四方一致性校验，BUILD.md 已同步。

- [ ] FVCC 存储迁 SQLite：任务、项目、渲染节点数据由 JSON 文件迁移至数据库，提供事务与恢复能力。❌ 未落地（P0-2 续延，需独立里程碑）
  - 建议动作：
    - 将 `store.go` 的 JSON 文件存储迁移至 SQLite，替换全量扫描/读写逻辑；
    - 引入事务（批量写入）与索引（任务状态、时间、节点维度），保证并发与查询性能；
    - 落地前先补齐 JSON 存储的定时备份 / 配置迁移 / 快照回滚（混乱报告 P3-5）。

### P1（体验与安全增强）

- [x] 调度器优化：任务排队、失败重试、节点健康分选机；✅ 已落地（P1-1，2026-09-21 复核）：后端调度器由 1s tick 全量扫描改为事件驱动 select 多路循环（事件 / 1s tick 兜底 / 5s 落盘 / stop），store 层新增内存索引（taskByID / checksumIdx）与 Flush 定时落盘、任务变更钩子（创建 / 状态变更触发，纯进度更新不触发）；冷却注册唤醒 timer，Stop 优雅退出并 Flush；全部状态变更（20+ 处）统一走 hub.BroadcastTaskUpdate 广播，前端已由轮询改为 WS 订阅（store.startWS 订阅 task_update / snapshot / node_status / proxy_ready，完成态延迟移入历史，断线自动重连 3s）。验证：go build ./... 与 go test ./... 全量通过（11 包），前端 npm run build（check:design:diff + tsc -b + vite build）通过。任务排队 / 失败重试 / 节点健康分选机属 P1-1 后续演进项，另行立项。
  - 建议动作：
    - 由 1s tick 全量扫描改为事件驱动 + 内存索引 + 定时落盘，降低 IO 与调度延迟；✅ 已落地（2026-09-21）
    - 任务状态变更统一走 WS Hub 广播，替代前端轮询；✅ 已落地（2026-09-21）

- [ ] 前端收敛：以 freecut 为参照逐步收敛 FVCC editor，减少双轨维护；🟡 部分落地（P1-2 未定案；P2-2 组件化 4 件套已收口、UI 优化方向阶段 A-D 已落地（1.4.9）；"引入轻量框架或复用 freecut 底座"决策门未关闭）
  - 建议动作：
    - 二选一并定案：引入轻量框架（Vue3/React）重构 FVCC editor，或直接复用 freecut 底座；
    - 收敛后删除冗余 editor 代码，避免长期双轨维护。

- [ ] 数据面加密：渲染任务与凭据传输加密，消除 SMB 明文凭据残留；🟡 部分落地（WSS 已支持：节点配置 useWSS/tlsCACert/tlsSkipVerify 后数据面走 WSS，2026-09-18 实测；`docs/SECURITY.md` 已输出数据面安全策略（2026-09-20）；默认仍明文，公网部署需显式开启）
  - 建议动作：
    - 控制面与数据面启用 HTTPS/WSS（局域网内可自签证书）；
    - 公网环境禁用 `0.0.0.0` 监听，仅绑定内网网卡（已写入 SECURITY.md）。

### P2（可观测性与质量）

- [ ] 可观测性：接入结构化日志、指标与告警（当前失败原因需翻 `D:\FVCS\logs\`）；🟡 部分落地（`/metrics` Prometheus 指标端点与测试已实现（P2-1"可观测性端点验证"）；trace ID 全链路注入已完成（2026-09-18，见 IMPROVEMENT_LOG §七）；结构化告警未做）
  - 建议动作：
    - 上报任务耗时、失败率、节点健康度等关键指标至 Prometheus（端点已就绪，待接入采集端）；
    - 补齐结构化告警（阈值 / 通知渠道）。

- [ ] 测试补强：补齐 FVCC/FVCS 契约与回归用例；🟡 部分落地（FVCC 侧 38 个 *_test.go 随 P2-1 分层迁移，新增 render_progress / model_convert 回归，CI + 覆盖率门禁 55%（基线 56.1%）已建；FVCS 侧 GUI 抽离未做，EDL fuzz 已存在）
  - 建议动作：
    - 将 FVCS GUI 逻辑抽离为可测的纯函数/服务层；
    - 增加 EDL 构造 fuzz 测试与并发压力测试（多任务、多节点场景）。

- [ ] 仓库治理：清理根目录历史 .fpk/exe 备份，纳入版本管理与发布目录；🟡 部分落地（.gitignore 已覆盖 *.exe/*.fpk；archive/ 已收纳 v1.1.0~v1.3.1 旧包；**根目录 v1.4.0~v1.4.9 共 10 个版本包 + fvcc.exe/fvcs-service.exe 仍物理堆积；temp/ 回潮至 549.7MB（node_modules 全量 / chrome-profile / cdp 调试残留 / temp/secret.key 敏感残留）；运行时数据（task.db / credentials.db / master.key / secret.key / locks.json）混入根目录待归位**；⚠️ **git 工作区 1.4.7~1.4.9 改动未提交（manifest / VERSION / models.go / 前端 editor 等），最新提交仍为 1.4.6 同步（adabef9），快照漂移风险实锤——先提交再清理**）
  - 建议动作：
    - `.fpk` / `.exe` 与备份统一归入 `release/`、`archive/` 目录；
    - 清理 temp/ 回潮与根目录运行时数据，`temp/secret.key` 需确认处置（删除或归位规范目录）；
    - 提交 git 未跟踪改动并打 tag，恢复"代码即真相"基线。

### P3（文档与一致性）

- [x] 文档同步：分析报告、升级指南与实际 go.mod/package.json 版本对齐；✅ 已落地（P3-1，2026-09-20 复核）：check-versions.ps1 四方校验已建；docs/ 内 7 份台账（混乱报告 / 项目现状 / 项目分析 / TASK_REFERENCE / SECURITY / IMPROVEMENT_LOG / UI 优化方向）已对齐 1.4.9；README（1.4.4）/ BUILD（1.4.6）版本表属 docs/ 外滞后，待根仓库同步（混乱报告 P3-4 登记）

- [x] 建立文档快照漂移说明机制，源码变动时同步回写设计文档；✅ 已落地（P3-2）：docs/ 下混乱报告 / 项目现状 / 项目分析 / TASK_REFERENCE / IMPROVEMENT_LOG / UI 优化方向 / SECURITY 均带维护日期与版本基线；编号体系对照见 `docs/TASK_REFERENCE.md`（含编号漂移登记）

---

*本文档基于源码分析生成，作为项目后续迭代的基线参考。*
